<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>EXCEL中VLOOKUP的应用</title>
    <url>/%E5%B7%A5%E5%85%B7/EXCEL%20VLOOKUP%E7%9A%84%E5%BA%94%E7%94%A8/</url>
    <content><![CDATA[<h1 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h1><p>别再用JAVA撸excel中的数据啦，vlookup爽到起飞~</p>
<a id="more"></a>
<h1 id="VLOOKUP函数"><a href="#VLOOKUP函数" class="headerlink" title="VLOOKUP函数"></a>VLOOKUP函数</h1><p>lookup函数是Excel中的一个纵向查找函数，他可以用来进行数据核对，多个表格之间的数据进行快速引用，动态表格的制作等它主要包括四个参数</p>
<p>1.lookup_value:要查找的值数值、引用或文本字符串</p>
<p>2.table_array :要查找的区域数据表区域</p>
<p>3.col_index_num: 返回数据在查找区域的第几列数正整数</p>
<p>4.range_lookup :近似匹配/精确匹配TRUE/FALSE（或不填）<br><img src="/images/lookup1.jpeg" alt="alt"></p>
<p>在这里我们要查找“橙子的单价”，橙子所在位置在：D2，我们查找的区域是水果单价区域即：A2：B5，水果单价在区域的第2列，所以匹配列为2，因为要精确获得水果单价所以我们选择精确匹配是不是非常简单呢，前期如果我们不太熟悉vlookup函数，我们可以调用函数参数对话框来设置函数效果虽然是一样的，但是操作起来更加直观便于理解<br><img src="/images/lookup2.gif" alt="alt"></p>
<p>Vlookup函数使用十分的简单，但是仍然需要注意一下几点：</p>
<p>1.查找值中不可有重复值：如果查找值中有重复值，所有的重复值都讲返回一个相同的值，</p>
<p>2.如果没有特殊的要求，一般对查找区域进行绝对引用，可以有效避免拖动公式带来的数据报错</p>
<p>3.查找值，和查找区域中的查找值必须完全一致，有时候我们会发现公式运用没有错但是就是返回错误值，可能就是查找值和查找区域中的查找值不不一样导致的，比如空格</p>
<p>4.数据格式统一：如果你检查了所有内容确定没有问题，可以看下是否是数据格式不同所造成的</p>
<h1 id="多条件查询"><a href="#多条件查询" class="headerlink" title="多条件查询"></a>多条件查询</h1><p>{=VLOOKUP(F2&amp;G2,IF({1,0},A2:A10&amp;B2:B10,C2:C10),2,0)}</p>
<p>我要根据姓名和部门来进行查找，避免重名时候查找错误<br><img src="/images/lookup2.jpeg" alt="alt"></p>
<p>这种办法是利用if函数构建一个二维数组，用于查找，公式外必须加大括号，因为公式本身利用了数组函数。是不是看的一头雾水，不妨试试下面这种办法更加直观，只不过需要添加辅助列。删除辅助之前需要将公式所得数据粘贴为数值，才不会报错。辅助列可以串联更多数据，可以根据自己的需要来串联<br><img src="/images/lookup3.jpeg" alt="alt"></p>
<h1 id="反向查找"><a href="#反向查找" class="headerlink" title="反向查找"></a>反向查找</h1><p>{=VLOOKUP(G2,IF({1,0},C2:C10,A2:A10),2,0)}</p>
<p>因为利用vlookup函数只能利用左边的数据来查找右边的数据，当我想用右边的数据来查找左边的数据该怎么办呢，原理跟反向查找是一样的，利用if函数构建一个二维数组，下面让我来看看怎么操作把<br><img src="/images/lookup4.jpeg" alt="alt"><br>如果工作中需要用到反向查找，只需更换公式中，查找位置，得分列和姓名列即可使用</p>
<h1 id="区间查找"><a href="#区间查找" class="headerlink" title="区间查找"></a>区间查找</h1><p>在使用vlookup进行区间查找时，我们必须明白在使用近似查找时函数会选择小于查找内容的最大值<br><img src="/images/lookup5.jpeg" alt="alt"><br>我们要取每个区间的最小值然后对应所得薪资构建新的辅助列，新辅助必须以考核得分为准升序排列因为近似匹配会查找小于查找内容的最大值</p>
<p>我们以图中93分为例，93分所在期间为95-80之间，当使用函数查找时，他会选择小于93的最大值即：80，80所对应的薪资为1500，正好在其区域。</p>
]]></content>
      <categories>
        <category>工具</category>
      </categories>
      <tags>
        <tag>工具</tag>
        <tag>excel</tag>
      </tags>
  </entry>
  <entry>
    <title>Spring：基于注解的Spring MVC（下）</title>
    <url>/%E6%A1%86%E6%9E%B6/Spring%EF%BC%9A%E5%9F%BA%E4%BA%8E%E6%B3%A8%E8%A7%A3%E7%9A%84Spring%20MVC%EF%BC%88%E4%B8%8B%EF%BC%89%20/</url>
    <content><![CDATA[<h1 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h1><p>springMVC中数据是如何交互的</p>
<a id="more"></a>
<h1 id="Model"><a href="#Model" class="headerlink" title="Model"></a>Model</h1><p>1、Model本身是一个接口，其实现类为ExtendedModelMap，除了使用Model之外还可以使用ModelAndView、ModelMap这些，不过要是没有特殊需求，使用Model比较简单，我个人也比较喜欢使用Model</p>
<p>2、Model的生命周期是Request，也就是说要通过Model传值只能使用转发而不能使用重定向</p>
<p>3、为什么要使用Model而不是用Request，最主要的原因就是减少代码的侵入性或者说代码的耦合度也行。因为Model是Spring的组件，Request是J2EE的组件，使用Model而不去使用Request可以减少对J2EE的依赖，也便于调试</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">@Controller</span><br><span class="line">@RequestMapping(value &#x3D; &quot;&#x2F;test&quot;)</span><br><span class="line">public class TestController</span><br><span class="line">&#123;</span><br><span class="line">    @RequestMapping</span><br><span class="line">    public String dispatchTest(Test test, Model model)</span><br><span class="line">    &#123;</span><br><span class="line">        model.addAttribute(&quot;modelKey&quot;, &quot;modelValue&quot;);</span><br><span class="line">        return &quot;test&quot;;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>就往Model里面塞一个Key-Value，然后转发到test.jsp下，test.jsp页面要取Model的值，可以通过JSTL（EL表达式也可以）获取，反正直接在jsp页面上通过”&lt;% … %&gt;”写Java脚本是行不通的。test.jsp页面这么写：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;%@ page language&#x3D;&quot;java&quot; import&#x3D;&quot;java.util.*&quot; pageEncoding&#x3D;&quot;ISO-8859-1&quot;%&gt;</span><br><span class="line">&lt;%@ taglib uri&#x3D;&quot;http:&#x2F;&#x2F;java.sun.com&#x2F;jsp&#x2F;jstl&#x2F;core&quot; prefix&#x3D;&quot;c&quot; %&gt;</span><br><span class="line">&lt;%@ taglib uri&#x3D;&quot;http:&#x2F;&#x2F;java.sun.com&#x2F;jsp&#x2F;jstl&#x2F;fmt&quot; prefix&#x3D;&quot;fmt&quot; %&gt;</span><br><span class="line">&lt;%</span><br><span class="line">String path &#x3D; request.getContextPath();</span><br><span class="line">String basePath &#x3D; request.getScheme()+&quot;:&#x2F;&#x2F;&quot;+request.getServerName()+&quot;:&quot;+request.getServerPort()+path+&quot;&#x2F;&quot;;</span><br><span class="line">%&gt;</span><br><span class="line"></span><br><span class="line">&lt;!DOCTYPE HTML PUBLIC &quot;-&#x2F;&#x2F;W3C&#x2F;&#x2F;DTD HTML 4.01 Transitional&#x2F;&#x2F;EN&quot;&gt;</span><br><span class="line">&lt;html&gt;</span><br><span class="line">      &lt;head&gt;</span><br><span class="line">        &lt;base href&#x3D;&quot;&lt;%&#x3D;basePath%&gt;&quot;&gt;</span><br><span class="line">        </span><br><span class="line">        &lt;title&gt;test页面&lt;&#x2F;title&gt;</span><br><span class="line">        </span><br><span class="line">        &lt;meta http-equiv&#x3D;&quot;pragma&quot; content&#x3D;&quot;no-cache&quot;&gt;</span><br><span class="line">        &lt;meta http-equiv&#x3D;&quot;cache-control&quot; content&#x3D;&quot;no-cache&quot;&gt;</span><br><span class="line">        &lt;meta http-equiv&#x3D;&quot;expires&quot; content&#x3D;&quot;0&quot;&gt;    </span><br><span class="line">        &lt;meta http-equiv&#x3D;&quot;keywords&quot; content&#x3D;&quot;keyword1,keyword2,keyword3&quot;&gt;</span><br><span class="line">        &lt;meta http-equiv&#x3D;&quot;description&quot; content&#x3D;&quot;This is my page&quot;&gt;</span><br><span class="line">        &lt;!--</span><br><span class="line">        &lt;link rel&#x3D;&quot;stylesheet&quot; type&#x3D;&quot;text&#x2F;css&quot; href&#x3D;&quot;styles.css&quot;&gt;</span><br><span class="line">        --&gt;</span><br><span class="line">      &lt;&#x2F;head&gt;</span><br><span class="line">  </span><br><span class="line">      &lt;body&gt;</span><br><span class="line">          &lt;c:out value&#x3D;&quot;$&#123;modelKey&#125;&quot; &#x2F;&gt;</span><br><span class="line">      &lt;&#x2F;body&gt;</span><br><span class="line">&lt;&#x2F;html&gt;</span><br></pre></td></tr></table></figure>
<p>OK，然后访问一下”<a href="http://localhost:8080/SpringMVC/test&quot;这个地址，页面上&quot;modelValue&quot;这几个字符就出来了。" target="_blank" rel="noopener">http://localhost:8080/SpringMVC/test&quot;这个地址，页面上&quot;modelValue&quot;这几个字符就出来了。</a></p>
<p>之前说过了，Model的生命周期是Request，那么如果页面是重定向到test.jsp上面去，肯定是取不到”modelValue”的，可以自己试一下，因此重定向过去的话，要在后台把数据设置到session中。</p>
<p>test.jsp页面不变，Controller可以这么改：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">@Controller</span><br><span class="line">@RequestMapping(value &#x3D; &quot;&#x2F;test&quot;)</span><br><span class="line">public class TestController</span><br><span class="line">&#123;</span><br><span class="line">    @RequestMapping</span><br><span class="line">    public String dispatchTest(Test test, HttpSession session)</span><br><span class="line">    &#123;</span><br><span class="line">        session.setAttribute(&quot;modelKey&quot;, &quot;modelValue&quot;);</span><br><span class="line">        return &quot;redirect:&#x2F;test.jsp&quot;;</span><br><span class="line">        &#x2F;&#x2F;return &quot;test&quot;;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>可以试一下，再访问一下”<a href="http://localhost:8080/SpringMVC/test&quot;这个地址，&quot;modelValue&quot;这几个字符在页面上就出来了。" target="_blank" rel="noopener">http://localhost:8080/SpringMVC/test&quot;这个地址，&quot;modelValue&quot;这几个字符在页面上就出来了。</a></p>
<p>在Spring MVC中，Request、Response、Session、InputStream、OutputStream这些对象是自动注入的，但是就像之前说的，为了减少代码的侵入性与耦合度，能不使用尽量还是不使用这些J2EE的对象的好。</p>
<h1 id="拦截器（Interceptor）"><a href="#拦截器（Interceptor）" class="headerlink" title="拦截器（Interceptor）"></a>拦截器（Interceptor）</h1><p>SpringMVC中的拦截器相当于J2EE中的过滤器，是非常重要和相当有用的，它的主要作用就是拦截用户的请求并进行相应的处理的，比如通过它来进行权限验证，或者是来判断用户是否登陆。</p>
<p>在SpringMVC中使用拦截器的方法比较简单，首先实现HandlerInterceptor接口，实现afterCompletion、postHandle、preHandle三个抽象方法，这里定义两个Interceptor：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">public class TestInterceptor1 implements HandlerInterceptor</span><br><span class="line">&#123;</span><br><span class="line">    public void afterCompletion(HttpServletRequest arg0,</span><br><span class="line">            HttpServletResponse arg1, Object arg2, Exception arg3)</span><br><span class="line">            throws Exception</span><br><span class="line">    &#123;</span><br><span class="line">        System.out.println(&quot;TestInterceptor1.afterCompletion()&quot;);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public void postHandle(HttpServletRequest arg0, HttpServletResponse arg1,</span><br><span class="line">            Object arg2, ModelAndView arg3) throws Exception</span><br><span class="line">    &#123;</span><br><span class="line">        System.out.println(&quot;TestInterceptor1.postHandle()&quot;);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public boolean preHandle(HttpServletRequest arg0, HttpServletResponse arg1,</span><br><span class="line">            Object arg2) throws Exception</span><br><span class="line">    &#123;</span><br><span class="line">        System.out.println(&quot;TestInterceptor1.preHandle()&quot;);</span><br><span class="line">        return true;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">public class TestInterceptor2 implements HandlerInterceptor</span><br><span class="line">&#123;</span><br><span class="line">    public void afterCompletion(HttpServletRequest arg0,</span><br><span class="line">            HttpServletResponse arg1, Object arg2, Exception arg3)</span><br><span class="line">            throws Exception</span><br><span class="line">    &#123;</span><br><span class="line">        System.out.println(&quot;TestInterceptor2.afterCompletion()&quot;);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public void postHandle(HttpServletRequest arg0, HttpServletResponse arg1,</span><br><span class="line">            Object arg2, ModelAndView arg3) throws Exception</span><br><span class="line">    &#123;</span><br><span class="line">        System.out.println(&quot;TestInterceptor2.postHandle()&quot;);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public boolean preHandle(HttpServletRequest arg0, HttpServletResponse arg1,</span><br><span class="line">            Object arg2) throws Exception</span><br><span class="line">    &#123;</span><br><span class="line">        System.out.println(&quot;TestInterceptor2.preHandle()&quot;);</span><br><span class="line">        return true;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>说明一下三个方法的作用：</p>
<p>1、afterCompletion：在整个视图渲染完毕之后执行方法里面的内容，主要用于释放一些资源</p>
<p>2、postHandle：在Controller执行之后，视图渲染之前执行方法里面的内容，也就是说postHandle方法可以对Model进行操作</p>
<p>3、preHandle：在Controller执行之前，执行方法里面的内容，注意该方法是有返回值的，当方法返回false时整个请求就结束了</p>
<p>然后在springmvc-servlet.xml里面增加拦截器的配置：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;!-- 配置拦截器 --&gt;</span><br><span class="line">&lt;mvc:interceptors&gt;</span><br><span class="line">   &lt;mvc:interceptor&gt;</span><br><span class="line">       &lt;mvc:mapping path&#x3D;&quot;&#x2F;test&quot; &#x2F;&gt;</span><br><span class="line">        &lt;bean class&#x3D;&quot;com.xrq.interceptor.TestInterceptor2&quot; &#x2F;&gt;</span><br><span class="line">    &lt;&#x2F;mvc:interceptor&gt;</span><br><span class="line">    &lt;mvc:interceptor&gt;</span><br><span class="line">        &lt;mvc:mapping path&#x3D;&quot;&#x2F;test&quot; &#x2F;&gt;</span><br><span class="line">        &lt;bean class&#x3D;&quot;com.xrq.interceptor.TestInterceptor1&quot; &#x2F;&gt;</span><br><span class="line">    &lt;&#x2F;mvc:interceptor&gt;</span><br><span class="line">&lt;&#x2F;mvc:interceptors&gt;</span><br></pre></td></tr></table></figure>
<p>假如有多个拦截器的话，”<a href="mvc:interceptor">mvc:interceptor</a>…</mvc:interceptor>“定义的顺序就是拦截器执行的顺序。</p>
]]></content>
      <categories>
        <category>框架</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>框架</tag>
        <tag>spring</tag>
      </tags>
  </entry>
  <entry>
    <title>Spring：基于注解的Spring MVC（上）</title>
    <url>/%E6%A1%86%E6%9E%B6/Spring%EF%BC%9A%E5%9F%BA%E4%BA%8E%E6%B3%A8%E8%A7%A3%E7%9A%84Spring%20MVC%EF%BC%88%E4%B8%8A%EF%BC%89/</url>
    <content><![CDATA[<h1 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h1><p>Spring MVC框架是一个MVC框架，通过实现Model-View-Controller模式来很好地将数据、业务与展现进行分离。从这样一个角度来说，Spring MVC和Structs、Structs2非常类似。Spring MVC的设计是围绕DispatcherServlet展开的，DispatcherServlet负责将请求派发到特定的handler。通过可配置的hander mappings、view resolution、locale以及theme resolution来处理请求并且转到对应的视图。Spring MVC请求处理的整体流程如图：</p>
<a id="more"></a>
<p><img src="/images/springmvc.png" alt="alt"></p>
<h1 id="Spring-MVC环境搭建"><a href="#Spring-MVC环境搭建" class="headerlink" title="Spring MVC环境搭建"></a>Spring MVC环境搭建</h1><p>要开始本文后面的内容，自然要搭建一个Spring MVC的环境，那么首先建立一个Java Web的工程，我建立的工程名字叫做SpringMVC，要搭建一个基础功能的Spring MVC环境，必须引入的jar包是beans、context、core、expression、web、webmvc以及commons-logging。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">		&lt;?xml version&#x3D;&quot;1.0&quot; encoding&#x3D;&quot;UTF-8&quot;?&gt;</span><br><span class="line">&lt;web-app version&#x3D;&quot;2.5&quot; </span><br><span class="line">    xmlns&#x3D;&quot;http:&#x2F;&#x2F;java.sun.com&#x2F;xml&#x2F;ns&#x2F;javaee&quot; </span><br><span class="line">    xmlns:xsi&#x3D;&quot;http:&#x2F;&#x2F;www.w3.org&#x2F;2001&#x2F;XMLSchema-instance&quot; </span><br><span class="line">    xsi:schemaLocation&#x3D;&quot;http:&#x2F;&#x2F;java.sun.com&#x2F;xml&#x2F;ns&#x2F;javaee </span><br><span class="line">    http:&#x2F;&#x2F;java.sun.com&#x2F;xml&#x2F;ns&#x2F;javaee&#x2F;web-app_2_5.xsd&quot;&gt;</span><br><span class="line">    </span><br><span class="line">    &lt;display-name&gt;&lt;&#x2F;display-name&gt;    </span><br><span class="line">    &lt;welcome-file-list&gt;</span><br><span class="line">        &lt;welcome-file&gt;index.jsp&lt;&#x2F;welcome-file&gt;</span><br><span class="line">    &lt;&#x2F;welcome-file-list&gt;</span><br><span class="line">      </span><br><span class="line">      &lt;!-- 该监听器将在Web容器启动时激活Spring --&gt;</span><br><span class="line">    &lt;listener&gt;</span><br><span class="line">        &lt;listener-class&gt;org.springframework.web.context.ContextLoaderListener&lt;&#x2F;listener-class&gt;</span><br><span class="line">    &lt;&#x2F;listener&gt;</span><br><span class="line">    </span><br><span class="line">    &lt;!-- 处理由JavaBeans,Introspector的使用而引起的缓冲泄露,建议配置此监听器 --&gt;</span><br><span class="line">    &lt;listener&gt;</span><br><span class="line">        &lt;listener-class&gt;org.springframework.web.util.IntrospectorCleanupListener&lt;&#x2F;listener-class&gt;</span><br><span class="line">    &lt;&#x2F;listener&gt;</span><br><span class="line">  </span><br><span class="line">      &lt;!--configure the setting of springmvcDispatcherServlet and configure the mapping--&gt;</span><br><span class="line">    &lt;servlet&gt;</span><br><span class="line">        &lt;servlet-name&gt;springmvc&lt;&#x2F;servlet-name&gt;</span><br><span class="line">        &lt;servlet-class&gt;org.springframework.web.servlet.DispatcherServlet&lt;&#x2F;servlet-class&gt;</span><br><span class="line">        &lt;init-param&gt;</span><br><span class="line">              &lt;param-name&gt;contextConfigLocation&lt;&#x2F;param-name&gt;</span><br><span class="line">              &lt;param-value&gt;classpath:springmvc-servlet.xml&lt;&#x2F;param-value&gt;</span><br><span class="line">          &lt;&#x2F;init-param&gt;</span><br><span class="line">          &lt;load-on-startup&gt;1&lt;&#x2F;load-on-startup&gt; </span><br><span class="line">    &lt;&#x2F;servlet&gt;</span><br><span class="line"> </span><br><span class="line">    &lt;servlet-mapping&gt;</span><br><span class="line">        &lt;servlet-name&gt;springmvc&lt;&#x2F;servlet-name&gt;</span><br><span class="line">        &lt;url-pattern&gt;&#x2F;&lt;&#x2F;url-pattern&gt;</span><br><span class="line">    &lt;&#x2F;servlet-mapping&gt;</span><br><span class="line">&lt;&#x2F;web-app&gt;</span><br></pre></td></tr></table></figure>
<p>两个listener不是必须的，但是servlet是必须的，url-pattern用于开发者选择哪些路径是需要让Spring MVC来处理的。接着在classpath下按照我们约定的名字springmvc-servlet.xml写一个xml文件：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;?xml version&#x3D;&quot;1.0&quot; encoding&#x3D;&quot;UTF-8&quot;?&gt;</span><br><span class="line">&lt;beans xmlns:xsi&#x3D;&quot;http:&#x2F;&#x2F;www.w3.org&#x2F;2001&#x2F;XMLSchema-instance&quot;   </span><br><span class="line">    xmlns&#x3D;&quot;http:&#x2F;&#x2F;www.springframework.org&#x2F;schema&#x2F;beans&quot;  </span><br><span class="line">    xmlns:mvc&#x3D;&quot;http:&#x2F;&#x2F;www.springframework.org&#x2F;schema&#x2F;mvc&quot;  </span><br><span class="line">    xmlns:context&#x3D;&quot;http:&#x2F;&#x2F;www.springframework.org&#x2F;schema&#x2F;context&quot;  </span><br><span class="line">    xmlns:aop&#x3D;&quot;http:&#x2F;&#x2F;www.springframework.org&#x2F;schema&#x2F;aop&quot;  </span><br><span class="line">    xmlns:tx&#x3D;&quot;http:&#x2F;&#x2F;www.springframework.org&#x2F;schema&#x2F;tx&quot;  </span><br><span class="line">    xsi:schemaLocation&#x3D;&quot;http:&#x2F;&#x2F;www.springframework.org&#x2F;schema&#x2F;beans </span><br><span class="line">        http:&#x2F;&#x2F;www.springframework.org&#x2F;schema&#x2F;beans&#x2F;spring-beans-4.2.xsd</span><br><span class="line">        http:&#x2F;&#x2F;www.springframework.org&#x2F;schema&#x2F;mvc</span><br><span class="line">        http:&#x2F;&#x2F;www.springframework.org&#x2F;schema&#x2F;mvc&#x2F;spring-mvc-4.2.xsd</span><br><span class="line">        http:&#x2F;&#x2F;www.springframework.org&#x2F;schema&#x2F;context</span><br><span class="line">        http:&#x2F;&#x2F;www.springframework.org&#x2F;schema&#x2F;context&#x2F;spring-context-4.2.xsd&quot;&gt;    </span><br><span class="line">    </span><br><span class="line">    &lt;context:annotation-config &#x2F;&gt;    </span><br><span class="line">    &lt;context:component-scan base-package&#x3D;&quot;com.xrq.controller&quot;&#x2F;&gt;</span><br><span class="line">    </span><br><span class="line">    &lt;!-- 配置视图解析器 --&gt;</span><br><span class="line">    &lt;bean class&#x3D;&quot;org.springframework.web.servlet.view.InternalResourceViewResolver&quot;&gt;  </span><br><span class="line">        &lt;!-- WebRoot到一指定文件夹文件路径 --&gt;  </span><br><span class="line">        &lt;property name&#x3D;&quot;prefix&quot; value&#x3D;&quot;&#x2F;&quot; &#x2F;&gt;  </span><br><span class="line">        &lt;!-- 视图名称后缀  --&gt;  </span><br><span class="line">        &lt;property name&#x3D;&quot;suffix&quot; value&#x3D;&quot;.jsp&quot; &#x2F;&gt;  </span><br><span class="line">    &lt;&#x2F;bean&gt;  </span><br><span class="line">&lt;&#x2F;beans&gt;</span><br></pre></td></tr></table></figure>
<p>另外，由于使用了Spring，所以Tomcat启动的时候默认会去WEB-INF下找applicationContext.xml，所以放一个空的applicationContext.xml到WEB-INF下：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;?xml version&#x3D;&quot;1.0&quot; encoding&#x3D;&quot;UTF-8&quot;?&gt;</span><br><span class="line">&lt;beans xmlns&#x3D;&quot;http:&#x2F;&#x2F;www.springframework.org&#x2F;schema&#x2F;beans&quot;</span><br><span class="line">    xmlns:xsi&#x3D;&quot;http:&#x2F;&#x2F;www.w3.org&#x2F;2001&#x2F;XMLSchema-instance&quot;</span><br><span class="line">    xsi:schemaLocation&#x3D;&quot;http:&#x2F;&#x2F;www.springframework.org&#x2F;schema&#x2F;beans</span><br><span class="line">    http:&#x2F;&#x2F;www.springframework.org&#x2F;schema&#x2F;beans&#x2F;spring-beans-4.2.xsd&quot;&gt;</span><br><span class="line">    </span><br><span class="line">&lt;&#x2F;beans&gt;</span><br></pre></td></tr></table></figure>
<p>注意，这里有一个@Controller，这个注解和@Service注解意思差不多，都表示该类是一个Spring Bean，这样就不需要再在Spring文件中为这个类定义Bean了。</p>
<p>另外，由于我前面在springmvc-servlet.xml中配置了prefix和suffix，因此return的时候就可以方便一些，不需要写前缀和后缀，Spring MVC默认会转发到（请求转发是Spring MVC默认的页面跳转方式）”/test.jsp”路径下。</p>
<p>最后，启动容器，访问”localhost:8080/SpringMVC/test”，容器就会把该请求转发到”localhost:8080/SpringMVC/test.jsp”页面下了。</p>
<h1 id="RequestMapping注解"><a href="#RequestMapping注解" class="headerlink" title="@RequestMapping注解"></a>@RequestMapping注解</h1><p>Spring MVC中最重要的注解应该就是@RequestMapping了，它是用于处理请求映射的。继续看刚才的TestController：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">@Controller</span><br><span class="line">@RequestMapping(value &#x3D; &quot;&#x2F;test&quot;)</span><br><span class="line">public class TestController</span><br><span class="line">&#123;</span><br><span class="line">    @RequestMapping</span><br><span class="line">    public String dispatchTest()</span><br><span class="line">    &#123;</span><br><span class="line">        System.out.println(&quot;Enter TestController.dispatchTest()&quot;);</span><br><span class="line">        return &quot;test&quot;;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>类上的RequestMapping是用于第一层匹配的。”localhost:8080/SpringMVC/test”和”localhost:8080/SpringMVC/add”，value是test，自然走的是前者。</p>
<p>接着看，比如我在TestController中又定义了三个方法，此时类上不使用RequestMapping注解：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">@RequestMapping(value &#x3D; &quot;&#x2F;add&quot;)</span><br><span class="line">public String dispatchAddTest()</span><br><span class="line">&#123;</span><br><span class="line">    System.out.println(&quot;Enter TestControll.dispatchAddTest()&quot;);</span><br><span class="line">    return &quot;test&quot;;</span><br><span class="line">&#125;</span><br><span class="line">    </span><br><span class="line">@RequestMapping(value &#x3D; &quot;&#x2F;add&#x2F;add&quot;)</span><br><span class="line">public String dispatchAddAddTest()</span><br><span class="line">&#123;</span><br><span class="line">    System.out.println(&quot;Enter TestControll.dispatchAddAddTest()&quot;);</span><br><span class="line">    return &quot;test&quot;;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">@RequestMapping(value &#x3D; &quot;&#x2F;del&quot;)</span><br><span class="line">public String dispatchDelTest()</span><br><span class="line">&#123;</span><br><span class="line">    System.out.println(&quot;Enter TestControll.dispatchDelTest()&quot;);</span><br><span class="line">    return &quot;test&quot;;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>那么这三个方法分别匹配的路径是：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&quot;localhost:8080&#x2F;SpringMVC&#x2F;test&#x2F;common&quot;</span><br><span class="line">&quot;localhost:8080&#x2F;SpringMVC&#x2F;test&#x2F;common&#x2F;&quot;</span><br><span class="line">&quot;localhost:8080&#x2F;SpringMVC&#x2F;test&#x2F;common.html&quot;</span><br><span class="line">&quot;localhost:8080&#x2F;SpringMVC&#x2F;test&#x2F;common.jsp&quot;</span><br><span class="line">&quot;localhost:8080&#x2F;SpringMVC&#x2F;test&#x2F;common.vm&quot;</span><br></pre></td></tr></table></figure>
<p>类似这种的路径，如果还想往”localhost:8080/SpringMVC/test/common/“再添加内容，那么root()这个方法就无法匹配到了，必须再添加方法。多说一句，”/“一直是一个容易弄混的东西，我自己试验的时候发现，RequestMapping里面的value属性，只要路径不存在多级的关系，加不加”/“是没有什么影响的。</p>
<h1 id="参数匹配"><a href="#参数匹配" class="headerlink" title="参数匹配"></a>参数匹配</h1><p>处理url也好、表单提交也好，参数匹配是非常重要的一个内容，万幸，Spring MVC对参数请求的支持做得非常好—-它会自动根据url或者表单中参数的名字和方法中同名形参进行匹配并赋值。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">@Controller</span><br><span class="line">@RequestMapping(value &#x3D; &quot;&#x2F;test&quot;)</span><br><span class="line">public class TestController</span><br><span class="line">&#123;</span><br><span class="line">    @RequestMapping</span><br><span class="line">    public String dispatchTest(String testId, String ttestId)</span><br><span class="line">    &#123;</span><br><span class="line">        System.out.println(&quot;Enter TestController.dispatchTest(), testId &#x3D; &quot; + testId + </span><br><span class="line">                &quot;, ttestId &#x3D; &quot; + ttestId);</span><br><span class="line">        return &quot;test&quot;;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>不仅如此，方法中还可以放入一个实体类对象：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">public class Test</span><br><span class="line">&#123;</span><br><span class="line">    private String tid;</span><br><span class="line">    private String nid;</span><br><span class="line">    private String bid;</span><br><span class="line">    </span><br><span class="line">    public void setTid(String tid)</span><br><span class="line">    &#123;</span><br><span class="line">        this.tid &#x3D; tid;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    public void setNid(String nid)</span><br><span class="line">    &#123;</span><br><span class="line">        this.nid &#x3D; nid;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    public void setBid(String bid)</span><br><span class="line">    &#123;</span><br><span class="line">        this.bid &#x3D; bid;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    public String toString()</span><br><span class="line">    &#123;</span><br><span class="line">        return &quot;tid &#x3D; &quot; + tid + &quot;, nid &#x3D; &quot; + nid + &quot;, bid &#x3D; &quot; + bid;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>注意，实体类对象中如果私有属性不打算对外提供，getter可以没有，但是为了Spring MVC可以将对应的属性根据属性名称进行匹配并赋值，setter必须要有。把TestController稍作改造，传入一个对象：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">@RequestMapping</span><br><span class="line">public String dispatchTest(Test test)</span><br><span class="line">&#123;</span><br><span class="line">    System.out.println(&quot;Enter TestController.dispatchTest(), test: &quot; + test);</span><br><span class="line">    return &quot;test&quot;;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>结论就是：</p>
<p>假如方法的参数是普通的字符串，只要字符串名字有和请求参数中的key完全匹配的，Spring MVC就会将完全匹配的自动赋值<br>假如方法的参数是实体类，只要实体类中的参数有和请求参数中的key完全匹配的，Spring MVC就会将完全匹配的自动赋值</p>
]]></content>
      <categories>
        <category>框架</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>框架</tag>
        <tag>spring</tag>
      </tags>
  </entry>
  <entry>
    <title>Spring框架IOC容器和AOP解析</title>
    <url>/%E6%A1%86%E6%9E%B6/Spring%E6%A1%86%E6%9E%B6IOC%E5%AE%B9%E5%99%A8%E5%92%8CAOP%E8%A7%A3%E6%9E%90/</url>
    <content><![CDATA[<h1 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h1><p>Spring 面向方面编程（AOP）和控制反转 （IOC） 容器。接着将使用几个示例演示 IOC 容器在典型应用程序用例场景中的应用情况。这些示例还将成为本系列后面部分进行的展开式讨论的基础，在本文的后面部分，将介绍 Spring 框架通过 Spring AOP 实现 AOP 构造的方式</p>
<a id="more"></a>
<h1 id="Spring下IOC容器和DI-依赖注入Dependency-injection"><a href="#Spring下IOC容器和DI-依赖注入Dependency-injection" class="headerlink" title="Spring下IOC容器和DI(依赖注入Dependency injection)"></a>Spring下IOC容器和DI(依赖注入Dependency injection)</h1><p>IOC容器：就是具有依赖注入功能的容器，是可以创建对象的容器，IOC容器负责实例化、定位、配置应用程序中的对象及建立这些对象间的依赖。通常new一个实例，控制权由程序员控制，而”控制反转”是指new实例工作不由程序员来做而是交给Spring容器来做。。在Spring中BeanFactory是IOC容器的实际代表者。</p>
<p>　　DI(依赖注入Dependency injection) ：在容器创建对象后，处理对象的依赖关系。</p>
<p>　　依赖注入spring的注入方式：</p>
<p>set注入方式<br>静态工厂注入方式<br>构造方法注入方式<br>基于注解的方式</p>
<h2 id="set注入方式"><a href="#set注入方式" class="headerlink" title="set注入方式"></a>set注入方式</h2><p>控制层代码：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">private OrderServiceImp orderService;</span><br><span class="line">    </span><br><span class="line">public void setOrderService(OrderServiceImp orderService) &#123;</span><br><span class="line">       this.orderService &#x3D; orderService;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>Spring配置XML文件：其中配置声明OrderAction类存在属性orderService。程式运行时候，会将已经实例化的orderService对象调用setOrderService方式注入。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;bean name&#x3D;&quot;orderAction&quot; class&#x3D;&quot;com.pec.action.OrderAction&quot;&gt;</span><br><span class="line">        &lt;property name&#x3D;&quot;orderService&quot; ref&#x3D;&quot;orderService&quot;&gt;&lt;&#x2F;property&gt;</span><br><span class="line">&lt;&#x2F;bean&gt;</span><br><span class="line">&lt;bean name&#x3D;&quot;orderService&quot; class&#x3D;&quot;com.pec.service.imp.OrderServiceImp&quot;&gt;&lt;&#x2F;bean&gt;</span><br></pre></td></tr></table></figure>
<p>上面程序中用到了一个重要的内部接口：Map.Entry，每个 Map.Entry 其实就是一个 key-value 对。从上面程序中可以看出：当系统决定存储 HashMap 中的 key-value 对时，仅仅只是根据 key 来计算并决定每个 Entry 的存储位置。我们完全可以把 Map 集合中的 value 当成 key 的附属，当系统决定了 key 的存储位置之后，value 随之保存在那里即可。</p>
<h2 id="构造器注入方式"><a href="#构造器注入方式" class="headerlink" title="构造器注入方式"></a>构造器注入方式</h2><p>控制层代码：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">private OrderServiceImp orderService;</span><br><span class="line">    </span><br><span class="line">public OrderAction(OrderServiceImp orderService) &#123;</span><br><span class="line">        this.orderService &#x3D; orderService;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>
<p>Spring配置XML文件</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;bean name&#x3D;&quot;orderAction&quot; class&#x3D;&quot;com.pec.action.OrderAction&quot;&gt;</span><br><span class="line">      &lt;constructor-arg ref&#x3D;&quot;orderService&quot;&gt;&lt;&#x2F;constructor-arg&gt;</span><br><span class="line">&lt;&#x2F;bean&gt;</span><br><span class="line">&lt;bean name&#x3D;&quot;orderService&quot; class&#x3D;&quot;com.pec.service.imp.OrderServiceImp&quot;&gt;&lt;&#x2F;bean&gt;</span><br></pre></td></tr></table></figure>

<h2 id="基于注解的方式"><a href="#基于注解的方式" class="headerlink" title="基于注解的方式"></a>基于注解的方式</h2><p>控制层代码：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">@Autowired   &#x2F;&#x2F;@Resource</span><br><span class="line">private OrderServiceImp orderService;</span><br></pre></td></tr></table></figure>
<p> 服务层代码：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">@Service(&quot;orderService&quot;)</span><br><span class="line">public class OrderServiceImp implements IOrderService &#123;</span><br><span class="line"></span><br><span class="line">    @Autowired</span><br><span class="line">    private JavaOrderMDaoImp javaOrderMDao;</span><br><span class="line"></span><br><span class="line">    @Autowired</span><br><span class="line">    private JavaOrderDDaoImp javaOrderDDao;</span><br><span class="line"></span><br><span class="line">    @Override</span><br><span class="line">    public List&lt;JavaOrderMList&gt; findOrderM(OrderSearch search) &#123;</span><br><span class="line">        return javaOrderMDao.findJavaOrderM(search);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    @Override</span><br><span class="line">    public List&lt;JavaOrderDList&gt; findOrderD(OrderSearch search) &#123;</span><br><span class="line">        return javaOrderDDao.findJavaOrderD(search);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p> DAO层代码：<br> <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">@Repository(&quot;javaOrderMDao&quot;)</span><br><span class="line">public class JavaOrderMDaoImp extends BaseHibernateDAO&lt;JavaOrderM, Serializable&gt; implements IJavaOrderMDao &#123;...&#125;</span><br></pre></td></tr></table></figure><br>注意点：</p>
<p>　　⑴ 持久层DAO层注解Repository中规定了名称，在Service层中声明名称必须一致。</p>
<p>　　⑵ 服务层Service层注解Service中规定了名称，在控制层中声明的名称必须一致。</p>
<p>　　⑶ 注解方式注入依赖注解：<br> <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">@Component         把对象加入ioc容器，对象引用名称是类名，第一个字母小写</span><br><span class="line">@Component(“name”) 把指定名称的对象，加入ioc容器</span><br><span class="line">@Repository        主要用于标识加入容器的对象是一个持久层的组件(类)</span><br><span class="line">@Service           主要用于标识加入容器的对象是一个业务逻辑层的组件</span><br><span class="line">@Controller        主要用于标识加入容器的对象是一个控制层的组件</span><br><span class="line">@Resource          注入属性(DI), 会从容器中找对象注入到@Resource修饰的对象上</span><br><span class="line">@Autowired         注入属性(DI), 会从容器中找对象注入到@Autowired修饰的对象上</span><br></pre></td></tr></table></figure><br> ⑷ 注解可以简化配置，提升开发效率，但是也不利于后期维护。</p>
<h1 id="Spring下面向切面编程-AOP-和事务管理配置"><a href="#Spring下面向切面编程-AOP-和事务管理配置" class="headerlink" title="Spring下面向切面编程(AOP)和事务管理配置"></a>Spring下面向切面编程(AOP)和事务管理配置</h1><p> AOP就是纵向的编程，如业务1和业务2都需要一个共同的操作，与其往每个业务中都添加同样的代码，不如写一遍代码，让两个业务共同使用这段代码。在日常有订单管理、商品管理、资金管理、库存管理等业务，都会需要到类似日志记录、事务控制、权限控制、性能统计、异常处理及事务处理等。AOP把所有共有代码全部抽取出来，放置到某个地方集中管理，然后在具体运行时，再由容器动态织入这些共有代码。</p>
<p> AOP涉及名称：</p>
<p>切面（Aspect）：其实就是共有功能的实现。如日志切面、权限切面、事务切面等。在实际应用中通常是一个存放共有功能实现的普通Java类，之所以能被AOP容器识别成切面，是在配置中指定的。</p>
<p>通知（Advice）：是切面的具体实现。以目标方法为参照点，根据放置的地方不同，可分为前置通知（Before）、后置通知（AfterReturning）、异常通知（AfterThrowing）、最终通知（After）与环绕通知（Around）5种。在实际应用中通常是切面类中的一个方法，具体属于哪类通知，同样是在配置中指定的。</p>
<p>连接点（Joinpoint）：就是程序在运行过程中能够插入切面的地点。例如，方法调用、异常抛出或字段修改等，但Spring只支持方法级的连接点。</p>
<p>切入点（Pointcut）：用于定义通知应该切入到哪些连接点上。不同的通知通常需要切入到不同的连接点上，这种精准的匹配是由切入点的正则表达式来定义的。</p>
<p>目标对象（Target）：就是那些即将切入切面的对象，也就是那些被通知的对象。这些对象中已经只剩下干干净净的核心业务逻辑代码了，所有的共有功能代码等待AOP容器的切入。</p>
<p>代理对象（Proxy）：将通知应用到目标对象之后被动态创建的对象。可以简单地理解为，代理对象的功能等于目标对象的核心业务逻辑功能加上共有功能。代理对象对于使用者而言是透明的，是程序运行过程中的产物。</p>
<p>织入（Weaving）：将切面应用到目标对象从而创建一个新的代理对象的过程。这个过程可以发生在编译期、类装载期及运行期，当然不同的发生点有着不同的前提条件。譬如发生在编译期的话，就要求有一个支持这种AOP实现的特殊编译器；发生在类装载期，就要求有一个支持AOP实现的特殊类装载器；只有发生在运行期，则可直接通过Java语言的反射机制与动态代理机制来动态实现。</p>
<p>　</p>
<p>　　Spring使用AOP配置事务管理由三个部分组成，分别是DataSource、TransactionManager和代理机制这三部分，无论哪种配置方式，一般变化的只是代理机制这部分。DataSource、TransactionManager这两部分只是会根据数据访问方式有所变化，比如使用hibernate进行数据访问时，DataSource实际为SessionFactory，TransactionManager的实现为HibernateTransactionManager。<br><img src="/images/springaop.png" alt="alt"><br>spring事务配置的五种方式：每个Bean都有一个代理、所有Bean共享一个代理基类、使用拦截器、使用tx标签配置的拦截器、全注解</p>
<h2 id="使用tx标签配置的拦截器"><a href="#使用tx标签配置的拦截器" class="headerlink" title="使用tx标签配置的拦截器"></a>使用tx标签配置的拦截器</h2> <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;!--4、配置hibernate属性 --&gt;</span><br><span class="line">    &lt;!--引入db.properties属性文件 --&gt;</span><br><span class="line">    &lt;bean class&#x3D;&quot;org.springframework.beans.factory.config.PropertyPlaceholderConfigurer&quot;&gt;</span><br><span class="line">    &lt;property name&#x3D;&quot;location&quot; value&#x3D;&quot;classpath:db.properties&quot;&gt;&lt;&#x2F;property&gt;</span><br><span class="line">    &lt;&#x2F;bean&gt;</span><br><span class="line">    &lt;!-- 配置数据源，连接池使用c3p0,详细信息参见hibernate官方文档&quot;基础配置章节&quot; --&gt;</span><br><span class="line">    &lt;bean id&#x3D;&quot;dataSource&quot; class&#x3D;&quot;com.mchange.v2.c3p0.ComboPooledDataSource&quot;</span><br><span class="line">        destroy-method&#x3D;&quot;close&quot; dependency-check&#x3D;&quot;none&quot;&gt;</span><br><span class="line">        &lt;property name&#x3D;&quot;driverClass&quot;&gt;</span><br><span class="line">            &lt;value&gt;$&#123;datasource.driverClassName&#125;&lt;&#x2F;value&gt;</span><br><span class="line">        &lt;&#x2F;property&gt;</span><br><span class="line">        &lt;property name&#x3D;&quot;jdbcUrl&quot;&gt;</span><br><span class="line">            &lt;value&gt;$&#123;datasource.url&#125;&lt;&#x2F;value&gt;</span><br><span class="line">        &lt;&#x2F;property&gt;</span><br><span class="line">        &lt;property name&#x3D;&quot;user&quot;&gt;</span><br><span class="line">            &lt;value&gt;$&#123;datasource.username&#125;&lt;&#x2F;value&gt;</span><br><span class="line">        &lt;&#x2F;property&gt;</span><br><span class="line">        &lt;property name&#x3D;&quot;password&quot;&gt;</span><br><span class="line">            &lt;value&gt;$&#123;datasource.password&#125;&lt;&#x2F;value&gt;</span><br><span class="line">        &lt;&#x2F;property&gt;</span><br><span class="line">        &lt;property name&#x3D;&quot;acquireIncrement&quot;&gt;</span><br><span class="line">            &lt;!--当连接池中的连接耗尽的时候c3p0一次同时获取的连接数。Default: 3 --&gt;</span><br><span class="line">            &lt;value&gt;$&#123;c3p0.acquireIncrement&#125;&lt;&#x2F;value&gt;</span><br><span class="line">        &lt;&#x2F;property&gt;</span><br><span class="line">        &lt;property name&#x3D;&quot;initialPoolSize&quot;&gt;</span><br><span class="line">            &lt;!--初始化时获取的连接数，取值应在minPoolSize与maxPoolSize之间。Default: 3 --&gt;</span><br><span class="line">            &lt;value&gt;$&#123;c3p0.initialPoolSize&#125;&lt;&#x2F;value&gt;</span><br><span class="line">        &lt;&#x2F;property&gt;</span><br><span class="line">        &lt;property name&#x3D;&quot;minPoolSize&quot;&gt;</span><br><span class="line">            &lt;!--连接池中保留的最小连接数。 --&gt;</span><br><span class="line">            &lt;value&gt;$&#123;c3p0.minPoolSize&#125;&lt;&#x2F;value&gt;</span><br><span class="line">        &lt;&#x2F;property&gt;</span><br><span class="line">        &lt;property name&#x3D;&quot;maxPoolSize&quot;&gt;</span><br><span class="line">            &lt;!--连接池中保留的最大连接数。Default: 15 --&gt;</span><br><span class="line">            &lt;value&gt;$&#123;c3p0.maxPoolSize&#125;&lt;&#x2F;value&gt;</span><br><span class="line">        &lt;&#x2F;property&gt;</span><br><span class="line">        &lt;property name&#x3D;&quot;maxIdleTime&quot;&gt;</span><br><span class="line">            &lt;!--最大空闲时间,60秒内未使用则连接被丢弃。若为0则永不丢弃。Default: 0 --&gt;</span><br><span class="line">            &lt;value&gt;$&#123;c3p0.maxIdleTime&#125;&lt;&#x2F;value&gt;</span><br><span class="line">        &lt;&#x2F;property&gt;</span><br><span class="line">        &lt;property name&#x3D;&quot;idleConnectionTestPeriod&quot;&gt;</span><br><span class="line">            &lt;!--每60秒检查所有连接池中的空闲连接。Default: 0 --&gt;</span><br><span class="line">            &lt;value&gt;$&#123;c3p0.idleConnectionTestPeriod&#125;&lt;&#x2F;value&gt;</span><br><span class="line">        &lt;&#x2F;property&gt;</span><br><span class="line">        &lt;property name&#x3D;&quot;maxStatements&quot;&gt;</span><br><span class="line">            &lt;!-- JDBC的标准参数，用以控制数据源内加载的PreparedStatements数量。但由于预缓存的statements 属于单个connection而不是整个连接池。所以设置这个参数需要考虑到多方面的因素。 </span><br><span class="line">                如果maxStatements与maxStatementsPerConnection均为0，则缓存被关闭。Default: 0 --&gt;</span><br><span class="line">            &lt;value&gt;$&#123;c3p0.maxStatements&#125;&lt;&#x2F;value&gt;</span><br><span class="line">        &lt;&#x2F;property&gt;</span><br><span class="line">        &lt;property name&#x3D;&quot;numHelperThreads&quot;&gt;</span><br><span class="line">            &lt;!-- C3P0是异步操作的，缓慢的JDBC操作通过帮助进程完成。扩展这些操作可以有效的提升性能， 通过多线程实现多个操作同时被执行。Default: </span><br><span class="line">                3 --&gt;</span><br><span class="line">            &lt;value&gt;$&#123;c3p0.numHelperThreads&#125;&lt;&#x2F;value&gt;</span><br><span class="line">        &lt;&#x2F;property&gt;</span><br><span class="line">    &lt;&#x2F;bean&gt;</span><br><span class="line"></span><br><span class="line">    &lt;!--配置 sessionFactory --&gt;</span><br><span class="line">    &lt;bean id&#x3D;&quot;sessionFactory&quot;</span><br><span class="line">        class&#x3D;&quot;org.springframework.orm.hibernate3.annotation.AnnotationSessionFactoryBean&quot;&gt;</span><br><span class="line">        &lt;property name&#x3D;&quot;dataSource&quot; ref&#x3D;&quot;dataSource&quot;&gt;</span><br><span class="line">        &lt;&#x2F;property&gt;</span><br><span class="line">        &lt;!-- hibernate的设置 --&gt;</span><br><span class="line">        &lt;property name&#x3D;&quot;hibernateProperties&quot;&gt;</span><br><span class="line">            &lt;props&gt;</span><br><span class="line">                &lt;prop key&#x3D;&quot;hibernate.dialect&quot;&gt;$&#123;hibernate.dialect&#125;&lt;&#x2F;prop&gt;</span><br><span class="line">                &lt;prop key&#x3D;&quot;hibernate.show_sql&quot;&gt; $&#123;hibernate.show_sql&#125; &lt;&#x2F;prop&gt;</span><br><span class="line">                &lt;prop key&#x3D;&quot;hibernate.jdbc.fetch_size&quot;&gt;$&#123;hibernate.jdbc.fetch_size&#125;&lt;&#x2F;prop&gt;</span><br><span class="line">                &lt;prop key&#x3D;&quot;hibernate.jdbc.batch_size&quot;&gt;$&#123;hibernate.jdbc.batch_size&#125;&lt;&#x2F;prop&gt;</span><br><span class="line">                &lt;prop key&#x3D;&quot;hibernate.connection.release_mode&quot;&gt;$&#123;hibernate.connection.release_mode&#125;&lt;&#x2F;prop&gt;</span><br><span class="line">                &lt;prop key&#x3D;&quot;hibernate.format_sql&quot;&gt;$&#123;hibernate.format_sql&#125;&lt;&#x2F;prop&gt;</span><br><span class="line">                &lt;prop key&#x3D;&quot;hibernate.connection.SetBigStringTryClob&quot;&gt;true&lt;&#x2F;prop&gt;</span><br><span class="line">            &lt;&#x2F;props&gt;</span><br><span class="line">        &lt;&#x2F;property&gt;</span><br><span class="line">        &lt;!-- anotation注解扫描实体类 --&gt;</span><br><span class="line">        &lt;property name&#x3D;&quot;packagesToScan&quot;&gt;</span><br><span class="line">            &lt;list&gt;</span><br><span class="line">                &lt;value&gt;com.pec.model&lt;&#x2F;value&gt;</span><br><span class="line">            &lt;&#x2F;list&gt;</span><br><span class="line">        &lt;&#x2F;property&gt;</span><br><span class="line">    &lt;&#x2F;bean&gt;</span><br><span class="line"></span><br><span class="line">    &lt;!--5、Spring 配置声明式事物 --&gt;</span><br><span class="line">    </span><br><span class="line">    &lt;!-- 配置事务 --&gt;</span><br><span class="line">    &lt;bean id&#x3D;&quot;transactionManager&quot;</span><br><span class="line">        class&#x3D;&quot;org.springframework.orm.hibernate3.HibernateTransactionManager&quot;&gt;</span><br><span class="line">        &lt;property name&#x3D;&quot;sessionFactory&quot; ref&#x3D;&quot;sessionFactory&quot;&gt;&lt;&#x2F;property&gt;</span><br><span class="line">    &lt;&#x2F;bean&gt;</span><br><span class="line">    </span><br><span class="line">    &lt;!-- 配置事务范围 --&gt;</span><br><span class="line">    &lt;tx:advice id&#x3D;&quot;txAdvice&quot; transaction-manager&#x3D;&quot;transactionManager&quot;&gt;</span><br><span class="line">        &lt;tx:attributes&gt;</span><br><span class="line">            &lt;tx:method name&#x3D;&quot;get*&quot; read-only&#x3D;&quot;false&quot; propagation&#x3D;&quot;NOT_SUPPORTED&quot; &#x2F;&gt;</span><br><span class="line">            &lt;tx:method name&#x3D;&quot;find*&quot; read-only&#x3D;&quot;false&quot; propagation&#x3D;&quot;NOT_SUPPORTED&quot; &#x2F;&gt;</span><br><span class="line">            &lt;tx:method name&#x3D;&quot;save*&quot; propagation&#x3D;&quot;REQUIRED&quot; &#x2F;&gt;</span><br><span class="line">            &lt;tx:method name&#x3D;&quot;update*&quot; propagation&#x3D;&quot;REQUIRED&quot; &#x2F;&gt;</span><br><span class="line">            &lt;tx:method name&#x3D;&quot;delete*&quot; propagation&#x3D;&quot;REQUIRED&quot; &#x2F;&gt;</span><br><span class="line">            &lt;tx:method name&#x3D;&quot;create*&quot; propagation&#x3D;&quot;REQUIRED&quot; &#x2F;&gt;</span><br><span class="line">            &lt;tx:method name&#x3D;&quot;anscy*&quot; propagation&#x3D;&quot;REQUIRED&quot; &#x2F;&gt;</span><br><span class="line">        &lt;&#x2F;tx:attributes&gt;</span><br><span class="line">    &lt;&#x2F;tx:advice&gt;</span><br><span class="line">    </span><br><span class="line">    &lt;!-- 定义切面 --&gt;</span><br><span class="line">    &lt;aop:config proxy-target-class&#x3D;&quot;true&quot;&gt;</span><br><span class="line">        &lt;aop:pointcut id&#x3D;&quot;pointcut&quot; expression&#x3D;&quot;execution(* com.pec.service..*.*(..))&quot;  &#x2F;&gt;</span><br><span class="line">        &lt;aop:advisor advice-ref&#x3D;&quot;txAdvice&quot; pointcut-ref&#x3D;&quot;pointcut&quot; &#x2F;&gt;</span><br><span class="line">    &lt;&#x2F;aop:config&gt;</span><br></pre></td></tr></table></figure>

<p> 有几点需要说明：</p>
<p>⑴ pointcut中的三个”*”中，第一个<em>代表返回值，第二</em>代表service下子包，第三个*代表方法名，“（..）”代表方法参数。</p>
<p>⑵ 此时配置的切点在Service层，方法命名需要按照以上advice通知点开头命名。</p>
<p>⑶ 按照规定命名方法，会受到Spring事务的管控，保持操作的一致性。例如向数据库插入100条数据，前面99条记录都正常执行直至第100条出现错误，则事务管控会回滚到执行前的初始状态。</p>
<h2 id="使用Bean代理"><a href="#使用Bean代理" class="headerlink" title="使用Bean代理"></a>使用Bean代理</h2> <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;?xml version&#x3D;&quot;1.0&quot; encoding&#x3D;&quot;UTF-8&quot;?&gt;</span><br><span class="line">&lt;beans xmlns&#x3D;&quot;http:&#x2F;&#x2F;www.springframework.org&#x2F;schema&#x2F;beans&quot;</span><br><span class="line">    xmlns:xsi&#x3D;&quot;http:&#x2F;&#x2F;www.w3.org&#x2F;2001&#x2F;XMLSchema-instance&quot;</span><br><span class="line">    xmlns:tx&#x3D;&quot;http:&#x2F;&#x2F;www.springframework.org&#x2F;schema&#x2F;tx&quot;</span><br><span class="line">    xmlns:aop&#x3D;&quot;http:&#x2F;&#x2F;www.springframework.org&#x2F;schema&#x2F;aop&quot;</span><br><span class="line">    xmlns:context&#x3D;&quot;http:&#x2F;&#x2F;www.springframework.org&#x2F;schema&#x2F;context&quot;</span><br><span class="line">    xmlns:mvc&#x3D;&quot;http:&#x2F;&#x2F;www.springframework.org&#x2F;schema&#x2F;mvc&quot;</span><br><span class="line">    xsi:schemaLocation&#x3D;&quot;http:&#x2F;&#x2F;www.springframework.org&#x2F;schema&#x2F;beans</span><br><span class="line">      http:&#x2F;&#x2F;www.springframework.org&#x2F;schema&#x2F;beans&#x2F;spring-beans-4.0.xsd</span><br><span class="line">      http:&#x2F;&#x2F;www.springframework.org&#x2F;schema&#x2F;context</span><br><span class="line">      http:&#x2F;&#x2F;www.springframework.org&#x2F;schema&#x2F;context&#x2F;spring-context.xsd</span><br><span class="line">      http:&#x2F;&#x2F;www.springframework.org&#x2F;schema&#x2F;tx </span><br><span class="line">      http:&#x2F;&#x2F;www.springframework.org&#x2F;schema&#x2F;tx&#x2F;spring-tx-2.5.xsd</span><br><span class="line">      http:&#x2F;&#x2F;www.springframework.org&#x2F;schema&#x2F;aop </span><br><span class="line">      http:&#x2F;&#x2F;www.springframework.org&#x2F;schema&#x2F;aop&#x2F;spring-aop-2.5.xsd&quot;</span><br><span class="line">      default-autowire&#x3D;&quot;default&quot;&gt;</span><br><span class="line">    </span><br><span class="line">    &lt;!-- &lt;bean name&#x3D;&quot;userManager&quot; class&#x3D;&quot;com.tgb.manager.UserManagerImpl&quot;&gt;&lt;&#x2F;bean&gt;</span><br><span class="line">    &lt;bean name&#x3D;&quot;userController&quot; class&#x3D;&quot;com.tgb.web.UserController&quot;&gt;</span><br><span class="line">        &lt;property name&#x3D;&quot;userManager&quot; ref&#x3D;&quot;userManager&quot;&gt;&lt;&#x2F;property&gt;</span><br><span class="line">    &lt;&#x2F;bean&gt; --&gt;</span><br><span class="line">    &lt;context:component-scan base-package&#x3D;&quot;com.tgb.dao&quot; &#x2F;&gt;</span><br><span class="line">    &lt;context:component-scan base-package&#x3D;&quot;com.tgb.entity&quot; &#x2F;&gt;</span><br><span class="line">    &lt;context:component-scan base-package&#x3D;&quot;com.tgb.manager&quot; &#x2F;&gt;</span><br><span class="line">    &lt;context:component-scan base-package&#x3D;&quot;com.tgb.web&quot; &#x2F;&gt;</span><br><span class="line">    </span><br><span class="line">    &lt;!-- 引入init.properties中属性 --&gt;</span><br><span class="line">    &lt;bean id&#x3D;&quot;placeholderConfig&quot; class&#x3D;&quot;org.springframework.beans.factory.config.PropertyPlaceholderConfigurer&quot;&gt;</span><br><span class="line">        &lt;property name&#x3D;&quot;locations&quot;&gt;</span><br><span class="line">        &lt;list&gt;</span><br><span class="line">            &lt;value&gt;classpath:config&#x2F;spring&#x2F;jdbc.properties&lt;&#x2F;value&gt;</span><br><span class="line">        &lt;&#x2F;list&gt;</span><br><span class="line">        &lt;&#x2F;property&gt;</span><br><span class="line">    &lt;&#x2F;bean&gt;</span><br><span class="line">    </span><br><span class="line">    &lt;!-- 配置数据源，连接池使用c3p0,详细信息参见hibernate官方文档&quot;基础配置章节&quot; --&gt;</span><br><span class="line">    &lt;bean id&#x3D;&quot;dataSource&quot; class&#x3D;&quot;com.mchange.v2.c3p0.ComboPooledDataSource&quot; &gt;</span><br><span class="line">        &lt;property name&#x3D;&quot;driverClass&quot; value&#x3D;&quot;$&#123;datasource.driverClassName&#125;&quot;&gt;&lt;&#x2F;property&gt;</span><br><span class="line">        &lt;property name&#x3D;&quot;jdbcUrl&quot; value&#x3D;&quot;$&#123;datasource.url&#125;&quot;&gt;&lt;&#x2F;property&gt;</span><br><span class="line">        &lt;property name&#x3D;&quot;user&quot; value&#x3D;&quot;$&#123;datasource.username&#125;&quot;&gt;&lt;&#x2F;property&gt;</span><br><span class="line">        &lt;property name&#x3D;&quot;password&quot; value&#x3D;&quot;$&#123;datasource.password&#125;&quot;&gt;&lt;&#x2F;property&gt;</span><br><span class="line">    &lt;&#x2F;bean&gt;</span><br><span class="line">    </span><br><span class="line">    &lt;!-- 配置SessionFactory --&gt;</span><br><span class="line">    &lt;bean id&#x3D;&quot;sessionFactory&quot; class&#x3D;&quot;org.springframework.orm.hibernate4.LocalSessionFactoryBean&quot;&gt;</span><br><span class="line">        &lt;property name&#x3D;&quot;dataSource&quot; ref&#x3D;&quot;dataSource&quot; &#x2F;&gt;</span><br><span class="line">        &lt;property name&#x3D;&quot;hibernateProperties&quot;&gt;</span><br><span class="line">            &lt;props&gt;</span><br><span class="line">                &lt;prop key&#x3D;&quot;hibernate.dialect&quot;&gt;</span><br><span class="line">                    $&#123;hibernate.dialect&#125;</span><br><span class="line">                &lt;&#x2F;prop&gt;</span><br><span class="line">                &lt;prop key&#x3D;&quot;hibernate.show_sql&quot;&gt; </span><br><span class="line">                    $&#123;hibernate.show_sql&#125; </span><br><span class="line">                &lt;&#x2F;prop&gt;</span><br><span class="line">                &lt;prop key&#x3D;&quot;hibernate.jdbc.fetch_size&quot;&gt;</span><br><span class="line">                    $&#123;hibernate.jdbc.fetch_size&#125;</span><br><span class="line">                &lt;&#x2F;prop&gt;</span><br><span class="line">                &lt;prop key&#x3D;&quot;hibernate.jdbc.batch_size&quot;&gt;</span><br><span class="line">                    $&#123;hibernate.jdbc.batch_size&#125;</span><br><span class="line">                &lt;&#x2F;prop&gt;</span><br><span class="line">                &lt;prop key&#x3D;&quot;hibernate.connection.release_mode&quot;&gt;</span><br><span class="line">                    $&#123;hibernate.connection.release_mode&#125;</span><br><span class="line">                &lt;&#x2F;prop&gt;</span><br><span class="line">                &lt;prop key&#x3D;&quot;hibernate.format_sql&quot;&gt;</span><br><span class="line">                    $&#123;hibernate.format_sql&#125;</span><br><span class="line">                &lt;&#x2F;prop&gt;</span><br><span class="line">                &lt;prop key&#x3D;&quot;hibernate.connection.SetBigStringTryClob&quot;&gt;true&lt;&#x2F;prop&gt;</span><br><span class="line">            &lt;&#x2F;props&gt;</span><br><span class="line">        &lt;&#x2F;property&gt;</span><br><span class="line">        &lt;!-- anotation注解扫描实体类  --&gt;</span><br><span class="line">        &lt;property name&#x3D;&quot;annotatedClasses&quot;&gt;</span><br><span class="line">            &lt;list&gt;</span><br><span class="line">                &lt;value&gt;com.tgb.entity.User&lt;&#x2F;value&gt;</span><br><span class="line">            &lt;&#x2F;list&gt;</span><br><span class="line">        &lt;&#x2F;property&gt;</span><br><span class="line">    &lt;&#x2F;bean&gt;</span><br><span class="line"></span><br><span class="line">    &lt;!-- 配置一个事务管理器 将事务与Hibernate关联--&gt;</span><br><span class="line">    &lt;bean id&#x3D;&quot;transactionManager&quot; class&#x3D;&quot;org.springframework.orm.hibernate4.HibernateTransactionManager&quot;&gt;</span><br><span class="line">        &lt;property name&#x3D;&quot;sessionFactory&quot; ref&#x3D;&quot;sessionFactory&quot;&#x2F;&gt;</span><br><span class="line">    &lt;&#x2F;bean&gt;</span><br><span class="line">    </span><br><span class="line">    &lt;!-- 配置事务范围，使用代理的方式 --&gt;</span><br><span class="line">    &lt;bean id&#x3D;&quot;transactionProxy&quot; class&#x3D;&quot;org.springframework.transaction.interceptor.TransactionProxyFactoryBean&quot; abstract&#x3D;&quot;true&quot;&gt;  </span><br><span class="line">        &lt;!--   为事务代理bean注入事务管理器--&gt; </span><br><span class="line">        &lt;property name&#x3D;&quot;transactionManager&quot; &gt;</span><br><span class="line">            &lt;ref bean&#x3D;&quot;transactionManager&quot;&#x2F;&gt;</span><br><span class="line">        &lt;&#x2F;property&gt;  </span><br><span class="line">         &lt;!--设置事务属性范围--&gt;  </span><br><span class="line">        &lt;property name&#x3D;&quot;transactionAttributes&quot;&gt;  </span><br><span class="line">            &lt;props&gt;  </span><br><span class="line">                &lt;prop key&#x3D;&quot;add*&quot;&gt;PROPAGATION_REQUIRED,-Exception&lt;&#x2F;prop&gt;  </span><br><span class="line">                &lt;prop key&#x3D;&quot;get&quot;&gt;PROPAGATION_REQUIRED,-Exception&lt;&#x2F;prop&gt; </span><br><span class="line">                &lt;prop key&#x3D;&quot;update*&quot;&gt;PROPAGATION_REQUIRED,-myException&lt;&#x2F;prop&gt;  </span><br><span class="line">                &lt;prop key&#x3D;&quot;del*&quot;&gt;PROPAGATION_REQUIRED&lt;&#x2F;prop&gt;  </span><br><span class="line">                &lt;prop key&#x3D;&quot;*&quot;&gt;PROPAGATION_REQUIRED&lt;&#x2F;prop&gt;      </span><br><span class="line">            &lt;&#x2F;props&gt;  </span><br><span class="line">        &lt;&#x2F;property&gt;  </span><br><span class="line">    &lt;&#x2F;bean&gt; </span><br><span class="line">    </span><br><span class="line">    &lt;bean id&#x3D;&quot;userDao&quot; class&#x3D;&quot;com.tgb.dao.UserDaoImpl&quot;&gt;</span><br><span class="line">        &lt;property name&#x3D;&quot;sessionFactory&quot; ref&#x3D;&quot;sessionFactory&quot;&gt;&lt;&#x2F;property&gt;</span><br><span class="line">    &lt;&#x2F;bean&gt;</span><br><span class="line"></span><br><span class="line">    &lt;bean id&#x3D;&quot;userManagerBase&quot; class&#x3D;&quot;com.tgb.manager.UserManagerImpl&quot;&gt;</span><br><span class="line">        &lt;property name&#x3D;&quot;userDao&quot; ref&#x3D;&quot;userDao&quot;&gt;&lt;&#x2F;property&gt;</span><br><span class="line">    &lt;&#x2F;bean&gt;</span><br><span class="line">    </span><br><span class="line">    &lt;!-- 此处为代理 --&gt;</span><br><span class="line">    &lt;bean name&#x3D;&quot;userManager&quot; parent&#x3D;&quot;transactionProxy&quot;&gt;</span><br><span class="line">        &lt;property name&#x3D;&quot;target&quot; ref&#x3D;&quot;userManagerBase&quot;&gt;&lt;&#x2F;property&gt;</span><br><span class="line">    &lt;&#x2F;bean&gt;    </span><br><span class="line">     </span><br><span class="line">&lt;&#x2F;beans&gt;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>框架</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>框架</tag>
        <tag>spring</tag>
      </tags>
  </entry>
  <entry>
    <title>Java GC机制（下）</title>
    <url>/Java/Java%20GC%E6%9C%BA%E5%88%B6%EF%BC%88%E4%B8%8B%EF%BC%89/</url>
    <content><![CDATA[<h1 id="垃圾收集算法"><a href="#垃圾收集算法" class="headerlink" title="垃圾收集算法"></a>垃圾收集算法</h1><p>1、标记-清除（Mark-Sweep）算法</p>
<p>这是最基础的算法，标记-清除算法就如同它的名字样，分为“标记”和“清除”两个阶段：首先标记出所有需要回收的对象，标记完成后统一回收所有被标记的对象。这种算法的不足主要体现在效率和空间，从效率的角度讲，标记和清除两个过程的效率都不高；从空间的角度讲，标记清除后会产生大量不连续的内存碎片， 内存碎片太多可能会导致以后程序运行过程中在需要分配较大对象时，无法找到足够的连续内存而不得不提前触发一次垃圾收集动作。标记-清除算法执行过程如图：</p>
<a id="more"></a>
<p><img src="/images/marksw.png" alt="alt"></p>
<p>2、复制（Copying）算法</p>
<p>复制算法是为了解决效率问题而出现的，它将可用的内存分为两块，每次只用其中一块，当这一块内存用完了，就将还存活着的对象复制到另外一块上面，然后再把已经使用过的内存空间一次性清理掉。这样每次只需要对整个半区进行内存回收，内存分配时也不需要考虑内存碎片等复杂情况，只需要移动指针，按照顺序分配即可。复制算法的执行过程如图：<br><img src="/images/copying.png" alt="alt"></p>
<p>不过这种算法有个缺点，内存缩小为了原来的一半，这样代价太高了。现在的商用虚拟机都采用这种算法来回收新生代，不过研究表明1:1的比例非常不科学，因此新生代的内存被划分为一块较大的Eden空间和两块较小的Survivor空间，每次使用Eden和其中一块Survivor。每次回收时，将Eden和Survivor中还存活着的对象一次性复制到另外一块Survivor空间上，最后清理掉Eden和刚才用过的Survivor空间。HotSpot虚拟机默认Eden区和Survivor区的比例为8:1，意思是每次新生代中可用内存空间为整个新生代容量的90%。当然，我们没有办法保证每次回收都只有不多于10%的对象存活，当Survivor空间不够用时，需要依赖老年代进行分配担保（Handle Promotion）。</p>
<p>3、标记-整理（Mark-Compact）算法</p>
<p>复制算法在对象存活率较高的场景下要进行大量的复制操作，效率很低。万一对象100%存活，那么需要有额外的空间进行分配担保。老年代都是不易被回收的对象，对象存活率高，因此一般不能直接选用复制算法。根据老年代的特点，有人提出了另外一种标记-整理算法，过程与标记-清除算法一样，不过不是直接对可回收对象进行清理，而是让所有存活对象都向一端移动，然后直接清理掉边界以外的内存。标记-整理算法的工作过程如图：<br><img src="/images/markcom.png" alt="alt"></p>
<p>4、分代收集算法<br>根据上面的内容，用一张图概括一下堆内存的布局<br><img src="/images/gcsf.png" alt="alt"><br>现代商用虚拟机基本都采用分代收集算法来进行垃圾回收。这种算法没什么特别的，无非是上面内容的结合罢了，根据对象的生命周期的不同将内存划分为几块，然后根据各块的特点采用最适当的收集算法。大批对象死去、少量对象存活的（新生代），使用复制算法，复制成本低；对象存活率高、没有额外空间进行分配担保的（老年代），采用标记-清理算法或者标记-整理算法。</p>
<h1 id="垃圾收集器"><a href="#垃圾收集器" class="headerlink" title="垃圾收集器"></a>垃圾收集器</h1><p>垃圾收集器就是上面讲的理论知识的具体实现了。不同虚拟机所提供的垃圾收集器可能会有很大差别，我们使用的是HotSpot，HotSpot这个虚拟机所包含的所有收集器如图：<br><img src="/images/sjq.png" alt="alt"><br>上图展示了7种作用于不同分代的收集器，如果两个收集器之间存在连线，那说明它们可以搭配使用。虚拟机所处的区域说明它是属于新生代收集器还是老年代收集器。多说一句，我们必须明确一个观点：没有最好的垃圾收集器，更加没有万能的收集器，只能选择对具体应用最合适的收集器。这也是HotSpot为什么要实现这么多收集器的原因。OK，下面一个一个看一下收集器。</p>
<p>1、Serial收集器</p>
<pre><code>最基本、发展历史最久的收集器，这个收集器是一个采用复制算法的单线程的收集器，单线程一方面意味着它只会使用一个CPU或一条线程去完成垃圾收集工作，另一方面也意味着它进行垃圾收集时必须暂停其他线程的所有工作，直到它收集结束为止。后者意味着，在用户不可见的情况下要把用户正常工作的线程全部停掉，这对很多应用是难以接受的。不过实际上到目前为止，Serial收集器依然是虚拟机运行在Client模式下的默认新生代收集器，因为它简单而高效。用户桌面应用场景中，分配给虚拟机管理的内存一般来说不会很大，收集几十兆甚至一两百兆的新生代停顿时间在几十毫秒最多一百毫秒，只要不是频繁发生，这点停顿是完全可以接受的。Serial收集器运行过程如下图所示：</code></pre><p><img src="/images/sjq2.png" alt="alt"><br>2、ParNew收集器</p>
<pre><code>ParNew收集器其实就是Serial收集器的多线程版本，除了使用多条线程进行垃圾收集外，其余行为和Serial收集器完全一样，包括使用的也是复制算法。ParNew收集器除了多线程以外和Serial收集器并没有太多创新的地方，但是它却是Server模式下的虚拟机首选的新生代收集器，其中有一个很重要的和性能无关的原因是，除了Serial收集器外，目前只有它能与CMS收集器配合工作（看图）。CMS收集器是一款几乎可以认为有划时代意义的垃圾收集器，因为它第一次实现了让垃圾收集线程与用户线程基本上同时工作。ParNew收集器在单CPU的环境中绝对不会有比Serial收集器更好的效果，甚至由于线程交互的开销，该收集器在两个CPU的环境中都不能百分之百保证可以超越Serial收集器。当然，随着可用CPU数量的增加，它对于GC时系统资源的有效利用还是很有好处的。它默认开启的收集线程数与CPU数量相同，在CPU数量非常多的情况下，可以使用-XX:ParallelGCThreads参数来限制垃圾收集的线程数。</code></pre><p>3、Parallel Scavenge收集器</p>
<pre><code>Parallel Scavenge收集器也是一个新生代收集器，也是用复制算法的收集器，也是并行的多线程收集器，但是它的特点是它的关注点和其他收集器不同。介绍这个收集器主要还是介绍吞吐量的概念。CMS等收集器的关注点是尽可能缩短垃圾收集时用户线程的停顿时间，而Parallel Scavenge收集器的目标则是打到一个可控制的吞吐量。所谓吞吐量的意思就是CPU用于运行用户代码时间与CPU总消耗时间的比值，即吞吐量=运行用户代码时间/（运行用户代码时间+垃圾收集时间），虚拟机总运行100分钟，垃圾收集1分钟，那吞吐量就是99%。另外，Parallel Scavenge收集器是虚拟机运行在Server模式下的默认垃圾收集器。

停顿时间短适合需要与用户交互的程序，良好的响应速度能提升用户体验；高吞吐量则可以高效率利用CPU时间，尽快完成运算任务，主要适合在后台运算而不需要太多交互的任务。

虚拟机提供了-XX:MaxGCPauseMillis和-XX:GCTimeRatio两个参数来精确控制最大垃圾收集停顿时间和吞吐量大小。不过不要以为前者越小越好，GC停顿时间的缩短是以牺牲吞吐量和新生代空间换取的。由于与吞吐量关系密切，Parallel Scavenge收集器也被称为“吞吐量优先收集器”。Parallel Scavenge收集器有一个-XX:+UseAdaptiveSizePolicy参数，这是一个开关参数，这个参数打开之后，就不需要手动指定新生代大小、Eden区和Survivor参数等细节参数了，虚拟机会根据当前系统的运行情况手机性能监控信息，动态调整这些参数以提供最合适的停顿时间或者最大的吞吐量。如果对于垃圾收集器运作原理不太了解，以至于在优化比较困难的时候，使用Parallel Scavenge收集器配合自适应调节策略，把内存管理的调优任务交给虚拟机去完成将是一个不错的选择。</code></pre><p>4、Serial Old收集器</p>
<p>Serial收集器的老年代版本，同样是一个单线程收集器，使用“标记-整理算法”，这个收集器的主要意义也是在于给Client模式下的虚拟机使用。</p>
<p>5、Parallel Old收集器</p>
<p>Parallel Scavenge收集器的老年代版本，使用多线程和“标记-整理”算法。这个收集器在JDK 1.6之后的出现，“吞吐量优先收集器”终于有了比较名副其实的应用组合，在注重吞吐量以及CPU资源敏感的场合，都可以优先考虑Parallel Scavenge收集器+Parallel Old收集器的组合。</p>
<p>6、CMS收集器</p>
<p>CMS（Conrrurent Mark Sweep）收集器是以获取最短回收停顿时间为目标的收集器。使用标记 - 清除算法，收集过程分为如下四步：</p>
<p>(1). 初始标记，标记GCRoots能直接关联到的对象，时间很短。</p>
<p>(2). 并发标记，进行GCRoots Tracing（可达性分析）过程，时间很长。</p>
<p>(3). 重新标记，修正并发标记期间因用户程序继续运作而导致标记产生变动的那一部分对象的标记记录，时间较长。</p>
<p>(4). 并发清除，回收内存空间，时间很长。<br>说明：1. 对CPU资源非常敏感，可能会导致应用程序变慢，吞吐率下降。2. 无法处理浮动垃圾，因为在并发清理阶段用户线程还在运行，自然就会产生新的垃圾，而在此次收集中无法收集他们，只能留到下次收集，这部分垃圾为浮动垃圾，同时，由于用户线程并发执行，所以需要预留一部分老年代空间提供并发收集时程序运行使用。3. 由于采用的标记 - 清除算法，会产生大量的内存碎片，不利于大对象的分配，可能会提前触发一次Full GC。虚拟机提供了-XX:+UseCMSCompactAtFullCollection参数来进行碎片的合并整理过程，这样会使得停顿时间变长，虚拟机还提供了一个参数配置，-XX:+CMSFullGCsBeforeCompaction，用于设置执行多少次不压缩的Full GC后，接着来一次带压缩的GC。<br>7、G1收集器</p>
<p>G1是目前技术发展的最前沿成果之一，HotSpot开发团队赋予它的使命是未来可以替换掉JDK1.5中发布的CMS收集器。与其他GC收集器相比，G1收集器有以下特点：</p>
<p>(1). 并行和并发。使用多个CPU来缩短Stop The World停顿时间，与用户线程并发执行。</p>
<p>(2). 分代收集。独立管理整个堆，但是能够采用不同的方式去处理新创建对象和已经存活了一段时间、熬过多次GC的旧对象，以获取更好的收集效果。</p>
<p>(3). 空间整合。基于标记 - 整理算法，无内存碎片产生。</p>
<p>(4). 可预测的停顿。能简历可预测的停顿时间模型，能让使用者明确指定在一个长度为M毫秒的时间片段内，消耗在垃圾收集上的时间不得超过N毫秒。</p>
<pre><code>在G1之前的垃圾收集器，收集的范围都是整个新生代或者老年代，而G1不再是这样。使用G1收集器时，Java堆的内存布局与其他收集器有很大差别，它将整个Java堆划分为多个大小相等的独立区域（Region），虽然还保留有新生代和老年代的概念，但新生代和老年代不再是物理隔离的了，它们都是一部分（可以不连续）Region的集合。</code></pre><p>8、常用的收集器组合</p>
<h1 id="理解GC日志"><a href="#理解GC日志" class="headerlink" title="理解GC日志"></a>理解GC日志</h1><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">[GC [DefNew: 310K-&gt;194K(2368K), 0.0269163 secs] 310K-&gt;194K(7680K), 0.0269513 secs] [Times: user&#x3D;0.00 sys&#x3D;0.00, real&#x3D;0.03 secs] </span><br><span class="line">[GC [DefNew: 2242K-&gt;0K(2368K), 0.0018814 secs] 2242K-&gt;2241K(7680K), 0.0019172 secs] [Times: user&#x3D;0.00 sys&#x3D;0.00, real&#x3D;0.00 secs] </span><br><span class="line">[Full GC (System) [Tenured: 2241K-&gt;193K(5312K), 0.0056517 secs] 4289K-&gt;193K(7680K), [Perm : 2950K-&gt;2950K(21248K)], 0.0057094 secs] [Times: user&#x3D;0.00 sys&#x3D;0.00, real&#x3D;0.00 secs] </span><br><span class="line">Heap</span><br><span class="line"> def new generation   total 2432K, used 43K [0x00000000052a0000, 0x0000000005540000, 0x0000000006ea0000)</span><br><span class="line">  eden space 2176K,   2% used [0x00000000052a0000, 0x00000000052aaeb8, 0x00000000054c0000)</span><br><span class="line">  from space 256K,   0% used [0x00000000054c0000, 0x00000000054c0000, 0x0000000005500000)</span><br><span class="line">  to   space 256K,   0% used [0x0000000005500000, 0x0000000005500000, 0x0000000005540000)</span><br><span class="line"> tenured generation   total 5312K, used 193K [0x0000000006ea0000, 0x00000000073d0000, 0x000000000a6a0000)</span><br><span class="line">   the space 5312K,   3% used [0x0000000006ea0000, 0x0000000006ed0730, 0x0000000006ed0800, 0x00000000073d0000)</span><br><span class="line"> compacting perm gen  total 21248K, used 2982K [0x000000000a6a0000, 0x000000000bb60000, 0x000000000faa0000)</span><br><span class="line">   the space 21248K,  14% used [0x000000000a6a0000, 0x000000000a989980, 0x000000000a989a00, 0x000000000bb60000)</span><br><span class="line">No shared spaces configured.</span><br></pre></td></tr></table></figure>
<p>1、日志的开头“GC”、“Full GC”表示这次垃圾收集的停顿类型，而不是用来区分新生代GC还是老年代GC的。如果有Full，则说明本次GC停止了其他所有工作线程(Stop-The-World)。看到Full GC的写法是“Full GC(System)”，这说明是调用System.gc()方法所触发的GC。</p>
<p>2、“GC”中接下来的“[DefNew”表示GC发生的区域，这里显示的区域名称与使用的GC收集器是密切相关的，例如上面样例所使用的Serial收集器中的新生代名为“Default New Generation”，所以显示的是“[DefNew”。如果是ParNew收集器，新生代名称就会变为“[ParNew”，意为“Parallel New Generation”。如果采用Parallel Scavenge收集器，那它配套的新生代称为“PSYoungGen”，老年代和永久代同理，名称也是由收集器决定的。</p>
<p>3、后面方括号内部的“310K-&gt;194K(2368K)”、“2242K-&gt;0K(2368K)”，指的是该区域已使用的容量-&gt;GC后该内存区域已使用的容量(该内存区总容量)。方括号外面的“310K-&gt;194K(7680K)”、“2242K-&gt;2241K(7680K)”则指的是GC前Java堆已使用的容量-&gt;GC后Java堆已使用的容量(Java堆总容量)。</p>
<p>4、再往后“0.0269163 secs”表示该内存区域GC所占用的时间，单位是秒。最后的“[Times: user=0.00 sys=0.00 real=0.03 secs]”则更具体了，user表示用户态消耗的CPU时间、内核态消耗的CPU时间、操作从开始到结束经过的墙钟时间。后面两个的区别是，墙钟时间包括各种非运算的等待消耗，比如等待磁盘I/O、等待线程阻塞，而CPU时间不包括这些耗时，但当系统有多CPU或者多核的话，多线程操作会叠加这些CPU时间，所以如果看到user或sys时间超过real时间是完全正常的。</p>
<p>5、“Heap”后面就列举出堆内存目前各个年代的区域的内存情况。</p>
]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>java</tag>
      </tags>
  </entry>
  <entry>
    <title>Java GC机制（上）</title>
    <url>/Java/Java%20GC%E6%9C%BA%E5%88%B6%EF%BC%88%E4%B8%8A%EF%BC%89/</url>
    <content><![CDATA[<h1 id="垃圾回收的意义"><a href="#垃圾回收的意义" class="headerlink" title="垃圾回收的意义"></a>垃圾回收的意义</h1><p>如果不进行垃圾回收，内存迟早都会被消耗空，因为我们在不断的分配内存空间而不进行回收。除非内存无限大，我们可以任性的分配而不回收，但是事实并非如此。所以，垃圾回收是必须的。</p>
<a id="more"></a>
<h1 id="垃圾回收对象"><a href="#垃圾回收对象" class="headerlink" title="垃圾回收对象"></a>垃圾回收对象</h1><p>哪些内存需要回收是垃圾回收机制第一个要考虑的问题，所谓“要回收的垃圾”无非就是那些不可能再被任何途径使用的对象。那么如何找到这些对象？</p>
<p>1、引用计数法</p>
<p>这个算法的实现是，给对象中添加一个引用计数器，每当一个地方引用这个对象时，计数器值+1；当引用失效时，计数器值-1。任何时刻计数值为0的对象就是不可能再被使用的。这种算法使用场景很多，但是，Java中却没有使用这种算法，因为这种算法很难解决对象之间相互引用的情况。看一段代码：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&#x2F;**</span><br><span class="line"> * 虚拟机参数：-verbose:gc</span><br><span class="line"> *&#x2F;</span><br><span class="line">public class ReferenceCountingGC</span><br><span class="line">&#123;</span><br><span class="line">    private Object instance &#x3D; null;</span><br><span class="line">    private static final int _1MB &#x3D; 1024 * 1024;</span><br><span class="line">    </span><br><span class="line">    &#x2F;** 这个成员属性唯一的作用就是占用一点内存 *&#x2F;</span><br><span class="line">    private byte[] bigSize &#x3D; new byte[2 * _1MB];</span><br><span class="line">    </span><br><span class="line">    public static void main(String[] args)</span><br><span class="line">    &#123;</span><br><span class="line">        ReferenceCountingGC objectA &#x3D; new ReferenceCountingGC();</span><br><span class="line">        ReferenceCountingGC objectB &#x3D; new ReferenceCountingGC();</span><br><span class="line">        objectA.instance &#x3D; objectB;</span><br><span class="line">        objectB.instance &#x3D; objectA;</span><br><span class="line">        objectA &#x3D; null;</span><br><span class="line">        objectB &#x3D; null;</span><br><span class="line">        </span><br><span class="line">        System.gc();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>看下运行结果：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">[GC 4417K-&gt;288K(61440K), 0.0013498 secs]</span><br><span class="line">[Full GC 288K-&gt;194K(61440K), 0.0094790 secs]</span><br></pre></td></tr></table></figure>
<p>看到，两个对象相互引用着，但是虚拟机还是把这两个对象回收掉了，这也说明虚拟机并不是通过引用计数法来判定对象是否存活的。</p>
<p>2、可达性分析法</p>
<p>这个算法的基本思想是通过一系列称为“GC Roots”的对象作为起始点，从这些节点向下搜索，搜索所走过的路径称为引用链，当一个对象到GC Roots没有任何引用链（即GC Roots到对象不可达）时，则证明此对象是不可用的。</p>
<p>那么问题又来了，如何选取GCRoots对象呢？在Java语言中，可以作为GCRoots的对象包括下面几种：</p>
<p>(1). 虚拟机栈（栈帧中的局部变量区，也叫做局部变量表）中引用的对象。</p>
<p>(2). 方法区中的类静态属性引用的对象。</p>
<p>(3). 方法区中常量引用的对象。</p>
<p>(4). 本地方法栈中JNI(Native方法)引用的对象。</p>
<p>下面给出一个GCRoots的例子，如下图，为GCRoots的引用链。<br><img src="/images/javaGC.png" alt="alt"><br>由图可知，obj8、obj9、obj10都没有到GCRoots对象的引用链，即便obj9和obj10之间有引用链，他们还是会被当成垃圾处理，可以进行回收。</p>
<h1 id="四种引用状态"><a href="#四种引用状态" class="headerlink" title="四种引用状态"></a>四种引用状态</h1><p>在JDK1.2之前，Java中引用的定义很传统：如果引用类型的数据中存储的数值代表的是另一块内存的起始地址，就称这块内存代表着一个引用。这种定义很纯粹，但是太过于狭隘，一个对象只有被引用或者没被引用两种状态。我们希望描述这样一类对象：当内存空间还足够时，则能保留在内存中；如果内存空间在进行垃圾收集后还是非常紧张，则可以抛弃这些对象。很多系统的缓存功能都符合这样的应用场景。在JDK1.2之后，Java对引用的概念进行了扩充，将引用分为强引用、软引用、弱引用、虚引用4种，这4种引用强度依次减弱。</p>
<p>1、强引用</p>
<p>代码中普遍存在的类似”Object obj = new Object()”这类的引用，只要强引用还存在，垃圾收集器永远不会回收掉被引用的对象。</p>
<p>2、软引用</p>
<p>描述有些还有用但并非必需的对象。在系统将要发生内存溢出异常之前，将会把这些对象列进回收范围进行二次回收。如果这次回收还没有足够的内存，才会抛出内存溢出异常。Java中的类SoftReference表示软引用。</p>
<p>3、弱引用</p>
<p>描述非必需对象。被弱引用关联的对象只能生存到下一次垃圾回收之前，垃圾收集器工作之后，无论当前内存是否足够，都会回收掉只被弱引用关联的对象。Java中的类WeakReference表示弱引用。</p>
<p>4、虚引用</p>
<p>这个引用存在的唯一目的就是在这个对象被收集器回收时收到一个系统通知，被虚引用关联的对象，和其生存时间完全没关系。Java中的类PhantomReference表示虚引用。<br><img src="/images/yinyongs.png" alt="alt"><br>对于可达性分析算法而言，未到达的对象并非是“非死不可”的，若要宣判一个对象死亡，至少需要经历两次标记阶段。</p>
<ol>
<li>如果对象在进行可达性分析后发现没有与GCRoots相连的引用链，则该对象被第一次标记并进行一次筛选，筛选条件为是否有必要执行该对象的finalize方法，若对象没有覆盖finalize方法或者该finalize方法是否已经被虚拟机执行过了，则均视作不必要执行该对象的finalize方法，即该对象将会被回收。反之，若对象覆盖了finalize方法并且该finalize方法并没有被执行过，那么，这个对象会被放置在一个叫F-Queue的队列中，之后会由虚拟机自动建立的、优先级低的Finalizer线程去执行，而虚拟机不必要等待该线程执行结束，即虚拟机只负责建立线程，其他的事情交给此线程去处理。</li>
</ol>
<p>2.对F-Queue中对象进行第二次标记，如果对象在finalize方法中拯救了自己，即关联上了GCRoots引用链，如把this关键字赋值给其他变量，那么在第二次标记的时候该对象将从“即将回收”的集合中移除，如果对象还是没有拯救自己，那就会被回收。如下代码演示了一个对象如何在finalize方法中拯救了自己，然而，它只能拯救自己一次，第二次就被回收了。具体代码如下：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">package com.demo;</span><br><span class="line"></span><br><span class="line">&#x2F;*</span><br><span class="line"> * 此代码演示了两点：</span><br><span class="line"> * 1.对象可以再被GC时自我拯救</span><br><span class="line"> * 2.这种自救的机会只有一次，因为一个对象的finalize()方法最多只会被系统自动调用一次</span><br><span class="line"> * *&#x2F;</span><br><span class="line">public class FinalizeEscapeGC &#123;</span><br><span class="line">    </span><br><span class="line">    public String name;</span><br><span class="line">    public static FinalizeEscapeGC SAVE_HOOK &#x3D; null;</span><br><span class="line"></span><br><span class="line">    public FinalizeEscapeGC(String name) &#123;</span><br><span class="line">        this.name &#x3D; name;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public void isAlive() &#123;</span><br><span class="line">        System.out.println(&quot;yes, i am still alive :)&quot;);</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    @Override</span><br><span class="line">    protected void finalize() throws Throwable &#123;</span><br><span class="line">        super.finalize();</span><br><span class="line">        System.out.println(&quot;finalize method executed!&quot;);</span><br><span class="line">        System.out.println(this);</span><br><span class="line">        FinalizeEscapeGC.SAVE_HOOK &#x3D; this;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    @Override</span><br><span class="line">    public String toString() &#123;</span><br><span class="line">        return name;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public static void main(String[] args) throws InterruptedException &#123;</span><br><span class="line">        SAVE_HOOK &#x3D; new FinalizeEscapeGC(&quot;leesf&quot;);</span><br><span class="line">        System.out.println(SAVE_HOOK);</span><br><span class="line">        &#x2F;&#x2F; 对象第一次拯救自己</span><br><span class="line">        SAVE_HOOK &#x3D; null;</span><br><span class="line">        System.out.println(SAVE_HOOK);</span><br><span class="line">        System.gc();</span><br><span class="line">        &#x2F;&#x2F; 因为finalize方法优先级很低，所以暂停0.5秒以等待它</span><br><span class="line">        Thread.sleep(500);</span><br><span class="line">        if (SAVE_HOOK !&#x3D; null) &#123;</span><br><span class="line">            SAVE_HOOK.isAlive();</span><br><span class="line">        &#125; else &#123;</span><br><span class="line">            System.out.println(&quot;no, i am dead : (&quot;);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        &#x2F;&#x2F; 下面这段代码与上面的完全相同,但是这一次自救却失败了</span><br><span class="line">        &#x2F;&#x2F; 一个对象的finalize方法只会被调用一次</span><br><span class="line">        SAVE_HOOK &#x3D; null;</span><br><span class="line">        System.gc();</span><br><span class="line">        &#x2F;&#x2F; 因为finalize方法优先级很低，所以暂停0.5秒以等待它</span><br><span class="line">        Thread.sleep(500);</span><br><span class="line">        if (SAVE_HOOK !&#x3D; null) &#123;</span><br><span class="line">            SAVE_HOOK.isAlive();</span><br><span class="line">        &#125; else &#123;</span><br><span class="line">            System.out.println(&quot;no, i am dead : (&quot;);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>看下运行结果：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">leesf</span><br><span class="line">null</span><br><span class="line">finalize method executed!</span><br><span class="line">leesf</span><br><span class="line">yes, i am still alive :)</span><br><span class="line">no, i am dead : (</span><br></pre></td></tr></table></figure>
<p>由结果可知，该对象拯救了自己一次，第二次没有拯救成功，因为对象的finalize方法最多被虚拟机调用一次。此外，从结果我们可以得知，一个堆对象的this（放在局部变量表中的第一项）引用会永远存在，在方法体内可以将this引用赋值给其他变量，这样堆中对象就可以被其他变量所引用，即不会被回收。</p>
<h1 id="方法区的垃圾回收"><a href="#方法区的垃圾回收" class="headerlink" title="方法区的垃圾回收"></a>方法区的垃圾回收</h1><p>方法区的垃圾回收主要回收两部分内容：1. 废弃常量。2. 无用的类。既然进行垃圾回收，就需要判断哪些是废弃常量，哪些是无用的类。</p>
<p>如何判断废弃常量呢？以字面量回收为例，如果一个字符串“abc”已经进入常量池，但是当前系统没有任何一个String对象引用了叫做“abc”的字面量，那么，如果发生垃圾回收并且有必要时，“abc”就会被系统移出常量池。常量池中的其他类（接口）、方法、字段的符号引用也与此类似。</p>
<p>如何判断无用的类呢？需要满足以下三个条件</p>
<ol>
<li><p>该类的所有实例都已经被回收，即Java堆中不存在该类的任何实例。</p>
</li>
<li><p>加载该类的ClassLoader已经被回收。</p>
</li>
<li><p>该类对应的java.lang.Class对象没有在任何地方被引用，无法在任何地方通过反射访问该类的方法。</p>
</li>
</ol>
<p>满足以上三个条件的类可以进行垃圾回收，但是并不是无用就被回收，虚拟机提供了一些参数供我们配置。</p>
]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>java</tag>
      </tags>
  </entry>
  <entry>
    <title>HTTP请求报文和HTTP响应报文</title>
    <url>/%E9%80%9A%E4%BF%A1/HTTP%E8%AF%B7%E6%B1%82%E6%8A%A5%E6%96%87%E5%92%8CHTTP%E5%93%8D%E5%BA%94%E6%8A%A5%E6%96%87/</url>
    <content><![CDATA[<h1 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h1><p>超文本传输协议(Hypertext Transfer Protocol，简称HTTP)是应用层协议。HTTP 是一种请求/响应式的协议，即一个客户端与服务器建立连接后，向服务器发送一个请求;服务器接到请求后，给予相应的响应信息。</p>
<a id="more"></a>
<h1 id="HTTP-请求报文"><a href="#HTTP-请求报文" class="headerlink" title="HTTP 请求报文"></a>HTTP 请求报文</h1><p>HTTP 请求报文由请求行、请求头部、空行 和 请求包体 4 个部分组成，如下图所示：</p>
<p>HTTP 请求报文由请求行、请求头部、空行 和 请求包体 4 个部分组成</p>
<p>　　下面对请求报文格式进行简单的分析：</p>
<p>　　请求行：请求行由方法字段、URL 字段 和HTTP 协议版本字段 3 个部分组成，他们之间使用空格隔开。常用的 HTTP 请求方法有 GET、POST、HEAD、PUT、DELETE、OPTIONS、TRACE、CONNECT;</p>
<p>　　● GET：当客户端要从服务器中读取某个资源时，使用GET 方法。GET 方法要求服务器将URL 定位的资源放在响应报文的数据部分，回送给客户端，即向服务器请求某个资源。使用GET 方法时，请求参数和对应的值附加在 URL 后面，利用一个问号(“?”)代表URL 的结尾与请求参数的开始，传递参数长度受限制。例如，/index.jsp?id=100&amp;op=bind。</p>
<p>　　● POST：当客户端给服务器提供信息较多时可以使用POST 方法，POST 方法向服务器提交数据，比如完成表单数据的提交，将数据提交给服务器处理。GET 一般用于获取/查询资源信息，POST 会附带用户数据，一般用于更新资源信息。POST 方法将请求参数封装在HTTP 请求数据中，以名称/值的形式出现，可以传输大量数据;</p>
<p>　　请求头部：请求头部由关键字/值对组成，每行一对，关键字和值用英文冒号“:”分隔。请求头部通知服务器有关于客户端请求的信息，典型的请求头有：</p>
<p>　　● User-Agent：产生请求的浏览器类型;</p>
<p>　　● Accept：客户端可识别的响应内容类型列表;星号 “ * ” 用于按范围将类型分组，用 “ <em>/</em> ” 指示可接受全部类型，用“ type/* ”指示可接受 type 类型的所有子类型;</p>
<p>　　● Accept-Language：客户端可接受的自然语言;</p>
<p>　　● Accept-Encoding：客户端可接受的编码压缩格式;</p>
<p>　　● Accept-Charset：可接受的应答的字符集;</p>
<p>　　● Host：请求的主机名，允许多个域名同处一个IP 地址，即虚拟主机;</p>
<p>　　● connection：连接方式(close 或 keepalive);</p>
<p>　　● Cookie：存储于客户端扩展字段，向同一域名的服务端发送属于该域的cookie;</p>
<p>　　空行：最后一个请求头之后是一个空行，发送回车符和换行符，通知服务器以下不再有请求头;</p>
<p>　　请求包体：请求包体不在 GET 方法中使用，而是在POST 方法中使用。POST 方法适用于需要客户填写表单的场合。与请求包体相关的最常使用的是包体类型 Content-Type 和包体长度 Content-Length;</p>
<h1 id="HTTP-响应报文"><a href="#HTTP-响应报文" class="headerlink" title="HTTP 响应报文"></a>HTTP 响应报文</h1><p>　　HTTP 响应报文由状态行、响应头部、空行 和 响应包体 4 个部分组成，如下图所示：<br><img src="/images/http%E8%AF%B7%E6%B1%82.png" alt="alt"></p>
<p>下面对响应报文格式进行简单的分析：</p>
<p>　　状态行：状态行由 HTTP 协议版本字段、状态码和状态码的描述文本 3 个部分组成，他们之间使用空格隔开;</p>
<p>　　● 状态码由三位数字组成，第一位数字表示响应的类型，常用的状态码有五大类如下所示：</p>
<p>　　1xx：表示服务器已接收了客户端请求，客户端可继续发送请求;</p>
<p>　　2xx：表示服务器已成功接收到请求并进行处理;</p>
<p>　　3xx：表示服务器要求客户端重定向;</p>
<p>　　4xx：表示客户端的请求有非法内容;</p>
<p>　　5xx：表示服务器未能正常处理客户端的请求而出现意外错误;</p>
<p>　　● 状态码描述文本有如下取值：</p>
<p>　　200 OK：表示客户端请求成功;</p>
<p>　　400 Bad Request：表示客户端请求有语法错误，不能被服务器所理解;</p>
<p>　　401 Unauthonzed：表示请求未经授权，该状态代码必须与 WWW-Authenticate 报头域一起使用;</p>
<p>　　403 Forbidden：表示服务器收到请求，但是拒绝提供服务，通常会在响应正文中给出不提供服务的原因;</p>
<p>　　404 Not Found：请求的资源不存在，例如，输入了错误的URL;</p>
<p>　　500 Internal Server Error：表示服务器发生不可预期的错误，导致无法完成客户端的请求;</p>
<p>　　503 Service Unavailable：表示服务器当前不能够处理客户端的请求，在一段时间之后，服务器可能会恢复正常;</p>
<p>　　响应头部：响应头可能包括：</p>
<p>　　Location：Location响应报头域用于重定向接受者到一个新的位置。例如：客户端所请求的页面已不存在原先的位置，为了让客户端重定向到这个页面新的位置，服务器端可以发回Location响应报头后使用重定向语句，让客户端去访问新的域名所对应的服务器上的资源;</p>
<p>　　Server：Server 响应报头域包含了服务器用来处理请求的软件信息及其版本。它和 User-Agent 请求报头域是相对应的，前者发送服务器端软件的信息，后者发送客户端软件(浏览器)和操作系统的信息。</p>
<p>　　Vary：指示不可缓存的请求头列表;<br>　Connection：连接方式;</p>
<p>　　对于请求来说：close(告诉 WEB 服务器或者代理服务器，在完成本次请求的响应后，断开连接，不等待本次连接的后续请求了)。keepalive(告诉WEB服务器或者代理服务器，在完成本次请求的响应后，保持连接，等待本次连接的后续请求);</p>
<p>　　对于响应来说：close(连接已经关闭); keepalive(连接保持着，在等待本次连接的后续请求); Keep-Alive：如果浏览器请求保持连接，则该头部表明希望WEB 服务器保持连接多长时间(秒);例如：Keep-Alive：300;</p>
<p>　　WWW-Authenticate：WWW-Authenticate响应报头域必须被包含在401 (未授权的)响应消息中，这个报头域和前面讲到的Authorization 请求报头域是相关的，当客户端收到 401 响应消息，就要决定是否请求服务器对其进行验证。如果要求服务器对其进行验证，就可以发送一个包含了Authorization 报头域的请求;</p>
<p>　　空行：最后一个响应头部之后是一个空行，发送回车符和换行符，通知服务器以下不再有响应头部。</p>
<p>　　响应包体：服务器返回给客户端的文本信息;</p>
<p>　　HTTP 工作原理</p>
<p>　　HTTP 协议采用请求/响应模型。客户端向服务器发送一个请求报文，服务器以一个状态作为响应。</p>
<p>　　以下是 HTTP 请求/响应的步骤：</p>
<p>　　● 客户端连接到web服务器：HTTP 客户端与web服务器建立一个 TCP 连接;</p>
<p>　　● 客户端向服务器发起 HTTP 请求：通过已建立的TCP 连接，客户端向服务器发送一个请求报文;</p>
<p>　　● 服务器接收 HTTP 请求并返回 HTTP 响应：服务器解析请求，定位请求资源，服务器将资源副本写到 TCP 连接，由客户端读取;</p>
<p>　　● 释放 TCP 连接：若connection 模式为close，则服务器主动关闭TCP 连接，客户端被动关闭连接，释放TCP 连接;若connection 模式为keepalive，则该连接会保持一段时间，在该时间内可以继续接收请求;</p>
<p>　　● 客户端浏览器解析HTML内容：客户端将服务器响应的 html 文本解析并显示;</p>
<p>　　例如：在浏览器地址栏键入URL，按下回车之后会经历以下流程：</p>
<p>　　1、浏览器向 DNS 服务器请求解析该 URL 中的域名所对应的 IP 地址;</p>
<p>　　2、解析出 IP 地址后，根据该 IP 地址和默认端口 80，和服务器建立 TCP 连接;</p>
<p>　　3、浏览器发出读取文件(URL 中域名后面部分对应的文件)的HTTP 请求，该请求报文作为 TCP 三次握手的第三个报文的数据发送给服务器;</p>
<p>　　4、服务器对浏览器请求作出响应，并把对应的 html 文本发送给浏览器;</p>
<p>　　5、释放 TCP 连接;</p>
<p>　　6、浏览器将该 html 文本并显示内容;</p>
<p>　　HTTP 无状态性</p>
<p>　　HTTP 协议是无状态的(stateless)。也就是说，同一个客户端第二次访问同一个服务器上的页面时，服务器无法知道这个客户端曾经访问过，服务器也无法分辨不同的客户端。HTTP 的无状态特性简化了服务器的设计，使服务器更容易支持大量并发的HTTP 请求。</p>
<p>　　HTTP 持久连接</p>
<p>　　HTTP1.0 使用的是非持久连接，主要缺点是客户端必须为每一个待请求的对象建立并维护一个新的连接，即每请求一个文档就要有两倍RTT 的开销。因为同一个页面可能存在多个对象，所以非持久连接可能使一个页面的下载变得十分缓慢，而且这种短连接增加了网络传输的负担。HTTP1.1 使用持久连接keepalive，所谓持久连接，就是服务器在发送响应后仍然在一段时间内保持这条连接，允许在同一个连接中存在多次数据请求和响应，即在持久连接情况下，服务器在发送完响应后并不关闭TCP 连接，而客户端可以通过这个连接继续请求其他对象。</p>
<p>　　HTTP/1.1 协议的持久连接有两种方式：</p>
<p>　　● 非流水线方式：客户在收到前一个响应后才能发出下一个请求;</p>
<p>　　● 流水线方式：客户在收到 HTTP 的响应报文之前就能接着发送新的请求报文;</p>
<p>　　最后给出一个具体例子：</p>
<p>　　Remote Address:116.57.254.104:80 Request URL:<a href="http://hr.tencent.com/" target="_blank" rel="noopener">http://hr.tencent.com/</a> Request Method:GET Status Code:200 OK Request Headers GET / HTTP/1.1 Host: hr.tencent.com Connection: keep-alive Accept: text/html,application/xhtml+xml,application/xml;q=0.9,image/webp,<em>/</em>;q=0.8 User-Agent: Mozilla/5.0 (X11; Linux i686) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/35.0.1916.114 Safari/537.36 Accept-Encoding: gzip,deflate,sdch Accept-Language: en-US,en;q=0.8,zh-CN;q=0.6,zh;q=0.4 Cookie: pgv_pvi=2098703360; PHPSESSID=bc7onl0dojbsatscsfv79pds77; pgv_info=ssid=s1454606128; pgv_pvid=926725350; ts_uid=4084753309 Response Header HTTP/1.1 200 OK Server: nginx Date: Mon, 26 Jan 2015 01:09:10 GMT Content-Type: text/html;charset=utf-8 Content-Length: 3631 Connection: keep-alive X-Powered-By: PHP/5.3.10 Expires: Thu, 19 Nov 1981 08:52:00 GMT Cache-Control: no-store, no-cache, must-revalidate, post-check=0, pre-check=0 Pragma: no-cache Vary: Accept-Encoding Content-Encoding: gzip</p>
<p>　　从请求报文可以知道：</p>
<p>　　GET / HTTP/1.1</p>
<p>　　请求方法 GET 表示一个读取请求，将从服务器获得网页数据，/表示URL 的路径，URL 总是以/开头，/就表示首页，最后的HTTP/1.1 指示采用的 HTTP 协议版本是 1.1;请求域名如下所示：</p>
<p>　　Host: hr.tencent.com</p>
<p>　　响应报文如下：</p>
<p>　　HTTP/1.1 200 OK</p>
]]></content>
      <categories>
        <category>通信</category>
      </categories>
      <tags>
        <tag>网络</tag>
      </tags>
  </entry>
  <entry>
    <title>MySQL：InnoDB存储引擎的B+树索引算法</title>
    <url>/MySql/MySQL%EF%BC%9AInnoDB%E5%AD%98%E5%82%A8%E5%BC%95%E6%93%8E%E7%9A%84B+%E6%A0%91%E7%B4%A2%E5%BC%95%E7%AE%97%E6%B3%95/</url>
    <content><![CDATA[<h1 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h1><p>MySql技术原理窥探…</p>
<a id="more"></a>
<h1 id="平衡二叉树"><a href="#平衡二叉树" class="headerlink" title="平衡二叉树"></a>平衡二叉树</h1><p>平衡二叉树的定义如下：首先符合二叉查找树的定义，其次必须满足任何节点的两个字数的高度最大差为1。最好的想能需要建立一颗最优二叉树，但是最优二叉树的建立和维护需要大量的操作，因此，用户一般只需要建立一颗平衡二叉树即可。</p>
<p>平衡二叉树的查询速度很快，但是维护一颗平衡二叉树的代价是非常大的。通常来说，需要1次或多次左旋和右旋来得到插入或更新后树的平衡性。因此对于一颗平衡二叉树的维护是有一定开销的，不过平衡二叉树多用于内存结构对象中，因此维护的开销相对较小</p>
<h1 id="B-树"><a href="#B-树" class="headerlink" title="B+树"></a>B+树</h1><p>B+树的定义十分复杂，因此只简要地介绍B+树：B+树是为磁盘或其他直接存取辅助设备而设计的一种平衡查找树，在B+树中，所有记录节点都是按键值的大小顺序存放在同一层的叶节点中，各叶节点指针进行连接。</p>
<h2 id="B-树的插入操作"><a href="#B-树的插入操作" class="headerlink" title="B+树的插入操作"></a>B+树的插入操作</h2><p>B+树的插入必须保证插入后叶节点中的记录依然排序，同时需要考虑插入B+树的三种情况，每种情况都可能会导致不同的插入算法，如表5-1所示。<br><img src="/images/innodb.png" alt="alt"><br>可以看到，不管怎么变化，B+树总是会保持平衡。但是为了保持平衡，对于新插入的键值可能需要做大量的拆分页（split）操作，而B+树主要用于磁盘，因此页的拆分意味着磁盘的操作，应该在可能的情况下尽量减少页的拆分。因此，B+树提供了旋转（rotation）的功能。</p>
<p>旋转发生在Leaf Page已经满了、但是其左右兄弟节点没有满的情况下。这时B+树并不会急于去做拆分页的操作，而是将记录移到所在页的兄弟节点上。通常情况下，左兄弟被首先检查用来做旋转操作。</p>
<h2 id="B-树的删除操作"><a href="#B-树的删除操作" class="headerlink" title="B+树的删除操作"></a>B+树的删除操作</h2><p>B+树使用填充因子（fill factor）来控制树的删除变化，50%是填充因子可设的最小值。B+树的删除操作同样必须保证删除后叶节点中的记录依然排序，同插入一样，B+树的删除操作同样需要考虑如表5-2所示的三种情况，与插入不同的是，删除根据填充因子的变化来衡量。<br><img src="/images/innodb2.png" alt="alt"></p>
<h1 id="B-树索引"><a href="#B-树索引" class="headerlink" title="B+树索引"></a>B+树索引</h1><p>前面讨论的都是B+树的数据结构及其一般操作，B+树索引的本质就是B+树在数据库中的实现。但是B+索引在数据库中有一个特点就是高扇出性，因此在数据库中，B+树的高度一般都在2-4层，也就是说查找某一键值的行记录时最多只需要2-4次IO。因为当前一般的机械磁盘每秒至少可以做100次IO，2-4次的IO意味着查询时间只需要0.02-0.04秒。</p>
<p>数据库中的B+树索引可以分为聚集索引和辅助索引（又被称为非聚集索引），但不管是聚集还是辅助的索引，其内部都是B+树的，即高度平衡的，叶子节点存放着所有的数据。聚集索引与辅助索引不同的是，叶子结点存放的是否是一整行的信息。</p>
<h2 id="聚集索引"><a href="#聚集索引" class="headerlink" title="聚集索引"></a>聚集索引</h2><p>InnoDB存储引擎表是索引组织表，即表中数据按照主键顺序存放。而聚集索引就是按照每张表的主键构造一棵B+树，同时叶子节点中存放的即为整张表的行记录数据，也将聚集索引的叶子节点称为数据页。聚集索引的这个特性决定了索引组织表中数据也是索引的一部分。同B+树数据结构一样，每个数据页都通过一个双向链表来进行链接。</p>
<p>由于实际的数据页只能按照一棵B+树进行排序，因此每张表只能拥有一个聚集索引。在多数情况下，查询优化器倾向于采用聚集索引。因为聚集索引能够在B+树索引的叶子节点上直接找到数据。此外，由于定义了数据的逻辑顺序，聚集索引能够特别快地访问针对范围值的查询。查询优化器能够快速发现某一段范围的数据页需要扫描。</p>
<p>许多文档会告诉我们：聚集索引按照顺序物理地存储数据，但是试想一下，如果聚集索引必须按照特定顺序存放物理记录，则维护成本显得非常之高。所以，聚集索引的存储并不是物理上连续的，而是逻辑上连续的。这其中有两点：一是前面说过的页通过双向链表链接，页按照主键的顺序排序；另一点是每个页中的记录也是通过双向链表进行维护的，物理存储上可以同样不按照主键存储。</p>
<p>聚集索引的另一个好处是，它对于主键的排序查找和范围查找速度非常快。叶子节点的数据就是用户所要查询的数据。</p>
<p>另一个是范围查询，即如果要查找主键某一范围内的数据，通过叶子节点的上层中间节点就可以得到页的范围，之后直接读取数据页即可。</p>
<h2 id="辅助索引"><a href="#辅助索引" class="headerlink" title="辅助索引"></a>辅助索引</h2><p>对于辅助索引（secondary index，也称非聚集索引），叶子节点并不包含行记录的全部数据，叶子节点除了包含键值以外，每个叶子节点中的索引行中还包含了一个书签（bookmark）。该书签用来告诉Innodb存储引擎哪里可以找到与索引相对应的行数据。由于Innodb存储引擎是索引组织表，因此Innodb存储引擎的辅助索引的书签就是相应行数据的聚集索引键。<br>辅助索引的存在并不影响数据在聚集索引中的组织，因此每张表可以由多个辅助索引。当通过辅助索引寻找数据时，InnoDB存储引擎会遍历辅助索引并通过叶级别的指针获得指向主键索引的主键，然后再通过主键索引来找到一个完整的记录。</p>
<p>举例来说，如果在一棵高度为3的辅助索引树中查找数据，那需要对这棵辅助索引数查找3次找到指定主键，如果聚集索引树的高度同样为3，那么还需要对聚集索引树进行3次查找，最终找到一个完整的行数据所在的页。因为一共需要6次逻辑IO访问以得到最终的一个数据页。</p>
<h2 id="B-树索引的分裂"><a href="#B-树索引的分裂" class="headerlink" title="B+树索引的分裂"></a>B+树索引的分裂</h2><p>B+树索引页的分裂并不总是从页的中间记录开始，这样可能会导致页空间的浪费。</p>
<p>InnoDB存储引擎的Page Header中有以下几个部分用来保存插入的顺序信息：</p>
<p>PAGE_LAST_INSERT<br>PAGE_DIRECTION<br>PAGE_N_DIRECTION<br>通过这些信息，InnoDB存储引擎可以决定是向左还是向右进行分裂，同时决定将分裂点记录为哪一个。若插入时随机的，则取页的中间记录作为分裂点的记录，这和之前介绍的相同。若往同一个方向进行插入的记录数量为5，并且目前已经定位到记录（InnoDB存储引擎插入时，首先需要进行定位，定位到的记录为待插入记录的前一条记录）之后还有3条记录，则分裂点的记录为定位到的记录后的第三条记录，否则分裂点记录就是待插入的记录。</p>
<h2 id="B-树索引的管理"><a href="#B-树索引的管理" class="headerlink" title="B+树索引的管理"></a>B+树索引的管理</h2><p>查看表中索引的信息可以使用SHOW INDEX语句。</p>
<p>接着我们来具体讲解每个列的含义：</p>
<p>Table：索引所在的表名。<br>Non_unique：非唯一的索引，可以看到primary key是0，因为必须是唯一的。<br>Key_name：索引的名称，我们可以通过这个名称来DROP INDEX。<br>Seq_in_index：索引中该列的位置，如果看联合索引idx_a_b就比较直观了。<br>Column_name：索引的列<br>Collation：列以什么方式存储在索引中。可以是’A’或者NULL。B+树索引总是A，即排序的。如果使用了Heap存储引擎，并且建立了Hash索引，这里就会显示NULL了。因为Hash根据Hash桶来存放索引数据，而不是对数据进行排序。<br>Cardinality：非常关键的值，表示索引中唯一值的数目的估计值。Cardinality/表的行数应尽可能接近1，如果非常小，那么需要考虑是否还需要建这个索引。<br>Sub_part：是否是列的部分被索引。如果看idx_b这个索引，这里显示100，表示我们只索引b列的前100个字符。如果索引整个列，则该字段为NULL。<br>Packed：关键字如何被压缩。如果没有被压缩，则为NULL。<br>Null：是否索引的列含有NULL值。可以看到idx_b这里为Yes。因为我们定义的b列允许NULL值。<br>Index_type：索引的类型。InnoDB存储引擎只支持B+树索引，所以这里显示的都是BTREE。<br>Comment：注释。<br>Cardinality值非常关键，优化器会根据这个值来判断是否使用这个索引。但是这个值并不是实时更新的，并非每次索引的更新都会更新该值，因为这样代价太大了。因此这个值是不太准确的，只是一个大概的值。上面显示的结果主键的Cardinality为2，但是很显然我们表中有4条记录，这个值应该是4。如果需要更新索引Cardinality的信息，可以使用ANALYZE TABLE命令。</p>
]]></content>
      <categories>
        <category>MySql</category>
      </categories>
      <tags>
        <tag>算法</tag>
        <tag>MySql</tag>
      </tags>
  </entry>
  <entry>
    <title>JVM类加载机制（转）</title>
    <url>/Java/JVM%E7%B1%BB%E5%8A%A0%E8%BD%BD%E7%9A%84%E9%82%A3%E4%BA%9B%E4%BA%8B/</url>
    <content><![CDATA[<h1 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h1><p> 主要探索类加载器是如何找到指定的Class文件以及怎样将Class文件装载进内存，以便执行引擎执行Class文件中存在的数据和指令，从而使你的Java程序跑起来。</p>
<a id="more"></a>
<h1 id="类的生命周期"><a href="#类的生命周期" class="headerlink" title="类的生命周期"></a>类的生命周期</h1><p>先来看一下类的生命周期：<br><img src="/images/class.jpg" alt="alt"></p>
<h1 id="类加载的过程"><a href="#类加载的过程" class="headerlink" title="类加载的过程"></a>类加载的过程</h1><h2 id="加载"><a href="#加载" class="headerlink" title="加载"></a>加载</h2><p>我们首先要明白一件事情：什么开始进行类加载过程的第一阶段：加载？Java虚拟机没有进行强制约束，交由虚拟机的具体实现自由把握。</p>
<p>看完上面的话，我们来看在加载阶段，虚拟机需要完成哪些事情：</p>
<p>通过一个类的全限定名来获取定义此类的二进制字节流<br>将获取到的二进制字节流转化成一种数据结构并放进方法区<br>在内存中生成一个代表此类的java.lang.Class对象，作为访问方法区中各种数据的接口</p>
<p>对于方法区的认识：被加载的类的信息存储在方法区中，可以被线程所共享，也就是说，加载阶段完成后，虚拟机外部的二进制字节流就按照虚拟机所需的格式存储在了方法区之中。然后你能想起来那个二进制流中都存储了哪些信息吗？</p>
<p>对于Class对象认识：Class对象虽然是在内存中，但并未明确规定是在Java堆中，对于HotSpot来说，Class对象存储在方法区中。它作为程序访问方法区中二进制字节流中所存储各种数据的接口。</p>
<h2 id="验证"><a href="#验证" class="headerlink" title="验证"></a>验证</h2><p>从上面类的生命周期一图中我们可以看出，验证是连接的第一步，这一阶段的目的主要是为了确保Class文件的字节流中包含的信息符合当前虚拟机的要求，从而不会危害虚拟机自身安全。也就是说，当加载阶段将字节流加载进方法区之后，JVM需要做的第一件事就是对字节流进行安全校验，以保证格式正确，使自己之后能正确的解析到数据并保证这些数据不会对自身造成危害。</p>
<p>验证阶段主要分成四个子阶段：<br>文件格式验证<br>元数据验证<br>字节码验证<br>符号引用验证</p>
<p>而什么叫做元数据呢？</p>
<p>所谓的元数据是指用来描述数据的数据，更通俗一点就是描述代码间关系，或者代码与其它资源（例如数据库表）之间内在联系的数据，你也可以更简单的认为成框架中的各种@注解，因为这些@注解很简介的描述了大量有关各个类、方法、字段额外的信息或之间的联系。</p>
<p>元数据验证也就是验证这些额外的信息或它们之间的联系是否正确。</p>
<p>我们还得注意字节码验证，在字节码验证中涉及到了一个概念：字节码流。</p>
<p>字节码流 = 操作码 + 操作数。</p>
<p>操作码就是伪指令，操作数就是普通的Java数据，如int，float等等。</p>
<p>所以对字节码验证的过程就是对字节码流验证的过程，也就是验证操作码是否合法，操作数是否合法。</p>
<p>而符号引用验证涉及到常量池解析的知识，在下文中我们顺带着将符号引用验证带过就行，现在先不说。</p>
<p>准备<br>准备阶段你只要掌握两个知识点：</p>
<p>1.准备阶段的目的：正式为类变量分配内存并设置类变量初始值的阶段，这些变量所使用的内存将在方法区中分配。</p>
<p>注意我的重点：是类变量（static）不是实例变量，还有，我们又知道了在JVM的方法区中不仅存储着Class字节流（按照运行时方法区的数据结构进行存储，上述的二进制字节流是不严谨的说法，只是为了大家好理解），还有我们的类变量。</p>
<p>2.这里的类变量初始值通常是指数据类型的零值。比如int的零值为0，long为0L，boolean为false… …真正的初始化赋值是在初始化阶段进行的。</p>
<p>额外一点，如果你设置的类变量还具有final字段，如下：</p>
<p>public static final int value = 123;</p>
<p>那么在准备阶段变量的初始值就会被直接初始化为123，具体原因是由于拥有final字段的变量在它的字段属性表中会出现ConstantValue属性。</p>
<p>解析<br>这一阶段我个人觉得不太好理解并且非常重要，但我还是会一点点剖析难点，保证你能听懂，所以开始吧～～</p>
<p>先来看一下解析阶段的目的：虚拟机将常量池内的符号引用替换为直接引用。</p>
<p>然后说一下解析阶段最大的特点：发生时间不可预料，有可能和初始化阶段互相交换位置。至于原因，我们等下再说。</p>
<p>先来说看完解析阶段的目的吧，你有可能有三个疑问。哪个常量池？什么符号引用？什么直接引用？Ok，搞清这三个问题，解析这部分你也就学会了。</p>
<p>首先来说常量池：在Class的文件结构中我们就花了大量的篇幅去介绍了常量池，我们再来总结一下：常量池(constant pool)指的是在编译期被确定，并被保存在已编译的.class文件中的一些数据。它包括了关于类、方法、接口等中的常量，也包括字符串常量。</p>
<p>然后这段话中的常量池指的就是存在于.class文件中的常量池，结果在运行期被JVM装载，并且可以扩充的存在于方法区中的运行时常量池。</p>
<p>然后来看符号引用：在Class文件中我们也讲述了什么是符号引用。总的来说就是常量池中存储的那些描述类、方法、接口的字面量，你可以简单的理解为就是那些所需要信息的全限定名，目的就是为了虚拟机在使用的时候可以定位到所需要的目标。</p>
<p>最后来看直接引用：直接指向目标的指针、相对偏移量或能间接定位到目标的句柄。</p>
<p>现在我们对上面那句话进行重新解读：虚拟机将运行时常量池中那些仅代表其他信息的符号引用解析为直接指向所需信息所在地址的指针。</p>
<p>大概就是这样，我觉得你应该已经完全明白了。</p>
<p>解决一个遗留的问题：还记得刚才没有说到的符号引用吗？</p>
<p>这一阶段就是发生在JVM将符号引用转换为直接引用的时候，它的作用就是对类自身以外（常量池中的各种符号引用）的信息进行匹配性校验，以确保解析动作能够正常执行！</p>
<p>在解析阶段主要有以下不同的动作，我只给大家罗列出来，不细讲，有兴趣的同学可以自行百度：</p>
<p>类或接口的解析（注意数组类和非数组类）<br>字段（简单名称+字段描述符）解析（注意递归搜索）<br>类方法解析（注意递归搜索）<br>接口方法解析（注意递归搜索）<br>在解析阶段还有一个很有意思的东西：动态连接！</p>
<p>它也是上面解析阶段发生时间不确定的直接原因：大部分JVM的实现都是延迟加载或者叫做动态连接。它的意思就是JVM装载某个类A时，如果类A中有引用其他类B，虚拟机并不会将这个类B也同时装载进JVM内存，而是等到执行的时候才去装载。</p>
<p>而这个被引用的B类在引用它的类A中的表现形式主要被登记在了符号表中，而解析的过程就是当需要用到被引用类B的时候，将引用类B在引用类A的符号引用名改为内存里的直接引用。这就是解析发生时间不可预料的原因，而且这个阶段是发生在方法区中的。</p>
<p>初始化<br>虚拟机规范定义了5种情况，会触发类的初始化阶段，也正是这个阶段，JVM才真正开始执行类中定义的Java程序代码：</p>
<p>new一个对象、读取一个类静态字段、调用一个类的静态方法的时候<br>对类进行反射调用的时候<br>初始化一个类，发现父类还没有初始化，则先初始化父类<br>main方法开始执行时所在的类<br>最后一种情况我也不懂，就不贴了<br>额外补充：</p>
<p>有三种引用类的方式不会触发初始化（也就是类的加载），为以下三种：</p>
<p>通过子类引用父类的静态字段，不会导致子类初始化<br>通过数组定义来引用类，不会触发此类的初始化<br>引用另一个类中的常量不会触发另一个类的初始化，原因在于“常量传播优化”<br>来说一说常量传播优化吧（先看一份代码）：</p>
<p>public class ConstClass {<br>    static {<br>        System.out.println(“ConstClass init!”);<br>    }</p>
<pre><code>public static final String HELLOWORLD = &quot;hello world&quot;;</code></pre><p>}</p>
<p>public class NotInitialization {</p>
<pre><code>public static void main(String[] args) {
    System.out.println(ConstClass.HELLOWORLD);
}</code></pre><p>}<br>这种调用方式不会触发ConstClass的初始化，因为常量传播优化，常量“hello world”已经被存储到了NotInitialization类的常量池中，以后NotInitialization对常量ConstClass.HELLOWORLD的引用实际上都被转化为NotInitialization对自身常量池的引用。</p>
<p>然后在初识化阶段我们重点掌握的知识就是类构造器<clinit>()了。</p>
<p>这个东西我也只是提几点重要的：<br>1.<clinit>()是编译器自动收集类中的所有类变量的赋值动作和静态语句块合并产生的。<br>2.父类中定义的静态语句块要优先于子类的变量赋值操作。<br>3.虚拟机保证一个类的<clinit>()方法在多线程环境中被正确的加锁、同步。</p>
<p>方法区使用实例<br>上面讲了那么多，不知道大家吸收了没有，学习的过程中一定要学会总结和抓中重点哦～</p>
<p>咱们来看一个例子将上面类加载的过程来串一下吧，加深一下自己的印象：（其中还涉及了一点执行引擎的知识，没关系，很容易理解）</p>
<p>class Lava {</p>
<pre><code>private int speed = 5;
void flow() {

}</code></pre><p>}</p>
<p>public class Volcano {</p>
<pre><code>public static void main(String[] args) {
    Lava lava = new Lava();
    lava.flow();
}</code></pre><p>}<br>不同的虚拟机实现可能会用完全不同的方法来操作，下面描述的只是其中一种可能——但并不是仅有的一种。</p>
<p>加载：读取一个类的class文件并将其中的二进制字节流组织成正确的数据结构放进运行时方法区中：</p>
<p>要运行Volcano程序，首先得以某种“依赖于实现的”方式告诉虚拟机“Volcano”这个名字。之后，虚拟机将找到并读入相应的class文件“Volcano.class”，然后它会从导入的class文件里的二进制数据中提取类型信息并放到方法区中。通过执行保存在方法区中的字节码，虚拟机开始执行main()方法，在执行时，它会一直持有指向当前类（Volcano类）的常量池（方法区中的一个数据结构）的指针。</p>
<p>注意：虚拟机开始执行Volcano类中main()方法的字节码的时候，尽管Lava类还没被装载，但是和大多数（也许所有）虚拟机实现一样，它不会等到把程序中用到的所有类都装载后才开始运行。恰好相反，它只会需要时才装载相应的类。（延迟加载、动态连接）</p>
<p>main()的第一条指令告知虚拟机为列在常量池第一项的类分配足够的内存。所以虚拟机使用指向Volcano常量池的指针找到第一项，发现它是一个对Lava类的符号引用，然后它就检查方法区，看Lava类是否已经被加载了。</p>
<p>这个符号引用仅仅是一个给出了类Lava的全限定名“Lava”的字符串。为了能让虚拟机尽可能快地从一个名称找到类，虚拟机的设计者应当选择最佳的数据结构和算法。</p>
<p>当虚拟机发现还没有装载过名为“Lava”的类时，它就开始查找并装载文件“Lava.class”，并把从读入的二进制数据中提取的类型信息放在方法区中。</p>
<p>解析：</p>
<p>紧接着，虚拟机以一个直接指向方法区Lava类数据的指针来替换常量池第一项（就是那个字符串“Lava”），以后就可以用这个指针来快速地访问Lava类了。这个替换过程称为常量池解析，即把常量池中的符号引用替换为直接引用。</p>
<p>终于，虚拟机准备为一个新的Lava对象分配内存。此时它又需要方法区中的信息。还记得刚刚放到Volcano类常量池第一项的指针吗？现在虚拟机用它来访问Lava类型信息，找出其中记录的这样一条信息：一个Lava对象需要分配多少堆空间。</p>
<p>JAVA虚拟机总能够通过存储在方法区的类型信息来确定一个对象需要多少内存，当JAVA虚拟机确定了一个Lava对象的大小后，它就在堆上分配这么大的空间，并把这个对象实例的变量speed初始化为默认初始值0。</p>
<p>当把新生成的Lava对象的引用压到栈中，main()方法的第一条指令也完成了。接下来的指令通过这个引用调用Java代码（该代码把speed变量初始化为正确初始值5）。另一条指令将用这个引用调用Lava对象引用的flow()方法。</p>
<p>类加载器<br>其实这一部分的知识并不多，你需要了解、掌握的知识只有两点：</p>
<p>1.类加载器的命名空间<br>2.双亲委派模型</p>
<p>说一点啊，看到这些高大上的名词你们不要怕，又不让你拿代码去实现，其实其中的原理都是很简单的。</p>
<p>上面说了那么多，类加载器就是用于实现类加载动作的一段代码实现。好了，明白了它的作用，我们来看看什么是它命名空间。</p>
<p>类加载器的命名空间：对于任意一个类，都需要由加载它的类加载器和这个类本身一同确立其在Java虚拟机中的唯一性，每一个类加载器，都拥有一个独立的类命名空间。也就是说，你现在要比较两个类是否相等，只有在这两个类是同一个类加载器加载的前提下才有意义。</p>
<p>这就是类加载器的命名空间，不难吧～但是读完上面这段话我们可以提取出另一个重要的信息：你上面所说的话中好像包含这样一种意思：类加载器在JVM中不止一个？你很聪明！好了，接下来说明何谓双亲委派模型。</p>
<p>双亲委派模型：首先你得知道在JVM中有三种系统提供的类加载器：启动类加载器，扩展类加载器、应用程序类加载器。关于这三种加载器的描述大家自行百度，这也不是重点。</p>
<p>贴一张图：</p>
<p>这里写图片描述</p>
<p>如图，这种层次结构就是双亲委派模型。</p>
<p>好了，为了让大家印象深刻，我在给大家描述一下双亲委派模型的工作过程吧：</p>
<p>它是一个递归调用类加载器的模型，也就是说如果一个类加载器收到了类加载的请求，它首先不会自己去加载这个类，而是不断请求父加载器，如果父加载器可以完成这个加载请求，那么就由父加载器进行加载，如果父加载器不能完成加载请求（它的搜索范围中没有找到所需的类），子加载器才会尝试自己去加载。</p>
<p>那么使用这种模型有什么好处？</p>
<p>Java类随着类加载器一起具备了带有优先级的层级关系。例如java.lang.Object，在程序的各种类加载器环境中都是同一个类。</p>
]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>java</tag>
      </tags>
  </entry>
  <entry>
    <title>Synchronized关键字和Lock</title>
    <url>/Java/Synchronized%E5%85%B3%E9%94%AE%E5%AD%97%E3%80%81Lock%EF%BC%8C%E5%B9%B6%E8%A7%A3%E9%87%8A%E5%AE%83%E4%BB%AC%E4%B9%8B%E9%97%B4%E7%9A%84%E5%8C%BA%E5%88%AB/</url>
    <content><![CDATA[<h1 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h1><p>java中并发那点事儿…</p>
<a id="more"></a>
<h1 id="并发编程概念"><a href="#并发编程概念" class="headerlink" title="并发编程概念"></a>并发编程概念</h1><p>原子性： 一个操作或多个操作要么全部执行且执行过程不被中断，要么不执行<br>可见性： 多个线程修改同一个共享变量时，一个线程修改后，其他线程能马上获得修改后的值<br>有序性 : 程序执行的顺序按照代码的先后顺序执行<br>其中，LanZhouNoodle 和 HuangMenChicken 都继承自 Food。<br>简单地说，简单工厂模式通常就是这样，一个工厂类 XxxFactory，里面有一个静态方法，根据我们不同的参数，返回不同的派生自同一个父类（或实现同一接口）的实例对象。<br>我们强调职责单一原则，一个类只提供一种功能，FoodFactory 的功能就是只要负责生产各种 Food。</p>
<h1 id="synchronized"><a href="#synchronized" class="headerlink" title="synchronized"></a>synchronized</h1><p>synchronized关键字是为了线程安全服务的，线程安全就是当多个线程访问一个类(变量或方法)的时候，这个对象都可以表现出正常的行为。那线程的安全性又表现出三个特性：原子性、可见性及有序性。synchronized保证了同步，原子性。volatile关键字保证了可见性。wait，notify 负责多个线程之间的通信。</p>
<p>synchronized 可以在任意对象及方法上加锁，若一个线程想要执行synchronized修饰的代码块，首先要</p>
<p>step1 尝试获得锁<br>step2 如果拿到锁，执行synchronized代码体内容<br>step3 如果拿不到锁，这个线程就会不断的尝试获得这把锁，直到拿到为止，而且是多个线程同时去竞争这把锁。</p>
<p>注*（线程多了也就是会出现锁竞争的问题，多个线程执行的顺序是按照CPU分配的先后顺序而定的，而并非代码执行的先后顺序）</p>
<p>synchronized 可以修饰方法，修饰代码块，这些都是对象锁。若和static一起使用，则升级为类锁。建议修饰代码块，减小锁的颗粒度，提高系统的性能。<br>synchronized 锁是可以重入的，当一个线程得到了一个对象的锁后，再次请求此对象时是可以再次得到该对象的锁。锁重入的机制，也支持在父子类继承的场景。<br>synchronized 同步异步，一个线程得到了一个对象的锁后，其他线程是可以执行非加锁的方法(异步)。但是不能执行其他加锁的方法(同步)。<br>synchronized 锁异常，当一个线程执行的代码出现异常时，其所持有的锁会自动释放。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">private void thisLock () &#123; &#x2F;&#x2F; 使用对象锁</span><br><span class="line">　　　　　　synchronized (this) &#123;</span><br><span class="line">　　　　　　　　System.out.println(&quot;this 对象锁!&quot;);</span><br><span class="line">　　　　　　&#125;</span><br><span class="line">　　　　&#125;</span><br><span class="line"></span><br><span class="line"> </span><br><span class="line"></span><br><span class="line">　　　　private void classLock () &#123; &#x2F;&#x2F; 使用类锁</span><br><span class="line">　　　　　　synchronized (ITDragonSynchronized.class) &#123;</span><br><span class="line">　　　　　　　　System.out.println(&quot;class 类锁!&quot;);</span><br><span class="line">　　　　　　&#125;</span><br><span class="line">　　　　&#125;</span><br></pre></td></tr></table></figure>
<p>目前可以使用的场景，在使用单例模式的时候可以使用synchronized关键字，进行二次验证，防止在并发的情况下创建出多个对象。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">　　　　　　&#x2F;&#x2F; 公共静态成员方法，返回唯一实例</span><br><span class="line">　　　　　　public static LoadBalancer GetLoadBalancer()&#123;</span><br><span class="line">　　　　　　　　&#x2F;&#x2F; 第一重判断</span><br><span class="line">　　　　　　　　if (instance &#x3D;&#x3D; null)&#123;</span><br><span class="line">　　　　　　　　　　&#x2F;&#x2F; 锁定代码块</span><br><span class="line">　　　　　　　　　　synchronized(syncLocker)&#123;</span><br><span class="line">　　　　　　　　　　　　&#x2F;&#x2F; 第二重判断</span><br><span class="line">　　　　　　　　　　　　if (instance &#x3D;&#x3D; null)&#123;</span><br><span class="line">　　　　　　　　　　　　　　instance &#x3D; new LoadBalancer();</span><br><span class="line">　　　　　　　　　　　　&#125;</span><br><span class="line">　　　　　　　　　　&#125;</span><br><span class="line">　　　　　　　　&#125;</span><br><span class="line">　　　　　　　　return instance;</span><br><span class="line">　　　　　　&#125;</span><br></pre></td></tr></table></figure>

<h1 id="volatile"><a href="#volatile" class="headerlink" title="volatile"></a>volatile</h1><p>volatile关键字虽然不具备synchronized关键字的原子性（同步）但其主要作用就是使变量在多个线程中可见。也就是可见性。用法很简单，直接用来修饰变量。因为其不具备原子性。</p>
<p>volatile 关键字工作原理每个线程都有自己的工作内存，如果线程需要用到一个变量的时，会从主内存拷贝一份到自己的工作内存中。从而提高了效率。每次执行完线程后再将变量从工作内存同步回主内存中。</p>
<p>这样就存在一个问题，变量在不同线程中可能存在不同的值。如果用volatile 关键字修饰变量，则会让线程的执行引擎直接从主内存中获取值。<br><img src="/images/volite.png" alt="alt"><br>如果一个代码块被synchronized修饰了，当一个线程获取了对应的锁，并执行该代码块时，其他线程便只能一直等待，等待获取锁的线程释放锁，而这里获取锁的线程释放锁只会有两种情况：</p>
<p>1）获取锁的线程执行完了该代码块，然后线程释放对锁的占有；</p>
<p>2）线程执行发生异常，此时JVM会让线程自动释放锁；</p>
<p>因此就需要有一种机制可以不让等待的线程一直无期限地等待下去（比如只等待一定的时间或者能够响应中断），通过Lock就可以办到，注意：</p>
<p>1）Lock不是Java语言内置的，synchronized是Java语言的关键字，因此是内置特性。Lock是一个类，通过这个类可以实现同步访问；</p>
<p>2）Lock和synchronized有一点非常大的不同，采用synchronized不需要用户去手动释放锁，当synchronized方法或者synchronized代码块执行完之后，系统会自动让线程释放对锁的占用；而Lock则必须要用户去手动释放锁，如果没有主动释放锁，就有可能导致出现死锁现象</p>
<p>使用Lock必须在try{}catch{}块中进行，并且将释放锁的操作放在finally块中进行，以保证锁一定被被释放，防止死锁的发生</p>
<p>首先要说明的就是Lock，通过查看Lock的源码可知，Lock是一个接口：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">public interface Lock &#123;</span><br><span class="line"></span><br><span class="line">    void lock();</span><br><span class="line">    void lockInterruptibly() throws InterruptedException;</span><br><span class="line">    boolean tryLock();</span><br><span class="line">    boolean tryLock(long time, TimeUnit unit) throws InterruptedException;</span><br><span class="line">    void unlock();</span><br><span class="line">    Condition newCondition();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>编程</tag>
      </tags>
  </entry>
  <entry>
    <title>并发编程之volatile关键字解析</title>
    <url>/Java/%E5%B9%B6%E5%8F%91%E7%BC%96%E7%A8%8B%E4%B9%8Bvolatile%E5%85%B3%E9%94%AE%E5%AD%97%E8%A7%A3%E6%9E%90/</url>
    <content><![CDATA[<h1 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h1><p>java并发那点事儿…</p>
<a id="more"></a>
<h1 id="volatile关键字"><a href="#volatile关键字" class="headerlink" title="volatile关键字"></a>volatile关键字</h1><p>作用是使变量在多个线程间可见。</p>
<p>关键字volatile可以说是Java虚拟机提供的最轻量级的同步机制。</p>
<p>一个变量被定义为volatile后，它将具备两种特性：</p>
<p>1、保证此变量对所有线程的”可见性”，所谓”可见性”是指当一条线程修改了这个变量的值，新值对于其它线程来说都是可以立即得知的，而普通变量不能做到这一点，普通变量的值在在线程间传递均需要通过主内存来完成，再强调一遍，volatile只保证了可见性，并不保证基于volatile变量的运算在并发下是安全的</p>
<p>2、使用volatile变量的第二个语义是禁止指令重排序优化，普通变量仅仅会保证在该方法的执行过程中所有依赖赋值结果的地方都能获取到正确的结果，而不能保证变量赋值操作的顺序与程序代码中的执行顺序一致。</p>
<p>总结一下Java内存模型对volatile变量定义的特殊规则：</p>
<p>1、在工作内存中，每次使用某个变量的时候都必须线从主内存刷新最新的值，用于保证能看见其他线程对该变量所做的修改之后的值</p>
<p>2、在工作内存中，每次修改完某个变量后都必须立刻同步回主内存中，用于保证其他线程能够看见自己对该变量所做的修改</p>
<p>3、volatile修饰的变量不会被指令重排序优化，保证代码的执行顺序与程序顺序相同</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">public class MyThread28 extends Thread</span><br><span class="line">  &#123;</span><br><span class="line">      private boolean isRunning &#x3D; true;</span><br><span class="line">  </span><br><span class="line">      public boolean isRunning()</span><br><span class="line">      &#123;</span><br><span class="line">          return isRunning;</span><br><span class="line">      &#125;</span><br><span class="line">  </span><br><span class="line">     public void setRunning(boolean isRunning)</span><br><span class="line">     &#123;</span><br><span class="line">         this.isRunning &#x3D; isRunning;</span><br><span class="line">     &#125;</span><br><span class="line">     </span><br><span class="line">     public void run()</span><br><span class="line">     &#123;</span><br><span class="line">         System.out.println(&quot;进入run了&quot;);</span><br><span class="line">         while (isRunning &#x3D;&#x3D; true)&#123;&#125;</span><br><span class="line">         System.out.println(&quot;线程被停止了&quot;);</span><br><span class="line">     &#125;</span><br><span class="line"> &#125;</span><br><span class="line"></span><br><span class="line">  public static void main(String[] args)</span><br><span class="line">  &#123;</span><br><span class="line">      try</span><br><span class="line">      &#123;</span><br><span class="line">          MyThread28 mt &#x3D; new MyThread28();</span><br><span class="line">          mt.start();</span><br><span class="line">          Thread.sleep(1000);</span><br><span class="line">          mt.setRunning(false);</span><br><span class="line">          System.out.println(&quot;已赋值为false&quot;);</span><br><span class="line">     &#125;</span><br><span class="line">     catch (InterruptedException e)</span><br><span class="line">     &#123;</span><br><span class="line">         e.printStackTrace();</span><br><span class="line">     &#125;</span><br><span class="line"> &#125;</span><br></pre></td></tr></table></figure>
<p>看一下运行结果：</p>
<p>进入run了<br>已赋值为false<br>也许这个结果有点奇怪，明明isRunning已经设置为false了， 线程还没停止呢？</p>
<p>这就要从Java内存模型（JMM）说起，这里先简单讲，虚拟机那块会详细讲的。根据JMM，Java中有一块主内存，不同的线程有自己的工作内存，同一个变量值在主内存中有一份，如果线程用到了这个变量的话，自己的工作内存中有一份一模一样的拷贝。每次进入线程从主内存中拿到变量值，每次执行完线程将变量从工作内存同步回主内存中。</p>
<p>出现打印结果现象的原因就是主内存和工作内存中数据的不同步造成的。因为执行run()方法的时候拿到一个主内存isRunning的拷贝，而设置isRunning是在main函数中做的，换句话说 ，设置的isRunning设置的是主内存中的isRunning，更新了主内存的isRunning，线程工作内存中的isRunning没有更新，当然一直死循环了，因为对于线程来说，它的isRunning依然是true。</p>
<p>解决这个问题很简单，给isRunning关键字加上volatile。加上了volatile的意思是，每次读取isRunning的值的时候，都先从主内存中把isRunning同步到线程的工作内存中，再当前时刻最新的isRunning。看一下给isRunning加了volatile关键字的运行效果：</p>
<p>进入run了<br>已赋值为false<br>线程被停止了<br>看到这下线程停止了，因为从主内存中读取了最新的isRunning值，线程工作内存中的isRunning变成了false，自然while循环就结束了。</p>
<p>volatile的作用就是这样，被volatile修饰的变量，保证了每次读取到的都是最新的那个值。线程安全围绕的是可见性和原子性这两个特性展开的，volatile解决的是变量在多个线程之间的可见性，但是无法保证原子性。</p>
<p>多提一句，synchronized除了保障了原子性外，其实也保障了可见性。因为synchronized无论是同步的方法还是同步的代码块，都会先把主内存的数据拷贝到工作内存中，同步代码块结束，会把工作内存中的数据更新到主内存中，这样主内存中的数据一定是最新的。</p>
<p>原子类也无法保证线程安全</p>
<p>原子操作表示一段操作是不可分割的，没有其他线程能够中断或检查正在原子操作中的变量。一个原子类就是一个原子操作可用的类，它可以在没有锁的情况下保证线程安全。</p>
<p>但是这种线程安全不是绝对的，在有逻辑的情况下输出结果也具有随机性，比如</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"> public class ThreadDomain29</span><br><span class="line">  &#123;</span><br><span class="line">      public static AtomicInteger aiRef &#x3D; new AtomicInteger();</span><br><span class="line">      </span><br><span class="line">      public void addNum()</span><br><span class="line">      &#123;</span><br><span class="line">          System.out.println(Thread.currentThread().getName() + &quot;加了100之后的结果：&quot; + </span><br><span class="line">                  aiRef.addAndGet(100));</span><br><span class="line">          aiRef.getAndAdd(1);</span><br><span class="line">     &#125;</span><br><span class="line"> &#125;</span><br><span class="line"></span><br><span class="line">   public class MyThread29 extends Thread</span><br><span class="line">  &#123;</span><br><span class="line">      private ThreadDomain29 td;</span><br><span class="line">      </span><br><span class="line">      public MyThread29(ThreadDomain29 td)</span><br><span class="line">      &#123;</span><br><span class="line">          this.td &#x3D; td;</span><br><span class="line">      &#125;</span><br><span class="line">      </span><br><span class="line">     public void run()</span><br><span class="line">     &#123;</span><br><span class="line">         td.addNum();</span><br><span class="line">     &#125;</span><br><span class="line"> &#125;</span><br><span class="line"></span><br><span class="line">public static void main(String[] args)</span><br><span class="line">  &#123;</span><br><span class="line">      try</span><br><span class="line">      &#123;</span><br><span class="line">          ThreadDomain29 td &#x3D; new ThreadDomain29();</span><br><span class="line">          MyThread29[] mt &#x3D; new MyThread29[5];</span><br><span class="line">          for (int i &#x3D; 0; i &lt; mt.length; i++)</span><br><span class="line">          &#123;</span><br><span class="line">              mt[i] &#x3D; new MyThread29(td);</span><br><span class="line">         &#125;</span><br><span class="line">         for (int i &#x3D; 0; i &lt; mt.length; i++)</span><br><span class="line">         &#123;</span><br><span class="line">             mt[i].start();</span><br><span class="line">         &#125;</span><br><span class="line">         Thread.sleep(1000);</span><br><span class="line">         System.out.println(ThreadDomain29.aiRef.get());</span><br><span class="line">     &#125; </span><br><span class="line">     catch (InterruptedException e)</span><br><span class="line">     &#123;</span><br><span class="line">         e.printStackTrace();</span><br><span class="line">     &#125;</span><br><span class="line"> &#125;</span><br></pre></td></tr></table></figure>
<p>这里用了一个Integer的原子类AtomicInteger，看一下运行结果：</p>
<p>Thread-1加了100之后的结果：200<br>Thread-4加了100之后的结果：500<br>Thread-3加了100之后的结果：400<br>Thread-2加了100之后的结果：300<br>Thread-0加了100之后的结果：100<br>505<br>显然，结果是正确的，但不是我们想要的，因为我们肯定希望按顺序输出加了之后的结果，现在却是200、500、400、300、100这么输出。导致这个问题产生的原因是aiRef.addAndGet(100)和aaiRef.addAndGet(1)这两个操作是可分割导致的。</p>
<p>解决方案，就是给addNum方法加上synchronized即可。</p>
<p>synchronized关键字可以使多个线程访问同一资源具有同步性，而且还具有将线程工作内存中的私有变量和公共内存中的变量同步的功能。</p>
]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>编程</tag>
      </tags>
  </entry>
  <entry>
    <title>进阶之路—设计模式（七）</title>
    <url>/Java/%E8%BF%9B%E9%98%B6%E4%B9%8B%E8%B7%AF-%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F%EF%BC%88%E4%B8%83%EF%BC%89/</url>
    <content><![CDATA[<h1 id="责任链模式"><a href="#责任链模式" class="headerlink" title="责任链模式"></a>责任链模式</h1><p>责任链通常需要先建立一个单向链表，然后调用方只需要调用头部节点就可以了，后面会自动流转下去。比如流程审批就是一个很好的例子，只要终端用户提交申请，根据申请的内容信息，自动建立一条责任链，然后就可以开始流转了</p>
<p>有这么一个场景，用户参加一个活动可以领取奖品，但是活动需要进行很多的规则校验然后才能放行，比如首先需要校验用户是否是新用户、今日参与人数是否有限额、全场参与人数是否有限额等等。设定的规则都通过后，才能让用户领走奖品。</p>
<p>如果产品给你这个需求的话，我想大部分人一开始肯定想的就是，用一个 List 来存放所有的规则，然后 foreach 执行一下每个规则就好了。不过，读者也先别急，看看责任链模式和我们说的这个有什么不一样？</p>
<p>首先，我们要定义流程上节点的基类：</p>
<a id="more"></a>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">public abstract class RuleHandler &#123;</span><br><span class="line"></span><br><span class="line">      &#x2F;&#x2F; 后继节点</span><br><span class="line">    protected RuleHandler successor;</span><br><span class="line"></span><br><span class="line">    public abstract void apply(Context context);</span><br><span class="line"></span><br><span class="line">    public void setSuccessor(RuleHandler successor) &#123;</span><br><span class="line">        this.successor &#x3D; successor;</span><br><span class="line">    &#125;</span><br><span class="line">    public RuleHandler getSuccessor() &#123;</span><br><span class="line">        return successor;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>接下来，我们需要定义具体的每个节点了。</p>
<p>校验用户是否是新用户：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">public class NewUserRuleHandler extends RuleHandler &#123;</span><br><span class="line"></span><br><span class="line">    public void apply(Context context) &#123;</span><br><span class="line">        if (context.isNewUser()) &#123;</span><br><span class="line">              &#x2F;&#x2F; 如果有后继节点的话，传递下去</span><br><span class="line">            if (this.getSuccessor() !&#x3D; null) &#123;</span><br><span class="line">                this.getSuccessor().apply(context);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125; else &#123;</span><br><span class="line">            throw new RuntimeException(&quot;该活动仅限新用户参与&quot;);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>校验用户所在地区是否可以参与：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">public class LocationRuleHandler extends RuleHandler &#123;</span><br><span class="line">    public void apply(Context context) &#123;</span><br><span class="line">        boolean allowed &#x3D; activityService.isSupportedLocation(context.getLocation);</span><br><span class="line">          if (allowed) &#123;</span><br><span class="line">            if (this.getSuccessor() !&#x3D; null) &#123;</span><br><span class="line">                this.getSuccessor().apply(context);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125; else  &#123;</span><br><span class="line">            throw new RuntimeException(&quot;非常抱歉，您所在的地区无法参与本次活动&quot;);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>校验奖品是否已领完：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">public class LimitRuleHandler extends RuleHandler &#123;</span><br><span class="line">    public void apply(Context context) &#123;</span><br><span class="line">          int remainedTimes &#x3D; activityService.queryRemainedTimes(context); &#x2F;&#x2F; 查询剩余奖品</span><br><span class="line">        if (remainedTimes &gt; 0) &#123;</span><br><span class="line">            if (this.getSuccessor() !&#x3D; null) &#123;</span><br><span class="line">                this.getSuccessor().apply(userInfo);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125; else &#123;</span><br><span class="line">            throw new RuntimeException(&quot;您来得太晚了，奖品被领完了&quot;);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>代码其实很简单，就是先定义好一个链表，然后在通过任意一节点后，如果此节点有后继节点，那么传递下去。</p>
<h1 id="模板方法模式"><a href="#模板方法模式" class="headerlink" title="模板方法模式"></a>模板方法模式</h1><p>在含有继承结构的代码中，模板方法模式是非常常用的，这也是在开源代码中大量被使用的。</p>
<p>通常会有一个抽象类：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">public abstract class AbstractTemplate &#123;</span><br><span class="line">    &#x2F;&#x2F; 这就是模板方法</span><br><span class="line">      public void templateMethod()&#123;</span><br><span class="line">        init();</span><br><span class="line">        apply(); &#x2F;&#x2F; 这个是重点</span><br><span class="line">        end(); &#x2F;&#x2F; 可以作为钩子方法</span><br><span class="line">    &#125;</span><br><span class="line">    protected void init() &#123;</span><br><span class="line">        System.out.println(&quot;init 抽象层已经实现，子类也可以选择覆写&quot;);</span><br><span class="line">    &#125;</span><br><span class="line">      &#x2F;&#x2F; 留给子类实现</span><br><span class="line">    protected abstract void apply();</span><br><span class="line">    protected void end() &#123;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>模板方法中调用了 3 个方法，其中 apply() 是抽象方法，子类必须实现它，其实模板方法中有几个抽象方法完全是自由的，我们也可以将三个方法都设置为抽象方法，让子类来实现。也就是说，模板方法只负责定义第一步应该要做什么，第二步应该做什么，第三步应该做什么，至于怎么做，由子类来实现。<br>我们写一个实现类：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">public class ConcreteTemplate extends AbstractTemplate &#123;</span><br><span class="line">    public void apply() &#123;</span><br><span class="line">        System.out.println(&quot;子类实现抽象方法 apply&quot;);</span><br><span class="line">    &#125;</span><br><span class="line">      public void end() &#123;</span><br><span class="line">        System.out.println(&quot;我们可以把 method3 当做钩子方法来使用，需要的时候覆写就可以了&quot;);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>客户端调用演示：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">public static void main(String[] args) &#123;</span><br><span class="line">    AbstractTemplate t &#x3D; new ConcreteTemplate();</span><br><span class="line">      &#x2F;&#x2F; 调用模板方法</span><br><span class="line">      t.templateMethod();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="状态模式"><a href="#状态模式" class="headerlink" title="状态模式"></a>状态模式</h1><p>说一个简单的例子。商品库存中心有个最基本的需求是减库存和补库存，我们看看怎么用状态模式来写。<br>核心在于，我们的关注点不再是 Context 是该进行哪种操作，而是关注在这个 Context 会有哪些操作。</p>
<p>定义状态接口：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">public interface State &#123;</span><br><span class="line">   public void doAction(Context context);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>定义减库存的状态：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">public class DeductState implements State &#123;</span><br><span class="line"></span><br><span class="line">   public void doAction(Context context) &#123;</span><br><span class="line">      System.out.println(&quot;商品卖出，准备减库存&quot;);</span><br><span class="line">      context.setState(this);</span><br><span class="line"></span><br><span class="line">      &#x2F;&#x2F;... 执行减库存的具体操作</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   public String toString()&#123;</span><br><span class="line">      return &quot;Deduct State&quot;;</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>定义补库存状态：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">public class RevertState implements State &#123;</span><br><span class="line">    public void doAction(Context context) &#123;</span><br><span class="line">        System.out.println(&quot;给此商品补库存&quot;);</span><br><span class="line">          context.setState(this);</span><br><span class="line"></span><br><span class="line">          &#x2F;&#x2F;... 执行加库存的具体操作</span><br><span class="line">    &#125;</span><br><span class="line">      public String toString() &#123;</span><br><span class="line">        return &quot;Revert State&quot;;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>前面用到了 context.setState(this)，我们来看看怎么定义 Context 类：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">public class Context &#123;</span><br><span class="line">    private State state;</span><br><span class="line">      private String name;</span><br><span class="line">      public Context(String name) &#123;</span><br><span class="line">        this.name &#x3D; name;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">      public void setState(State state) &#123;</span><br><span class="line">        this.state &#x3D; state;</span><br><span class="line">    &#125;</span><br><span class="line">      public void getState() &#123;</span><br><span class="line">        return this.state;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>我们来看下客户端调用，大家就一清二楚了：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">public static void main(String[] args) &#123;</span><br><span class="line">    &#x2F;&#x2F; 我们需要操作的是 iPhone X</span><br><span class="line">    Context context &#x3D; new Context(&quot;iPhone X&quot;);</span><br><span class="line"></span><br><span class="line">    &#x2F;&#x2F; 看看怎么进行补库存操作</span><br><span class="line">      State revertState &#x3D; new RevertState();</span><br><span class="line">      revertState.doAction(context);</span><br><span class="line"></span><br><span class="line">    &#x2F;&#x2F; 同样的，减库存操作也非常简单</span><br><span class="line">      State deductState &#x3D; new DeductState();</span><br><span class="line">      deductState.doAction(context);</span><br><span class="line"></span><br><span class="line">      &#x2F;&#x2F; 如果需要我们可以获取当前的状态</span><br><span class="line">    &#x2F;&#x2F; context.getState().toString();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>如果我们不关心当前 context 处于什么状态，那么 Context 就可以不用维护 state 属性了，那样代码会简单很多。<br>不过，商品库存这个例子毕竟只是个例，我们还有很多实例是需要知道当前 context 处于什么状态的。</p>
]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>编程</tag>
      </tags>
  </entry>
  <entry>
    <title>进阶之路—设计模式（六）</title>
    <url>/Java/%E8%BF%9B%E9%98%B6%E4%B9%8B%E8%B7%AF-%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F%EF%BC%88%E5%85%AD%EF%BC%89/</url>
    <content><![CDATA[<h1 id="策略模式"><a href="#策略模式" class="headerlink" title="策略模式"></a>策略模式</h1><p>策略模式同样也是最常用的设计模式之一<br>下面设计的场景是，我们需要画一个图形，可选的策略就是用红色笔来画，还是绿色笔来画，或者蓝色笔来画。</p>
<p>首先，先定义一个策略接口：</p>
<a id="more"></a>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">public interface Strategy &#123;</span><br><span class="line">   public void draw(int radius, int x, int y);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>然后我们定义具体的几个策略：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">public class RedPen implements Strategy &#123;</span><br><span class="line">   @Override</span><br><span class="line">   public void draw(int radius, int x, int y) &#123;</span><br><span class="line">      System.out.println(&quot;用红色笔画图，radius:&quot; + radius + &quot;, x:&quot; + x + &quot;, y:&quot; + y);</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br><span class="line">public class GreenPen implements Strategy &#123;</span><br><span class="line">   @Override</span><br><span class="line">   public void draw(int radius, int x, int y) &#123;</span><br><span class="line">      System.out.println(&quot;用绿色笔画图，radius:&quot; + radius + &quot;, x:&quot; + x + &quot;, y:&quot; + y);</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br><span class="line">public class BluePen implements Strategy &#123;</span><br><span class="line">   @Override</span><br><span class="line">   public void draw(int radius, int x, int y) &#123;</span><br><span class="line">      System.out.println(&quot;用蓝色笔画图，radius:&quot; + radius + &quot;, x:&quot; + x + &quot;, y:&quot; + y);</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>使用策略的类：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">public class Context &#123;</span><br><span class="line">   private Strategy strategy;</span><br><span class="line"></span><br><span class="line">   public Context(Strategy strategy)&#123;</span><br><span class="line">      this.strategy &#x3D; strategy;</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   public int executeDraw(int radius, int x, int y)&#123;</span><br><span class="line">      return strategy.draw(radius, x, y);</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>客户端演示：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">public static void main(String[] args) &#123;</span><br><span class="line">    Context context &#x3D; new Context(new BluePen()); &#x2F;&#x2F; 使用绿色笔来画</span><br><span class="line">      context.executeDraw(10, 0, 0);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>放到一张图上，让大家看得清晰些：<br><img src="/images/celv1.png" alt="alt"><br>这个时候，大家有没有联想到结构型模式中的桥梁模式，它们其实非常相似，我把桥梁模式的图拿过来大家对比下：<br><img src="/images/celv2.png" alt="alt"><br>要我说的话，它们非常相似，桥梁模式在左侧加了一层抽象而已。桥梁模式的耦合更低，结构更复杂一些。</p>
<h1 id="观察者模式"><a href="#观察者模式" class="headerlink" title="观察者模式"></a>观察者模式</h1><p>观察者模式相对简单。无外乎两个操作，观察者订阅自己关心的主题和主题有数据变化后通知观察者们。</p>
<p>首先，需要定义主题，每个主题需要持有观察者列表的引用，用于在数据变更的时候通知各个观察者：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">public class Subject &#123;</span><br><span class="line"></span><br><span class="line">   private List&lt;Observer&gt; observers &#x3D; new ArrayList&lt;Observer&gt;();</span><br><span class="line">   private int state;</span><br><span class="line"></span><br><span class="line">   public int getState() &#123;</span><br><span class="line">      return state;</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   public void setState(int state) &#123;</span><br><span class="line">      this.state &#x3D; state;</span><br><span class="line">      &#x2F;&#x2F; 数据已变更，通知观察者们</span><br><span class="line">      notifyAllObservers();</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   public void attach(Observer observer)&#123;</span><br><span class="line">      observers.add(observer);        </span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   &#x2F;&#x2F; 通知观察者们</span><br><span class="line">   public void notifyAllObservers()&#123;</span><br><span class="line">      for (Observer observer : observers) &#123;</span><br><span class="line">         observer.update();</span><br><span class="line">      &#125;</span><br><span class="line">   &#125;     </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>定义观察者接口：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">public abstract class Observer &#123;</span><br><span class="line">   protected Subject subject;</span><br><span class="line">   public abstract void update();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>其实如果只有一个观察者类的话，接口都不用定义了，不过，通常场景下，既然用到了观察者模式，我们就是希望一个事件出来了，会有多个不同的类需要处理相应的信息。比如，订单修改成功事件，我们希望发短信的类得到通知、发邮件的类得到通知、处理物流信息的类得到通知等。</p>
<p>我们来定义具体的几个观察者类：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">public class BinaryObserver extends Observer &#123;</span><br><span class="line"></span><br><span class="line">      &#x2F;&#x2F; 在构造方法中进行订阅主题</span><br><span class="line">    public BinaryObserver(Subject subject) &#123;</span><br><span class="line">        this.subject &#x3D; subject;</span><br><span class="line">        &#x2F;&#x2F; 通常在构造方法中将 this 发布出去的操作一定要小心</span><br><span class="line">        this.subject.attach(this);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">      &#x2F;&#x2F; 该方法由主题类在数据变更的时候进行调用</span><br><span class="line">    @Override</span><br><span class="line">    public void update() &#123;</span><br><span class="line">        String result &#x3D; Integer.toBinaryString(subject.getState());</span><br><span class="line">        System.out.println(&quot;订阅的数据发生变化，新的数据处理为二进制值为：&quot; + result);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">public class HexaObserver extends Observer &#123;</span><br><span class="line"></span><br><span class="line">    public HexaObserver(Subject subject) &#123;</span><br><span class="line">        this.subject &#x3D; subject;</span><br><span class="line">        this.subject.attach(this);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    @Override</span><br><span class="line">    public void update() &#123;</span><br><span class="line">          String result &#x3D; Integer.toHexString(subject.getState()).toUpperCase();</span><br><span class="line">        System.out.println(&quot;订阅的数据发生变化，新的数据处理为十六进制值为：&quot; + result);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>客户端使用也非常简单：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">public static void main(String[] args) &#123;</span><br><span class="line">    &#x2F;&#x2F; 先定义一个主题</span><br><span class="line">      Subject subject1 &#x3D; new Subject();</span><br><span class="line">      &#x2F;&#x2F; 定义观察者</span><br><span class="line">      new BinaryObserver(subject1);</span><br><span class="line">      new HexaObserver(subject1);</span><br><span class="line"></span><br><span class="line">      &#x2F;&#x2F; 模拟数据变更，这个时候，观察者们的 update 方法将会被调用</span><br><span class="line">      subject.setState(11);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>实际生产过程中，观察者模式往往用消息中间件来实现</p>
]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>编程</tag>
      </tags>
  </entry>
  <entry>
    <title>进阶之路—设计模式（五）</title>
    <url>/Java/%E8%BF%9B%E9%98%B6%E4%B9%8B%E8%B7%AF-%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F%EF%BC%88%E4%BA%94%EF%BC%89/</url>
    <content><![CDATA[<h1 id="装饰模式"><a href="#装饰模式" class="headerlink" title="装饰模式"></a>装饰模式</h1><p>要把装饰模式说清楚明白，不是件容易的事情。 Java IO 中的几个类是典型的装饰模式的应用。</p>
<p>首先，我们先看一个简单的图，看这个图的时候，了解下层次结构就可以了：</p>
<a id="more"></a>
<p><img src="/images/zhuangshi.png" alt="alt"><br>我们来说说装饰模式的出发点，从图中可以看到，接口 Component其实已经有了 ConcreteComponentA 和 ConcreteComponentB 两个实现类了，但是，如果我们要增强这两个实现类的话，我们就可以采用装饰模式，用具体的装饰器来装饰实现类，以达到增强的目的。<br>从名字来简单解释下装饰器。既然说是装饰，那么往往就是添加小功能这种，而且，我们要满足可以添加多个小功能。最简单的，代理模式就可以实现功能的增强，但是代理不容易实现多个功能的增强，当然你可以说用代理包装代理的方式，但是那样的话代码就复杂了。</p>
<p>首先明白一些简单的概念，从图中我们看到，所有的具体装饰者们 ConcreteDecorator</p>
<p>都可以作为 Component 来使用，因为它们都实现了 Component 中的所有接口。它们和 Component 实现类 ConcreteComponent 的区别是，它们只是装饰者，起装饰作用，也就是即使它们看上去牛逼轰轰，但是它们都只是在具体的实现中加了层皮来装饰而已。</p>
<p>下面来看看一个例子，先把装饰模式弄清楚，然后再介绍下 java io 中的装饰模式的应用。</p>
<p>最近大街上流行起来了“快乐柠檬”，我们把快乐柠檬的饮料分为三类：红茶、绿茶、咖啡，在这三大类的基础上，又增加了许多的口味，什么金桔柠檬红茶、金桔柠檬珍珠绿茶、芒果红茶、芒果绿茶、芒果珍珠红茶、烤珍珠红茶、烤珍珠芒果绿茶、椰香胚芽咖啡、焦糖可可咖啡等等，每家店都有很长的菜单，但是仔细看下，其实原料也没几样，但是可以搭配出很多组合，如果顾客需要，很多没出现在菜单中的饮料他们也是可以做的。</p>
<p>在这个例子中，红茶、绿茶、咖啡是最基础的饮料，其他的像金桔柠檬、芒果、珍珠、椰果、焦糖等都属于装饰用的。当然，在开发中，我们确实可以像门店一样，开发这些类：LemonBlackTea、LemonGreenTea、MangoBlackTea、MangoLemonGreenTea……但是，很快我们就发现，这样子干肯定是不行的，这会导致我们需要组合出所有的可能，而且如果客人需要在红茶中加双份柠檬怎么办？三份柠檬怎么办？万一有个变态要四份柠檬，所以这种做法是给自己找加班的。</p>
<p>不说废话了，上代码。</p>
<p>首先，定义饮料抽象基类：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">public abstract class Beverage &#123;</span><br><span class="line">      &#x2F;&#x2F; 返回描述</span><br><span class="line">      public abstract String getDescription();</span><br><span class="line">      &#x2F;&#x2F; 返回价格</span><br><span class="line">      public abstract double cost();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>然后是三个基础饮料实现类，红茶、绿茶和咖啡：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">public class BlackTea extends Beverage &#123;</span><br><span class="line">      public String getDescription() &#123;</span><br><span class="line">        return &quot;红茶&quot;;</span><br><span class="line">    &#125;</span><br><span class="line">      public double cost() &#123;</span><br><span class="line">        return 10;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line">public class GreenTea extends Beverage &#123;</span><br><span class="line">    public String getDescription() &#123;</span><br><span class="line">        return &quot;绿茶&quot;;</span><br><span class="line">    &#125;</span><br><span class="line">      public double cost() &#123;</span><br><span class="line">        return 11;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line">...&#x2F;&#x2F; 咖啡省略</span><br></pre></td></tr></table></figure>
<p>定义调料，也就是装饰者的基类，此类必须继承自 Beverage：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">&#x2F;&#x2F; 调料</span><br><span class="line">public abstract class Condiment extends Beverage &#123;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>然后我们来定义柠檬、芒果等具体的调料，它们属于装饰者，毫无疑问，这些调料肯定都需要继承 Condiment 类：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">public class Lemon extends Condiment &#123;</span><br><span class="line">    private Beverage bevarage;</span><br><span class="line">      &#x2F;&#x2F; 这里很关键，需要传入具体的饮料，如需要传入没有被装饰的红茶或绿茶，</span><br><span class="line">      &#x2F;&#x2F; 当然也可以传入已经装饰好的芒果绿茶，这样可以做芒果柠檬绿茶</span><br><span class="line">      public Lemon(Beverage bevarage) &#123;</span><br><span class="line">        this.bevarage &#x3D; bevarage;</span><br><span class="line">    &#125;</span><br><span class="line">      public String getDescription() &#123;</span><br><span class="line">        &#x2F;&#x2F; 装饰</span><br><span class="line">        return bevarage.getDescription() + &quot;, 加柠檬&quot;;</span><br><span class="line">    &#125;</span><br><span class="line">      public double cost() &#123;</span><br><span class="line">          &#x2F;&#x2F; 装饰</span><br><span class="line">        return beverage.cost() + 2; &#x2F;&#x2F; 加柠檬需要 2 元</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line">public class Mango extends Condiment &#123;</span><br><span class="line">    private Beverage bevarage;</span><br><span class="line">      public Mango(Beverage bevarage) &#123;</span><br><span class="line">        this.bevarage &#x3D; bevarage;</span><br><span class="line">    &#125;</span><br><span class="line">      public String getDescription() &#123;</span><br><span class="line">        return bevarage.getDescription() + &quot;, 加芒果&quot;;</span><br><span class="line">    &#125;</span><br><span class="line">      public double cost() &#123;</span><br><span class="line">        return beverage.cost() + 3; &#x2F;&#x2F; 加芒果需要 3 元</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line">...&#x2F;&#x2F; 给每一种调料都加一个类</span><br></pre></td></tr></table></figure>
<p>看客户端调用</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">public static void main(String[] args) &#123;</span><br><span class="line">      &#x2F;&#x2F; 首先，我们需要一个基础饮料，红茶、绿茶或咖啡</span><br><span class="line">    Beverage beverage &#x3D; new GreenTea();</span><br><span class="line">      &#x2F;&#x2F; 开始装饰</span><br><span class="line">      beverage &#x3D; new Lemon(beverage); &#x2F;&#x2F; 先加一份柠檬</span><br><span class="line">      beverage &#x3D; new Mongo(beverage); &#x2F;&#x2F; 再加一份芒果</span><br><span class="line"></span><br><span class="line">      System.out.println(beverage.getDescription() + &quot; 价格：￥&quot; + beverage.cost());</span><br><span class="line">      &#x2F;&#x2F;&quot;绿茶, 加柠檬, 加芒果 价格：￥16&quot;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>如果我们需要芒果珍珠双份柠檬红茶：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">Beverage beverage &#x3D; new Mongo(new Pearl(new Lemon(new Lemon(new BlackTea()))));</span><br></pre></td></tr></table></figure>
<p>是不是很变态？</p>
<p>看看下图可能会清晰一些：<br><img src="/images/zhuangshi2.png" alt="alt"><br>到这里，大家应该已经清楚装饰模式了吧。<br>下面，我们再来说说 java IO 中的装饰模式。看下图 InputStream 派生出来的部分类<br><img src="/images/zhuangshi3.png" alt="alt"><br>我们知道 InputStream 代表了输入流，具体的输入来源可以是文件（FileInputStream）、管道（PipedInputStream）、数组（ByteArrayInputStream）等，这些就像前面奶茶的例子中的红茶、绿茶，属于基础输入流。</p>
<p>FilterInputStream 承接了装饰模式的关键节点，其实现类是一系列装饰器，比如 BufferedInputStream 代表用缓冲来装饰，也就使得输入流具有了缓冲的功能，LineNumberInputStream 代表用行号来装饰，在操作的时候就可以取得行号了，DataInputStream 的装饰，使得我们可以从输入流转换为 java 中的基本类型值。</p>
<h1 id="享元模式"><a href="#享元模式" class="headerlink" title="享元模式"></a>享元模式</h1><p>门英文是 Flyweight Pattern，不知道是谁最先翻译的这个词，感觉这翻译真的不好理解，我们试着强行关联起来吧。Flyweight 是轻量级的意思，享元分开来说就是 共享 元器件，也就是复用已经生成的对象，这种做法当然也就是轻量级的了。</p>
<p>复用对象最简单的方式是，用一个 HashMap 来存放每次新生成的对象。每次需要一个对象的时候，先到 HashMap 中看看有没有，如果没有，再生成新的对象，然后将这个对象放入 HashMap 中。<br>门面模式的优点显而易见，客户端不再需要关注实例化时应该使用哪个实现类，直接调用门面提供的方法就可以了，因为门面类提供的方法的方法名对于客户端来说已经很友好了。</p>
]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>编程</tag>
      </tags>
  </entry>
  <entry>
    <title>进阶之路—设计模式（四）</title>
    <url>/Java/%E8%BF%9B%E9%98%B6%E4%B9%8B%E8%B7%AF-%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F%EF%BC%88%E5%9B%9B%EF%BC%89/</url>
    <content><![CDATA[<h1 id="桥梁模式"><a href="#桥梁模式" class="headerlink" title="桥梁模式"></a>桥梁模式</h1><p>理解桥梁模式，其实就是理解代码抽象和解耦。</p>
<p>我们首先需要一个桥梁，它是一个接口，定义提供的接口方法。</p>
<a id="more"></a>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">public interface DrawAPI &#123;</span><br><span class="line">   public void draw(int radius, int x, int y);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>然后是一系列实现类：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">public class RedPen implements DrawAPI &#123;</span><br><span class="line">   @Override</span><br><span class="line">   public void draw(int radius, int x, int y) &#123;</span><br><span class="line">      System.out.println(&quot;用红色笔画图，radius:&quot; + radius + &quot;, x:&quot; + x + &quot;, y:&quot; + y);</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br><span class="line">public class GreenPen implements DrawAPI &#123;</span><br><span class="line">   @Override</span><br><span class="line">   public void draw(int radius, int x, int y) &#123;</span><br><span class="line">      System.out.println(&quot;用绿色笔画图，radius:&quot; + radius + &quot;, x:&quot; + x + &quot;, y:&quot; + y);</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br><span class="line">public class BluePen implements DrawAPI &#123;</span><br><span class="line">   @Override</span><br><span class="line">   public void draw(int radius, int x, int y) &#123;</span><br><span class="line">      System.out.println(&quot;用蓝色笔画图，radius:&quot; + radius + &quot;, x:&quot; + x + &quot;, y:&quot; + y);</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>定义一个抽象类，此类的实现类都需要使用 DrawAPI：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">public abstract class Shape &#123;</span><br><span class="line">   protected DrawAPI drawAPI;</span><br><span class="line"></span><br><span class="line">   protected Shape(DrawAPI drawAPI)&#123;</span><br><span class="line">      this.drawAPI &#x3D; drawAPI;</span><br><span class="line">   &#125;</span><br><span class="line">   public abstract void draw();    </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>定义抽象类的子类：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">&#x2F;&#x2F; 圆形</span><br><span class="line">public class Circle extends Shape &#123;</span><br><span class="line">   private int radius;</span><br><span class="line"></span><br><span class="line">   public Circle(int radius, DrawAPI drawAPI) &#123;</span><br><span class="line">      super(drawAPI);</span><br><span class="line">      this.radius &#x3D; radius;</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   public void draw() &#123;</span><br><span class="line">      drawAPI.draw(radius, 0, 0);</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br><span class="line">&#x2F;&#x2F; 长方形</span><br><span class="line">public class Rectangle extends Shape &#123;</span><br><span class="line">    private int x;</span><br><span class="line">      private int y;</span><br><span class="line"></span><br><span class="line">      public Rectangle(int x, int y, DrawAPI drawAPI) &#123;</span><br><span class="line">        super(drawAPI);</span><br><span class="line">          this.x &#x3D; x;</span><br><span class="line">          this.y &#x3D; y;</span><br><span class="line">    &#125;</span><br><span class="line">      public void draw() &#123;</span><br><span class="line">      drawAPI.draw(0, x, y);</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>最后，我们来看客户端演示：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">public static void main(String[] args) &#123;</span><br><span class="line">    Shape greenCircle &#x3D; new Circle(10, new GreenPen());</span><br><span class="line">      Shape redRectangle &#x3D; new Rectangle(4, 8, new RedPen());</span><br><span class="line"></span><br><span class="line">      greenCircle.draw();</span><br><span class="line">      redRectangle.draw();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>可能大家看上面一步步还不是特别清晰，我把所有的东西整合到一张图上：<br><img src="/images/qiaoliang.png" alt="alt"><br>桥梁模式的优点也是显而易见的，就是非常容易进行扩展。</p>
<h1 id="门面模式"><a href="#门面模式" class="headerlink" title="门面模式"></a>门面模式</h1><p>门面模式（也叫外观模式，Facade Pattern）在许多源码中有使用，比如 slf4j 就可以理解为是门面模式的应用。这是一个简单的设计模式，我们直接上代码再说吧。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">public interface Shape &#123;</span><br><span class="line">   void draw();</span><br><span class="line">&#125;</span><br><span class="line">public class Circle implements Shape &#123;</span><br><span class="line"></span><br><span class="line">   @Override</span><br><span class="line">   public void draw() &#123;</span><br><span class="line">      System.out.println(&quot;Circle::draw()&quot;);</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">public class Rectangle implements Shape &#123;</span><br><span class="line"></span><br><span class="line">   @Override</span><br><span class="line">   public void draw() &#123;</span><br><span class="line">      System.out.println(&quot;Rectangle::draw()&quot;);</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>客户端调用：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">public static void main(String[] args) &#123;</span><br><span class="line">    &#x2F;&#x2F; 画一个圆形</span><br><span class="line">      Shape circle &#x3D; new Circle();</span><br><span class="line">      circle.draw();</span><br><span class="line"></span><br><span class="line">      &#x2F;&#x2F; 画一个长方形</span><br><span class="line">      Shape rectangle &#x3D; new Rectangle();</span><br><span class="line">      rectangle.draw();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>以上是我们常写的代码，我们需要画圆就要先实例化圆，画长方形就需要先实例化一个长方形，然后再调用相应的 draw() 方法。</p>
<p>下面，我们看看怎么用门面模式来让客户端调用更加友好一些。</p>
<p>我们先定义一个门面：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">public class ShapeMaker &#123;</span><br><span class="line">   private Shape circle;</span><br><span class="line">   private Shape rectangle;</span><br><span class="line">   private Shape square;</span><br><span class="line"></span><br><span class="line">   public ShapeMaker() &#123;</span><br><span class="line">      circle &#x3D; new Circle();</span><br><span class="line">      rectangle &#x3D; new Rectangle();</span><br><span class="line">      square &#x3D; new Square();</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">  &#x2F;**</span><br><span class="line">   * 下面定义一堆方法，具体应该调用什么方法，由这个门面来决定</span><br><span class="line">   *&#x2F;</span><br><span class="line"></span><br><span class="line">   public void drawCircle()&#123;</span><br><span class="line">      circle.draw();</span><br><span class="line">   &#125;</span><br><span class="line">   public void drawRectangle()&#123;</span><br><span class="line">      rectangle.draw();</span><br><span class="line">   &#125;</span><br><span class="line">   public void drawSquare()&#123;</span><br><span class="line">      square.draw();</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>看看现在客户端怎么调用：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">public static void main(String[] args) &#123;</span><br><span class="line">  ShapeMaker shapeMaker &#x3D; new ShapeMaker();</span><br><span class="line"></span><br><span class="line">  &#x2F;&#x2F; 客户端调用现在更加清晰了</span><br><span class="line">  shapeMaker.drawCircle();</span><br><span class="line">  shapeMaker.drawRectangle();</span><br><span class="line">  shapeMaker.drawSquare();        </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>门面模式的优点显而易见，客户端不再需要关注实例化时应该使用哪个实现类，直接调用门面提供的方法就可以了，因为门面类提供的方法的方法名对于客户端来说已经很友好了。</p>
<h1 id="组合模式"><a href="#组合模式" class="headerlink" title="组合模式"></a>组合模式</h1><p>组合模式用于表示具有层次结构的数据，使得我们对单个对象和组合对象的访问具有一致性。<br>直接看一个例子吧，每个员工都有姓名、部门、薪水这些属性，同时还有下属员工集合（虽然可能集合为空），而下属员工和自己的结构是一样的，也有姓名、部门这些属性，同时也有他们的下属员工集合。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">public class Employee &#123;</span><br><span class="line">   private String name;</span><br><span class="line">   private String dept;</span><br><span class="line">   private int salary;</span><br><span class="line">   private List&lt;Employee&gt; subordinates; &#x2F;&#x2F; 下属</span><br><span class="line"></span><br><span class="line">   public Employee(String name,String dept, int sal) &#123;</span><br><span class="line">      this.name &#x3D; name;</span><br><span class="line">      this.dept &#x3D; dept;</span><br><span class="line">      this.salary &#x3D; sal;</span><br><span class="line">      subordinates &#x3D; new ArrayList&lt;Employee&gt;();</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   public void add(Employee e) &#123;</span><br><span class="line">      subordinates.add(e);</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   public void remove(Employee e) &#123;</span><br><span class="line">      subordinates.remove(e);</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   public List&lt;Employee&gt; getSubordinates()&#123;</span><br><span class="line">     return subordinates;</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   public String toString()&#123;</span><br><span class="line">      return (&quot;Employee :[ Name : &quot; + name + &quot;, dept : &quot; + dept + &quot;, salary :&quot; + salary+&quot; ]&quot;);</span><br><span class="line">   &#125;   </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>通常，这种类需要定义 add(node)、remove(node)、getChildren() 这些方法。</p>
]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>编程</tag>
      </tags>
  </entry>
  <entry>
    <title>进阶之路—设计模式（三）</title>
    <url>/Java/%E8%BF%9B%E9%98%B6%E4%B9%8B%E8%B7%AF-%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F%EF%BC%88%E4%B8%89%EF%BC%89/</url>
    <content><![CDATA[<h1 id="代理模式"><a href="#代理模式" class="headerlink" title="代理模式"></a>代理模式</h1><p>代理模式也是最为常用的设计模式之一，用一个代理来隐藏具体实现类的实现细节，通常还用于在真实的实现的前后添加一部分逻辑。<br>既然说是代理，那就要对客户端隐藏真实实现，由代理来负责客户端的所有请求。当然，代理只是个代理，它不会完成实际的业务逻辑，而是一层皮而已，但是对于客户端来说，它必须表现得就是客户端需要的真实实现。</p>
<a id="more"></a>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">public interface FoodService &#123;</span><br><span class="line">    Food makeChicken();</span><br><span class="line">    Food makeNoodle();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">public class FoodServiceImpl implements FoodService &#123;</span><br><span class="line">    public Food makeChicken() &#123;</span><br><span class="line">          Food f &#x3D; new Chicken()</span><br><span class="line">        f.setChicken(&quot;1kg&quot;);</span><br><span class="line">          f.setSpicy(&quot;1g&quot;);</span><br><span class="line">          f.setSalt(&quot;3g&quot;);</span><br><span class="line">        return f;</span><br><span class="line">    &#125;</span><br><span class="line">    public Food makeNoodle() &#123;</span><br><span class="line">        Food f &#x3D; new Noodle();</span><br><span class="line">        f.setNoodle(&quot;500g&quot;);</span><br><span class="line">        f.setSalt(&quot;5g&quot;);</span><br><span class="line">        return f;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">&#x2F;&#x2F; 代理要表现得“就像是”真实实现类，所以需要实现 FoodService</span><br><span class="line">public class FoodServiceProxy implements FoodService &#123;</span><br><span class="line"></span><br><span class="line">    &#x2F;&#x2F; 内部一定要有一个真实的实现类，当然也可以通过构造方法注入</span><br><span class="line">    private FoodService foodService &#x3D; new FoodServiceImpl();</span><br><span class="line"></span><br><span class="line">    public Food makeChicken() &#123;</span><br><span class="line">        System.out.println(&quot;我们马上要开始制作鸡肉了&quot;);</span><br><span class="line"></span><br><span class="line">        &#x2F;&#x2F; 如果我们定义这句为核心代码的话，那么，核心代码是真实实现类做的，</span><br><span class="line">        &#x2F;&#x2F; 代理只是在核心代码前后做些“无足轻重”的事情</span><br><span class="line">        Food food &#x3D; foodService.makeChicken();</span><br><span class="line"></span><br><span class="line">        System.out.println(&quot;鸡肉制作完成啦，加点胡椒粉&quot;); &#x2F;&#x2F; 增强</span><br><span class="line">          food.addCondiment(&quot;pepper&quot;);</span><br><span class="line"></span><br><span class="line">        return food;</span><br><span class="line">    &#125;</span><br><span class="line">    public Food makeNoodle() &#123;</span><br><span class="line">        System.out.println(&quot;准备制作拉面~&quot;);</span><br><span class="line">        Food food &#x3D; foodService.makeNoodle();</span><br><span class="line">        System.out.println(&quot;制作完成啦&quot;)</span><br><span class="line">        return food;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>客户端调用，注意，我们要用代理来实例化接口：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">&#x2F;&#x2F; 这里用代理类来实例化</span><br><span class="line">FoodService foodService &#x3D; new FoodServiceProxy();</span><br><span class="line">foodService.makeChicken();</span><br></pre></td></tr></table></figure>
<p><img src="/images/daili.png" alt="alt"><br>代理模式说白了就是做 “方法包装” 或做 “方法增强”。在面向切面编程中，其实就是动态代理的过程。比如 Spring 中，我们自己不定义代理类，但是 Spring 会帮我们动态来定义代理，然后把我们定义在 @Before、@After、@Around 中的代码逻辑动态添加到代理中。</p>
<p>说到动态代理，又可以展开说 …… Spring 中实现动态代理有两种，一种是如果我们的类定义了接口，如 UserService 接口和 UserServiceImpl 实现，那么采用 JDK 的动态代理，感兴趣的读者可以去看看 java.lang.reflect.Proxy 类的源码；另一种是我们自己没有定义接口的，Spring 会采用 CGLIB 进行动态代理，它是一个 jar 包，性能还不错。</p>
<h1 id="适配器模式"><a href="#适配器模式" class="headerlink" title="适配器模式"></a>适配器模式</h1><p>说完代理模式，说适配器模式，是因为它们很相似，这里可以做个比较。<br>适配器模式做的就是，有一个接口需要实现，但是我们现成的对象都不满足，需要加一层适配器来进行适配<br>适配器模式总体来说分三种：默认适配器模式、对象适配器模式、类适配器模式。先不急着分清楚这几个，先看看例子再说。</p>
<h2 id="默认适配器模式"><a href="#默认适配器模式" class="headerlink" title="默认适配器模式"></a>默认适配器模式</h2><p>首先，我们先看看最简单的适配器模式默认适配器模式(Default Adapter)是怎么样的。<br>我们用 Appache commons-io 包中的 FileAlterationListener 做例子，此接口定义了很多的方法，用于对文件或文件夹进行监控，一旦发生了对应的操作，就会触发相应的方法。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">public interface FileAlterationListener &#123;</span><br><span class="line">    void onStart(final FileAlterationObserver observer);</span><br><span class="line">    void onDirectoryCreate(final File directory);</span><br><span class="line">    void onDirectoryChange(final File directory);</span><br><span class="line">    void onDirectoryDelete(final File directory);</span><br><span class="line">    void onFileCreate(final File file);</span><br><span class="line">    void onFileChange(final File file);</span><br><span class="line">    void onFileDelete(final File file);</span><br><span class="line">    void onStop(final FileAlterationObserver observer);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>此接口的一大问题是抽象方法太多了，如果我们要用这个接口，意味着我们要实现每一个抽象方法，如果我们只是想要监控文件夹中的文件创建和文件删除事件，可是我们还是不得不实现所有的方法，很明显，这不是我们想要的。</p>
<p>所以，我们需要下面的一个适配器，它用于实现上面的接口，但是所有的方法都是空方法，这样，我们就可以转而定义自己的类来继承下面这个类即可。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">public class FileAlterationListenerAdaptor implements FileAlterationListener &#123;</span><br><span class="line"></span><br><span class="line">    public void onStart(final FileAlterationObserver observer) &#123;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public void onDirectoryCreate(final File directory) &#123;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public void onDirectoryChange(final File directory) &#123;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public void onDirectoryDelete(final File directory) &#123;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public void onFileCreate(final File file) &#123;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public void onFileChange(final File file) &#123;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public void onFileDelete(final File file) &#123;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public void onStop(final FileAlterationObserver observer) &#123;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>比如我们可以定义以下类，我们仅仅需要实现我们想实现的方法就可以了：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">public class FileMonitor extends FileAlterationListenerAdaptor &#123;</span><br><span class="line">    public void onFileCreate(final File file) &#123;</span><br><span class="line">        &#x2F;&#x2F; 文件创建</span><br><span class="line">        doSomething();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public void onFileDelete(final File file) &#123;</span><br><span class="line">        &#x2F;&#x2F; 文件删除</span><br><span class="line">        doSomething();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>当然，上面说的只是适配器模式的其中一种，也是最简单的一种，无需多言。下面，再介绍“正统的”适配器模式。</p>
<h2 id="对象适配器模式"><a href="#对象适配器模式" class="headerlink" title="对象适配器模式"></a>对象适配器模式</h2><p>来看一个《Head First 设计模式》中的一个例子，我稍微修改了一下，看看怎么将鸡适配成鸭，这样鸡也能当鸭来用。因为，现在鸭这个接口，我们没有合适的实现类可以用，所以需要适配器。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">public interface Duck &#123;</span><br><span class="line">    public void quack(); &#x2F;&#x2F; 鸭的呱呱叫</span><br><span class="line">      public void fly(); &#x2F;&#x2F; 飞</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">public interface Cock &#123;</span><br><span class="line">    public void gobble(); &#x2F;&#x2F; 鸡的咕咕叫</span><br><span class="line">      public void fly(); &#x2F;&#x2F; 飞</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">public class WildCock implements Cock &#123;</span><br><span class="line">    public void gobble() &#123;</span><br><span class="line">        System.out.println(&quot;咕咕叫&quot;);</span><br><span class="line">    &#125;</span><br><span class="line">      public void fly() &#123;</span><br><span class="line">        System.out.println(&quot;鸡也会飞哦&quot;);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>鸭接口有 fly() 和 quare() 两个方法，鸡 Cock 如果要冒充鸭，fly() 方法是现成的，但是鸡不会鸭的呱呱叫，没有 quack() 方法。这个时候就需要适配了：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">&#x2F;&#x2F; 毫无疑问，首先，这个适配器肯定需要 implements Duck，这样才能当做鸭来用</span><br><span class="line">public class CockAdapter implements Duck &#123;</span><br><span class="line"></span><br><span class="line">    Cock cock;</span><br><span class="line">    &#x2F;&#x2F; 构造方法中需要一个鸡的实例，此类就是将这只鸡适配成鸭来用</span><br><span class="line">      public CockAdapter(Cock cock) &#123;</span><br><span class="line">        this.cock &#x3D; cock;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    &#x2F;&#x2F; 实现鸭的呱呱叫方法</span><br><span class="line">      @Override</span><br><span class="line">      public void quack() &#123;</span><br><span class="line">        &#x2F;&#x2F; 内部其实是一只鸡的咕咕叫</span><br><span class="line">        cock.gobble();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">      @Override</span><br><span class="line">      public void fly() &#123;</span><br><span class="line">        cock.fly();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>客户端调用很简单了：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">public static void main(String[] args) &#123;</span><br><span class="line">    &#x2F;&#x2F; 有一只野鸡</span><br><span class="line">      Cock wildCock &#x3D; new WildCock();</span><br><span class="line">      &#x2F;&#x2F; 成功将野鸡适配成鸭</span><br><span class="line">      Duck duck &#x3D; new CockAdapter(wildCock);</span><br><span class="line">      ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>到这里，大家也就知道了适配器模式是怎么回事了。无非是我们需要一只鸭，但是我们只有一只鸡，这个时候就需要定义一个适配器，由这个适配器来充当鸭，但是适配器里面的方法还是由鸡来实现的。</p>
<p>我们用一个图来简单说明下：<br><img src="/images/shipeiqi.png" alt="alt"><br>上图应该还是很容易理解的，我就不做更多的解释了。下面，我们看看类适配模式怎么样的。</p>
<h2 id="类适配器模式"><a href="#类适配器模式" class="headerlink" title="类适配器模式"></a>类适配器模式</h2><p><img src="/images/shipeiqi2.png" alt="alt"><br>看到这个图，大家应该很容易理解的吧，通过继承的方法，适配器自动获得了所需要的大部分方法。这个时候，客户端使用更加简单，直接 Target t = new SomeAdapter(); 就可以了。</p>
<h2 id="适配器模式总结"><a href="#适配器模式总结" class="headerlink" title="适配器模式总结"></a>适配器模式总结</h2><p>类适配和对象适配的异同</p>
<p>一个采用继承，一个采用组合；</p>
<p>类适配属于静态实现，对象适配属于组合的动态实现，对象适配需要多实例化一个对象。</p>
<p>总体来说，对象适配用得比较多。</p>
<p>适配器模式和代理模式的异同<br>比较这两种模式，其实是比较对象适配器模式和代理模式，在代码结构上，它们很相似，都需要一个具体的实现类的实例。但是它们的目的不一样，代理模式做的是增强原方法的活；适配器做的是适配的活，为的是提供“把鸡包装成鸭，然后当做鸭来使用”，而鸡和鸭它们之间原本没有继承关系。<br><img src="/images/shipeiqi3.png" alt="alt"></p>
]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>编程</tag>
      </tags>
  </entry>
  <entry>
    <title>进阶之路—设计模式（二）</title>
    <url>/Java/%E8%BF%9B%E9%98%B6%E4%B9%8B%E8%B7%AF-%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F%EF%BC%88%E4%BA%8C%EF%BC%89/</url>
    <content><![CDATA[<h1 id="单例模式"><a href="#单例模式" class="headerlink" title="单例模式"></a>单例模式</h1><p>单例模式是最为常用的设计模式之一，同时也是大多数人理解错的最多的一种设计模式<br>饿汉模式：</p>
<a id="more"></a>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">public class Singleton &#123;</span><br><span class="line">    &#x2F;&#x2F; 首先，将 new Singleton() 堵死</span><br><span class="line">    private Singleton() &#123;&#125;;</span><br><span class="line">    &#x2F;&#x2F; 创建私有静态实例，意味着这个类第一次使用的时候就会进行创建</span><br><span class="line">    private static Singleton instance &#x3D; new Singleton();</span><br><span class="line"></span><br><span class="line">    public static Singleton getInstance() &#123;</span><br><span class="line">        return instance;</span><br><span class="line">    &#125;</span><br><span class="line">    &#x2F;&#x2F; 瞎写一个静态方法。这里想说的是，如果我们只是要调用 Singleton.getDate(...)，</span><br><span class="line">    &#x2F;&#x2F; 本来是不想要生成 Singleton 实例的，不过没办法，已经生成了</span><br><span class="line">    public static Date getDate(String mode) &#123;return new Date();&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>很多人都能说出饿汉模式的缺点，可是我觉得生产过程中，很少碰到这种情况：你定义了一个单例的类，不需要其实例，可是你却把一个或几个你会用到的静态方法塞到这个类中。<br>饱汉模式最容易出错：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">public class Singleton &#123;</span><br><span class="line">    &#x2F;&#x2F; 首先，也是先堵死 new Singleton() 这条路</span><br><span class="line">    private Singleton() &#123;&#125;</span><br><span class="line">    &#x2F;&#x2F; 和饿汉模式相比，这边不需要先实例化出来，注意这里的 volatile，它是必须的</span><br><span class="line">    private static volatile Singleton instance &#x3D; null;</span><br><span class="line"></span><br><span class="line">    public static Singleton getInstance() &#123;</span><br><span class="line">        if (instance &#x3D;&#x3D; null) &#123;</span><br><span class="line">            &#x2F;&#x2F; 加锁</span><br><span class="line">            synchronized (Singleton.class) &#123;</span><br><span class="line">                &#x2F;&#x2F; 这一次判断也是必须的，不然会有并发问题</span><br><span class="line">                if (instance &#x3D;&#x3D; null) &#123;</span><br><span class="line">                    instance &#x3D; new Singleton();</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        return instance;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>双重检查，指的是两次检查 instance 是否为 null，volatile 在这里是需要的。</p>
<h1 id="建造者模式"><a href="#建造者模式" class="headerlink" title="建造者模式"></a>建造者模式</h1><p>经常碰见的 XxxBuilder 的类，通常都是建造者模式的产物。建造者模式其实有很多的变种，但是对于客户端来说，我们的使用通常都是一个模式的<br>套路就是先 new 一个 Builder，然后可以链式地调用一堆方法，最后再调用一次 build() 方法，我们需要的对象就有了。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">class User &#123;</span><br><span class="line">    &#x2F;&#x2F; 下面是“一堆”的属性</span><br><span class="line">    private String name;</span><br><span class="line">    private String password;</span><br><span class="line">    private String nickName;</span><br><span class="line">    private int age;</span><br><span class="line"></span><br><span class="line">    &#x2F;&#x2F; 构造方法私有化，不然客户端就会直接调用构造方法了</span><br><span class="line">    private User(String name, String password, String nickName, int age) &#123;</span><br><span class="line">        this.name &#x3D; name;</span><br><span class="line">        this.password &#x3D; password;</span><br><span class="line">        this.nickName &#x3D; nickName;</span><br><span class="line">        this.age &#x3D; age;</span><br><span class="line">    &#125;</span><br><span class="line">    &#x2F;&#x2F; 静态方法，用于生成一个 Builder，这个不一定要有，不过写这个方法是一个很好的习惯，</span><br><span class="line">    &#x2F;&#x2F; 有些代码要求别人写 new User.UserBuilder().a()...build() 看上去就没那么好</span><br><span class="line">    public static UserBuilder builder() &#123;</span><br><span class="line">        return new UserBuilder();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public static class UserBuilder &#123;</span><br><span class="line">        &#x2F;&#x2F; 下面是和 User 一模一样的一堆属性</span><br><span class="line">        private String  name;</span><br><span class="line">        private String password;</span><br><span class="line">        private String nickName;</span><br><span class="line">        private int age;</span><br><span class="line"></span><br><span class="line">        private UserBuilder() &#123;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        &#x2F;&#x2F; 链式调用设置各个属性值，返回 this，即 UserBuilder</span><br><span class="line">        public UserBuilder name(String name) &#123;</span><br><span class="line">            this.name &#x3D; name;</span><br><span class="line">            return this;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        public UserBuilder password(String password) &#123;</span><br><span class="line">            this.password &#x3D; password;</span><br><span class="line">            return this;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        public UserBuilder nickName(String nickName) &#123;</span><br><span class="line">            this.nickName &#x3D; nickName;</span><br><span class="line">            return this;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        public UserBuilder age(int age) &#123;</span><br><span class="line">            this.age &#x3D; age;</span><br><span class="line">            return this;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        &#x2F;&#x2F; build() 方法负责将 UserBuilder 中设置好的属性“复制”到 User 中。</span><br><span class="line">        &#x2F;&#x2F; 当然，可以在 “复制” 之前做点检验</span><br><span class="line">        public User build() &#123;</span><br><span class="line">            if (name &#x3D;&#x3D; null || password &#x3D;&#x3D; null) &#123;</span><br><span class="line">                throw new RuntimeException(&quot;用户名和密码必填&quot;);</span><br><span class="line">            &#125;</span><br><span class="line">            if (age &lt;&#x3D; 0 || age &gt;&#x3D; 150) &#123;</span><br><span class="line">                throw new RuntimeException(&quot;年龄不合法&quot;);</span><br><span class="line">            &#125;</span><br><span class="line">            &#x2F;&#x2F; 还可以做赋予”默认值“的功能</span><br><span class="line">              if (nickName &#x3D;&#x3D; null) &#123;</span><br><span class="line">                nickName &#x3D; name;</span><br><span class="line">            &#125;</span><br><span class="line">            return new User(name, password, nickName, age);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>核心是：先把所有的属性都设置给 Builder，然后 build() 方法的时候，将这些属性复制给实际产生的对象。</p>
<p>看看客户端的调用：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">public class APP &#123;</span><br><span class="line">    public static void main(String[] args) &#123;</span><br><span class="line">        User d &#x3D; User.builder()</span><br><span class="line">                .name(&quot;foo&quot;)</span><br><span class="line">                .password(&quot;pAss12345&quot;)</span><br><span class="line">                .age(25)</span><br><span class="line">                .build();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>说实话，建造者模式的链式写法很吸引人，但是，多写了很多“无用”的 builder 的代码，感觉这个模式没什么用。不过，当属性很多，而且有些必填，有些选填的时候，这个模式会使代码清晰很多。我们可以在 Builder 的构造方法中强制让调用者提供必填字段，还有，在 build() 方法中校验各个参数比在 User 的构造方法中校验，代码要优雅一些。</p>
<h1 id="原型模式"><a href="#原型模式" class="headerlink" title="原型模式"></a>原型模式</h1><p>这是我要说的创建型模式的最后一个设计模式了。<br>原型模式很简单：有一个原型实例，基于这个原型实例产生新的实例，也就是“克隆”了。<br>Object 类中有一个 clone() 方法，它用于生成一个新的对象，当然，如果我们要调用这个方法，java 要求我们的类必须先实现 Cloneable 接口，此接口没有定义任何方法，但是不这么做的话，在 clone() 的时候，会抛出 CloneNotSupportedException 异常<br>java 的克隆是浅克隆，碰到对象引用的时候，克隆出来的对象和原对象中的引用将指向同一个对象。通常实现深克隆的方法是将对象进行序列化，然后再进行反序列化。</p>
<p>原型模式了解到这里我觉得就够了，各种变着法子说这种代码或那种代码是原型模式，没什么意义。</p>
]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>编程</tag>
      </tags>
  </entry>
  <entry>
    <title>进阶之路—设计模式（一）</title>
    <url>/Java/%E8%BF%9B%E9%98%B6%E4%B9%8B%E8%B7%AF-%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F%EF%BC%88%E4%B8%80%EF%BC%89/</url>
    <content><![CDATA[<h1 id="简单工厂模式"><a href="#简单工厂模式" class="headerlink" title="简单工厂模式"></a>简单工厂模式</h1><p>和名字一样简单，非常简单，直接上代码…</p>
<a id="more"></a>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">public class FoodFactory &#123;</span><br><span class="line"></span><br><span class="line">    public static Food makeFood(String name) &#123;</span><br><span class="line">        if (name.equals(&quot;noodle&quot;)) &#123;</span><br><span class="line">            Food noodle &#x3D; new LanZhouNoodle();</span><br><span class="line">            noodle.addSpicy(&quot;more&quot;);</span><br><span class="line">            return noodle;</span><br><span class="line">        &#125; else if (name.equals(&quot;chicken&quot;)) &#123;</span><br><span class="line">            Food chicken &#x3D; new HuangMenChicken();</span><br><span class="line">            chicken.addCondiment(&quot;potato&quot;);</span><br><span class="line">            return chicken;</span><br><span class="line">        &#125; else &#123;</span><br><span class="line">            return null;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>其中，LanZhouNoodle 和 HuangMenChicken 都继承自 Food。<br>简单地说，简单工厂模式通常就是这样，一个工厂类 XxxFactory，里面有一个静态方法，根据我们不同的参数，返回不同的派生自同一个父类（或实现同一接口）的实例对象。<br>我们强调职责单一原则，一个类只提供一种功能，FoodFactory 的功能就是只要负责生产各种 Food。</p>
<h1 id="工厂模式"><a href="#工厂模式" class="headerlink" title="工厂模式"></a>工厂模式</h1><p>简单工厂模式很简单，如果它能满足我们的需要，我觉得就不要折腾了。之所以需要引入工厂模式，是因为我们往往需要使用两个或两个以上的工厂。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">public interface FoodFactory &#123;</span><br><span class="line">    Food makeFood(String name);</span><br><span class="line">&#125;</span><br><span class="line">public class ChineseFoodFactory implements FoodFactory &#123;</span><br><span class="line"></span><br><span class="line">    @Override</span><br><span class="line">    public Food makeFood(String name) &#123;</span><br><span class="line">        if (name.equals(&quot;A&quot;)) &#123;</span><br><span class="line">            return new ChineseFoodA();</span><br><span class="line">        &#125; else if (name.equals(&quot;B&quot;)) &#123;</span><br><span class="line">            return new ChineseFoodB();</span><br><span class="line">        &#125; else &#123;</span><br><span class="line">            return null;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line">public class AmericanFoodFactory implements FoodFactory &#123;</span><br><span class="line"></span><br><span class="line">    @Override</span><br><span class="line">    public Food makeFood(String name) &#123;</span><br><span class="line">        if (name.equals(&quot;A&quot;)) &#123;</span><br><span class="line">            return new AmericanFoodA();</span><br><span class="line">        &#125; else if (name.equals(&quot;B&quot;)) &#123;</span><br><span class="line">            return new AmericanFoodB();</span><br><span class="line">        &#125; else &#123;</span><br><span class="line">            return null;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>其中，ChineseFoodA、ChineseFoodB、AmericanFoodA、AmericanFoodB 都派生自 Food。</p>
<p>客户端调用：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">public class APP &#123;</span><br><span class="line">    public static void main(String[] args) &#123;</span><br><span class="line">        &#x2F;&#x2F; 先选择一个具体的工厂</span><br><span class="line">        FoodFactory factory &#x3D; new ChineseFoodFactory();</span><br><span class="line">        &#x2F;&#x2F; 由第一步的工厂产生具体的对象，不同的工厂造出不一样的对象</span><br><span class="line">        Food food &#x3D; factory.makeFood(&quot;A&quot;);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>虽然都是调用 makeFood(“A”) 制作 A 类食物，但是，不同的工厂生产出来的完全不一样。</p>
<p>第一步，我们需要选取合适的工厂，然后第二步基本上和简单工厂一样。</p>
<p>核心在于，我们需要在第一步选好我们需要的工厂。比如，我们有 LogFactory 接口，实现类有 FileLogFactory 和 KafkaLogFactory，分别对应将日志写入文件和写入 Kafka 中，显然，我们客户端第一步就需要决定到底要实例化 FileLogFactory 还是 KafkaLogFactory，这将决定之后的所有的操作。</p>
<p>虽然简单，不过我也把所有的构件都画到一张图上，这样看着比较清晰：<br><img src="/images/gcmoshi.png" alt="alt"></p>
<h1 id="抽象工厂模式"><a href="#抽象工厂模式" class="headerlink" title="抽象工厂模式"></a>抽象工厂模式</h1><p>当涉及到产品族的时候，就需要引入抽象工厂模式了。<br>一个经典的例子是造一台电脑。我们先不引入抽象工厂模式，看看怎么实现。<br>因为电脑是由许多的构件组成的，我们将 CPU 和主板进行抽象，然后 CPU 由 CPUFactory 生产，主板由 MainBoardFactory 生产，然后，我们再将 CPU 和主板搭配起来组合在一起，如下图：<br><img src="/images/cxgc1.png" alt="alt"><br>这个时候的客户端调用是这样的：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">&#x2F;&#x2F; 得到 Intel 的 CPU</span><br><span class="line">CPUFactory cpuFactory &#x3D; new IntelCPUFactory();</span><br><span class="line">CPU cpu &#x3D; intelCPUFactory.makeCPU();</span><br><span class="line"></span><br><span class="line">&#x2F;&#x2F; 得到 AMD 的主板</span><br><span class="line">MainBoardFactory mainBoardFactory &#x3D; new AmdMainBoardFactory();</span><br><span class="line">MainBoard mainBoard &#x3D; mainBoardFactory.make();</span><br><span class="line"></span><br><span class="line">&#x2F;&#x2F; 组装 CPU 和主板</span><br><span class="line">Computer computer &#x3D; new Computer(cpu, mainBoard);</span><br></pre></td></tr></table></figure>
<p>单独看 CPU 工厂和主板工厂，它们分别是前面我们说的工厂模式。这种方式也容易扩展，因为要给电脑加硬盘的话，只需要加一个 HardDiskFactory 和相应的实现即可，不需要修改现有的工厂。</p>
<p>但是，这种方式有一个问题，那就是如果** Intel 家产的 CPU 和 AMD 产的主板不能兼容使用**，那么这代码就容易出错，因为客户端并不知道它们不兼容，也就会错误地出现随意组合。</p>
<p>-下面就是我们要说的产品族的概念，它代表了组成某个产品的一系列附件的集合：<br><img src="/images/cxgc2.png" alt="alt"><br>当涉及到这种产品族的问题的时候，就需要抽象工厂模式来支持了。我们不再定义 CPU 工厂、主板工厂、硬盘工厂、显示屏工厂等等，我们直接定义电脑工厂，每个电脑工厂负责生产所有的设备，这样能保证肯定不存在兼容问题。<br>客户端调用：<br><img src="/images/cxgc3.png" alt="alt"><br>这个时候，对于客户端来说，不再需要单独挑选 CPU厂商、主板厂商、硬盘厂商等，直接选择一家品牌工厂，品牌工厂会负责生产所有的东西，而且能保证肯定是兼容可用的。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">public static void main(String[] args) &#123;</span><br><span class="line">    &#x2F;&#x2F; 第一步就要选定一个“大厂”</span><br><span class="line">    ComputerFactory cf &#x3D; new AmdFactory();</span><br><span class="line">    &#x2F;&#x2F; 从这个大厂造 CPU</span><br><span class="line">    CPU cpu &#x3D; cf.makeCPU();</span><br><span class="line">    &#x2F;&#x2F; 从这个大厂造主板</span><br><span class="line">    MainBoard board &#x3D; cf.makeMainBoard();</span><br><span class="line">      &#x2F;&#x2F; 从这个大厂造硬盘</span><br><span class="line">      HardDisk hardDisk &#x3D; cf.makeHardDisk();</span><br><span class="line"></span><br><span class="line">    &#x2F;&#x2F; 将同一个厂子出来的 CPU、主板、硬盘组装在一起</span><br><span class="line">    Computer result &#x3D; new Computer(cpu, board, hardDisk);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>当然，抽象工厂的问题也是显而易见的，比如我们要加个显示器，就需要修改所有的工厂，给所有的工厂都加上制造显示器的方法。这有点违反了对修改关闭，对扩展开放这个设计原则。</p>
]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>编程</tag>
      </tags>
  </entry>
  <entry>
    <title>内功修炼-思维的进阶之设计模式</title>
    <url>/Java/%E5%86%85%E5%8A%9F%E4%BF%AE%E7%82%BC-%E6%80%9D%E7%BB%B4%E7%9A%84%E8%BF%9B%E9%98%B6/</url>
    <content><![CDATA[<h1 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h1><p>关于设计模式，是对开发人员一种思维上的修炼。旨在让代码易被他人理解、保证代码可靠性。 毫无疑问，设计模式于己于他人于系统都是多赢的，设计模式使代码编制真正工程化，设计模式是软件工程的基石，如同大厦的一块块砖石一样。项目中合理的运用设计模式可以完美的解决很多问题，每种模式在现在中都有相应的原理来与之对应，每一个模式描述了一个在我们周围不断重复发生的问题，以及该问题的核心解决方案，这也是它能被广泛应用的原因。<br>接触编程以来，只有在大四准备面试的时候认真刷过一段设计模式，可是当时时间仓促，导致至今也是半吊子水平.理解的并不深刻。。<br>正值四月末，借着这篇文章正式拉开五月设计模式深入学习之路。</p>
<a id="more"></a>

<h1 id="设计模式的分类"><a href="#设计模式的分类" class="headerlink" title="设计模式的分类"></a>设计模式的分类</h1><p>总体来说设计模式分为三大类：</p>
<p>创建型模式，共五种：工厂方法模式、抽象工厂模式、单例模式、建造者模式、原型模式。</p>
<p>结构型模式，共七种：适配器模式、装饰器模式、代理模式、外观模式、桥接模式、组合模式、享元模式。</p>
<p>行为型模式，共十一种：策略模式、模板方法模式、观察者模式、迭代子模式、责任链模式、命令模式、备忘录模式、状态模式、访问者模式、中介者模式、解释器模式。</p>
<p>其实还有两类：并发型模式和线程池模式。用一个图片来整体描述一下：<br><img src="/images/shejimoshi.jpg" alt="alt"></p>
<h1 id="设计模式的六大原则"><a href="#设计模式的六大原则" class="headerlink" title="设计模式的六大原则"></a>设计模式的六大原则</h1><p>1、开闭原则（Open Close Principle）</p>
<p>开闭原则就是说对扩展开放，对修改关闭。在程序需要进行拓展的时候，不能去修改原有的代码，实现一个热插拔的效果。所以一句话概括就是：为了使程序的扩展性好，易于维护和升级。想要达到这样的效果，我们需要使用接口和抽象类，后面的具体设计中我们会提到这点。</p>
<p>2、里氏代换原则（Liskov Substitution Principle）</p>
<p>里氏代换原则(Liskov Substitution Principle LSP)面向对象设计的基本原则之一。 里氏代换原则中说，任何基类可以出现的地方，子类一定可以出现。 LSP是继承复用的基石，只有当衍生类可以替换掉基类，软件单位的功能不受到影响时，基类才能真正被复用，而衍生类也能够在基类的基础上增加新的行为。里氏代换原则是对“开-闭”原则的补充。实现“开-闭”原则的关键步骤就是抽象化。而基类与子类的继承关系就是抽象化的具体实现，所以里氏代换原则是对实现抽象化的具体步骤的规范。—— From Baidu 百科</p>
<p>3、依赖倒转原则（Dependence Inversion Principle）</p>
<p>这个是开闭原则的基础，具体内容：真对接口编程，依赖于抽象而不依赖于具体。</p>
<p>4、接口隔离原则（Interface Segregation Principle）</p>
<p>这个原则的意思是：使用多个隔离的接口，比使用单个接口要好。还是一个降低类之间的耦合度的意思，从这儿我们看出，其实设计模式就是一个软件的设计思想，从大型软件架构出发，为了升级和维护方便。所以上文中多次出现：降低依赖，降低耦合。</p>
<p>5、迪米特法则（最少知道原则）（Demeter Principle）</p>
<p>为什么叫最少知道原则，就是说：一个实体应当尽量少的与其他实体之间发生相互作用，使得系统功能模块相对独立。</p>
<p>6、合成复用原则（Composite Reuse Principle）</p>
<p>原则是尽量使用合成/聚合的方式，而不是使用继承。</p>
<h1 id="Java中的23种设计模式"><a href="#Java中的23种设计模式" class="headerlink" title="Java中的23种设计模式"></a>Java中的23种设计模式</h1><p>本人主要语言为Java，接下来将详细整理java中的设计模式。</p>
]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>编程</tag>
      </tags>
  </entry>
  <entry>
    <title>排序算法归总-3(完结)</title>
    <url>/Java/%E6%8E%92%E5%BA%8F%E7%AE%97%E6%B3%95%E5%BD%92%E6%80%BB-3(%E5%AE%8C%E7%BB%93)/</url>
    <content><![CDATA[<h1 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h1><p>排序的整理(完结篇!)</p>
<a id="more"></a>
<h1 id="注"><a href="#注" class="headerlink" title="注"></a>注</h1><p>下面的演示图源于网络，个人觉得很清晰就一起收藏了</p>
<h1 id="基数排序"><a href="#基数排序" class="headerlink" title="基数排序"></a>基数排序</h1><p>基数排序（radix sort）属于“分配式排序”（distribution sort），又称“桶子法”（bucket sort）或bin sort，顾名思义，它是透过键值的部份资讯，将要排序的元素分配至某些“桶”中，藉以达到排序的作用，基数排序法是属于稳定性的排序，其时间复杂度为O(nlog( r )m)，其中r为所采取的基数，而m为堆数，在某些时候，基数排序法的效率高于其它的稳定性排序法。基数排序是用空间换时间的经典算法。<br>演示：<br><img src="/images/jishu.png" alt="alt"></p>
<p>基数排序的基本思想是：<br>将所有待比较的数值统一为同样的数位长度，数位较短的数前面补零。然后，从最低位开始，依次进行一次排序，这样从最低位排序一直到最高位排序完成以后，数列就变为了一个有序序列。<br>这样说可能过于抽象，我们通过详细步骤来分析一下：<br>我们假设有一个待排序数组[53，3，542，748，14，214]，那么如何使用基数排序对其进行排序呢？<br>首先我们有这样的十个一维数组，在基数排序中也叫桶。<br><img src="/images/jishu1.png" alt="alt"><br>那么第一轮排序开始，我们依次遍历每个元素，并得到元素的个位数。拿到的第一个元素为53，其个位数为3，所以将53放入编号为3的桶中，第二个元素3的个位数也是3，所以也放在编号为3的桶中，而第三个元素542的个位数为2，所以将542放入编号为2的桶中，以此类推。<br>所以结果为：<br><img src="/images/jishu2.png" alt="alt"><br>那么第一轮排序开始，我们依次遍历每个元素，并得到元素的个位数。拿到的第一个元素为53，其个位数为3，所以将53放入编号为3的桶中，第二个元素3的个位数也是3，所以也放在编号为3的桶中，而第三个元素542的个位数为2，所以将542放入编号为2的桶中，以此类推。<br>所以结果为：<br><img src="/images/jishu3.png" alt="alt"><br>然后同样按照桶的顺序将数据从中取出并放入原数组，此时原数组变为[3，14，214，542，748，53]。<br>接下来又进行第三轮排序，以元素的百位数进行区分，结果为：<br><img src="/images/jishu4.png" alt="alt"><br>按顺序取出数据后，原数组变为[3，14，53，214，542，748]。这时的数组已经完成排序。<br>从中我们也可以知道，基数排序的排序轮数取决于数组元素中最大位数的元素。<br>代码如下：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">@Test</span><br><span class="line">	public static void raixSort(int[] arr) &#123;</span><br><span class="line">		&#x2F;&#x2F; 第一轮(针对每个元素的个位进行排序处理)</span><br><span class="line"></span><br><span class="line">		&#x2F;&#x2F; 定义一个二维数组，模拟桶，每个桶就是一个一维数组</span><br><span class="line">		&#x2F;&#x2F; 为了防止放入数据的时候桶溢出，我们应该尽量将桶的容量设置得大一些</span><br><span class="line">		int[][] bucket &#x3D; new int[10][arr.length];</span><br><span class="line">		&#x2F;&#x2F; 记录每个桶中实际存放的元素个数</span><br><span class="line">		&#x2F;&#x2F; 定义一个一维数组来记录每个桶中每次放入的元素个数</span><br><span class="line">		int[] bucketElementCounts &#x3D; new int[10];</span><br><span class="line"></span><br><span class="line">		for (int j &#x3D; 0; j &lt; arr.length; j++) &#123;</span><br><span class="line">			&#x2F;&#x2F; 取出每个元素的个位</span><br><span class="line">			int digitOfElement &#x3D; arr[j] % 10;</span><br><span class="line">			&#x2F;&#x2F; 将元素放入对应的桶中</span><br><span class="line">			&#x2F;&#x2F; bucketElementCounts[digitOfElement]就是桶中的元素个数，初始为0，放在第一位</span><br><span class="line">			bucket[digitOfElement][bucketElementCounts[digitOfElement]] &#x3D; arr[j];</span><br><span class="line">			&#x2F;&#x2F; 将桶中的元素个数++</span><br><span class="line">			&#x2F;&#x2F; 这样接下来的元素就可以排在前面的元素后面</span><br><span class="line">			bucketElementCounts[digitOfElement]++;</span><br><span class="line">		&#125;</span><br><span class="line">		&#x2F;&#x2F; 按照桶的顺序取出数据并放回原数组</span><br><span class="line">		int index &#x3D; 0;</span><br><span class="line">		for (int k &#x3D; 0; k &lt; bucket.length; k++) &#123;</span><br><span class="line">			&#x2F;&#x2F; 如果桶中有数据，才取出放回原数组</span><br><span class="line">			if (bucketElementCounts[k] !&#x3D; 0) &#123;</span><br><span class="line">				&#x2F;&#x2F; 说明桶中有数据，对该桶进行遍历</span><br><span class="line">				for (int l &#x3D; 0; l &lt; bucketElementCounts[k]; l++) &#123;</span><br><span class="line">					&#x2F;&#x2F; 取出元素放回原数组</span><br><span class="line">					arr[index++] &#x3D; bucket[k][l];</span><br><span class="line">				&#125;</span><br><span class="line">			&#125;</span><br><span class="line">			&#x2F;&#x2F; 第一轮处理后，需要将每个bucketElementCounts[k]置0</span><br><span class="line">			bucketElementCounts[k] &#x3D; 0;</span><br><span class="line">		&#125;</span><br><span class="line">		System.out.println(&quot;第一轮:&quot; + Arrays.toString(arr));</span><br><span class="line"></span><br><span class="line">		&#x2F;&#x2F; ----------------------------</span><br><span class="line"></span><br><span class="line">		&#x2F;&#x2F; 第二轮(针对每个元素的十位进行排序处理)</span><br><span class="line">		for (int j &#x3D; 0; j &lt; arr.length; j++) &#123;</span><br><span class="line">			&#x2F;&#x2F; 取出每个元素的十位</span><br><span class="line">			int digitOfElement &#x3D; arr[j] &#x2F; 10 % 10;</span><br><span class="line">			&#x2F;&#x2F; 将元素放入对应的桶中</span><br><span class="line">			&#x2F;&#x2F; bucketElementCounts[digitOfElement]就是桶中的元素个数，初始为0，放在第一位</span><br><span class="line">			bucket[digitOfElement][bucketElementCounts[digitOfElement]] &#x3D; arr[j];</span><br><span class="line">			&#x2F;&#x2F; 将桶中的元素个数++</span><br><span class="line">			&#x2F;&#x2F; 这样接下来的元素就可以排在前面的元素后面</span><br><span class="line">			bucketElementCounts[digitOfElement]++;</span><br><span class="line">		&#125;</span><br><span class="line">		&#x2F;&#x2F; 按照桶的顺序取出数据并放回原数组</span><br><span class="line">		index &#x3D; 0;</span><br><span class="line">		for (int k &#x3D; 0; k &lt; bucket.length; k++) &#123;</span><br><span class="line">			&#x2F;&#x2F; 如果桶中有数据，才取出放回原数组</span><br><span class="line">			if (bucketElementCounts[k] !&#x3D; 0) &#123;</span><br><span class="line">				&#x2F;&#x2F; 说明桶中有数据，对该桶进行遍历</span><br><span class="line">				for (int l &#x3D; 0; l &lt; bucketElementCounts[k]; l++) &#123;</span><br><span class="line">					&#x2F;&#x2F; 取出元素放回原数组</span><br><span class="line">					arr[index++] &#x3D; bucket[k][l];</span><br><span class="line">				&#125;</span><br><span class="line">			&#125;</span><br><span class="line">			&#x2F;&#x2F; 第二轮处理后，需要将每个bucketElementCounts[k]置0</span><br><span class="line">			bucketElementCounts[k] &#x3D; 0;</span><br><span class="line">		&#125;</span><br><span class="line">		System.out.println(&quot;第二轮:&quot; + Arrays.toString(arr));</span><br><span class="line"></span><br><span class="line">		&#x2F;&#x2F; ----------------------------</span><br><span class="line"></span><br><span class="line">		&#x2F;&#x2F; 第三轮(针对每个元素的百位进行排序处理)</span><br><span class="line">		for (int j &#x3D; 0; j &lt; arr.length; j++) &#123;</span><br><span class="line">			&#x2F;&#x2F; 取出每个元素的百位</span><br><span class="line">			int digitOfElement &#x3D; arr[j] &#x2F; 100 % 10;</span><br><span class="line">			&#x2F;&#x2F; 将元素放入对应的桶中</span><br><span class="line">			&#x2F;&#x2F; bucketElementCounts[digitOfElement]就是桶中的元素个数，初始为0，放在第一位</span><br><span class="line">			bucket[digitOfElement][bucketElementCounts[digitOfElement]] &#x3D; arr[j];</span><br><span class="line">			&#x2F;&#x2F; 将桶中的元素个数++</span><br><span class="line">			&#x2F;&#x2F; 这样接下来的元素就可以排在前面的元素后面</span><br><span class="line">			bucketElementCounts[digitOfElement]++;</span><br><span class="line">		&#125;</span><br><span class="line">		&#x2F;&#x2F; 按照桶的顺序取出数据并放回原数组</span><br><span class="line">		index &#x3D; 0;</span><br><span class="line">		for (int k &#x3D; 0; k &lt; bucket.length; k++) &#123;</span><br><span class="line">			&#x2F;&#x2F; 如果桶中有数据，才取出放回原数组</span><br><span class="line">			if (bucketElementCounts[k] !&#x3D; 0) &#123;</span><br><span class="line">				&#x2F;&#x2F; 说明桶中有数据，对该桶进行遍历</span><br><span class="line">				for (int l &#x3D; 0; l &lt; bucketElementCounts[k]; l++) &#123;</span><br><span class="line">					&#x2F;&#x2F; 取出元素放回原数组</span><br><span class="line">					arr[index++] &#x3D; bucket[k][l];</span><br><span class="line">				&#125;</span><br><span class="line">			&#125;</span><br><span class="line">			&#x2F;&#x2F; 第三轮处理后，需要将每个bucketElementCounts[k]置0</span><br><span class="line">			bucketElementCounts[k] &#x3D; 0;</span><br><span class="line">		&#125;</span><br><span class="line">		System.out.println(&quot;第三轮:&quot; + Arrays.toString(arr));</span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>
<p>这样，基数排序就完成了。大家不要看到代码很多就怕了、烦了，觉得好难，其实也不能说一点难度都没有吧，只是要去理解这个过程，所以对于排序过程的分析我写了很多，也是为了能让你们更加理解，掌握了过程之后，相信理解这些代码也不是难事了。<br>其它：<br>这里说一说基数排序的一些其它内容，为什么单独只说基数排序呢？我们在前面提到了，基数排序是用空间换时间的经典算法，所以基数排序对于元素排序是非常快的。</p>
<p>测试一下速度:<br>case1(八十万个数据)：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">@Test</span><br><span class="line">public static void main(String[] args) &#123;</span><br><span class="line">		int[] arr &#x3D; new int[800000];</span><br><span class="line">		for (int i &#x3D; 0; i &lt; arr.length; i++) &#123;</span><br><span class="line">			arr[i] &#x3D; (int) (Math.random() * 8000000);</span><br><span class="line">		&#125;</span><br><span class="line">		Date date &#x3D; new Date();</span><br><span class="line">		SimpleDateFormat format &#x3D; new SimpleDateFormat(&quot;HH:mm:ss&quot;);</span><br><span class="line">		String dateStr &#x3D; format.format(date);</span><br><span class="line">		System.out.println(&quot;排序前的时间是:&quot; + dateStr);</span><br><span class="line">		raixSort(arr);</span><br><span class="line">		Date date2 &#x3D; new Date();</span><br><span class="line">		String dateStr2 &#x3D; format.format(date2);</span><br><span class="line">		System.out.println(&quot;排序前的时间是:&quot; + dateStr2);</span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>
<p>运行结果：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">排序前的时间是:17:37:21</span><br><span class="line">排序前的时间是:17:37:21</span><br></pre></td></tr></table></figure>
<p>一秒钟时间不到就完成排序了。</p>
<p>case2(八千万个数据)：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">public static void main(String[] args) &#123;</span><br><span class="line">	int[] arr &#x3D; new int[80000000];</span><br><span class="line">	for (int i &#x3D; 0; i &lt; arr.length; i++) &#123;</span><br><span class="line">		arr[i] &#x3D; (int) (Math.random() * 8000000);</span><br><span class="line">	&#125;</span><br><span class="line">	Date date &#x3D; new Date();</span><br><span class="line">	SimpleDateFormat format &#x3D; new SimpleDateFormat(&quot;HH:mm:ss&quot;);</span><br><span class="line">	String dateStr &#x3D; format.format(date);</span><br><span class="line">	System.out.println(&quot;排序前的时间是:&quot; + dateStr);</span><br><span class="line">	raixSort(arr);</span><br><span class="line">	Date date2 &#x3D; new Date();</span><br><span class="line">	String dateStr2 &#x3D; format.format(date2);</span><br><span class="line">	System.out.println(&quot;排序前的时间是:&quot; + dateStr2);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>运行结果：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">排序前的时间是:17:41:07</span><br><span class="line">Exception in thread &quot;main&quot; java.lang.OutOfMemoryError: Java heap space</span><br><span class="line">	at com.itcast.sort.RadixSortDemo.raixSort(RadixSortDemo.java:42)</span><br><span class="line">	at com.itcast.sort.RadixSortDemo.main(RadixSortDemo.java:22)</span><br></pre></td></tr></table></figure>
<p>堆内存溢出…所以在对大量数据进行排序的时候，基数排序显然不是一个好的选择，因为提升排序效率的条件是牺牲大量的内存空间，当数据足够多时，内存空间就不够用了。</p>
]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>算法</tag>
      </tags>
  </entry>
  <entry>
    <title>排序算法归总-2</title>
    <url>/Java/%E6%8E%92%E5%BA%8F%E7%AE%97%E6%B3%95%E5%BD%92%E6%80%BB-2/</url>
    <content><![CDATA[<h1 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h1><p>继续整理常用排序算法…</p>
<a id="more"></a>
<h1 id="注"><a href="#注" class="headerlink" title="注"></a>注</h1><p>下面的演示图源于网络，个人觉得很清晰就一起收藏了</p>
<h1 id="希尔排序"><a href="#希尔排序" class="headerlink" title="希尔排序"></a>希尔排序</h1><p>传统的插入排序算法在某些场景中存在着一些问题，例如[2，3，4，5，1]这样的一个数组，当我们对其进行插入排序的时候，发现要插入的数字是1，而要想将1插入到最前面，需要经过四个步骤，分别将5、4、3、2后移。所以得出结论：如果较小的数是我们需要进行插入的数，那效率就会比较低。鉴于这种场景的缺陷，希尔排序诞生了，它是插入排序的一种更高效的版本。<br>先看看希尔排序的概念：<br>希尔排序(Shell’s Sort)是插入排序的一种又称“缩小增量排序”（Diminishing Increment Sort），是直接插入排序算法的一种更高效的改进版本。希尔排序是非稳定排序算法。该方法因D.L.Shell于1959年提出而得名。<br>希尔排序是把记录按下标的一定增量分组，对每组使用直接插入排序算法排序；随着增量逐渐减少，每组包含的关键词越来越多，当增量减至1时，整个文件恰被分成一组，算法便终止。<br>演示：<br><img src="/images/xier.gif" alt="alt"></p>
<p>动画如果没有看懂，我这里再贴几张静态图：<br><img src="/images/xier.jpeg" alt="alt"><br>代码如下：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">@Test</span><br><span class="line">public void ShellSort() &#123;</span><br><span class="line">	int[] arr &#x3D; &#123; 3, 44, 38, 5, 47, 15, 36, 26, 27, 2, 46, 4, 19, 50, 48 &#125;;</span><br><span class="line">	for (int gap &#x3D; arr.length &#x2F; 2; gap &gt; 0; gap &#x2F;&#x3D; 2) &#123;</span><br><span class="line">		&#x2F;&#x2F; 对数组元素进行分组</span><br><span class="line">		for (int i &#x3D; gap; i &lt; arr.length; i++) &#123;</span><br><span class="line">			&#x2F;&#x2F; 遍历各组中的元素</span><br><span class="line">			for (int j &#x3D; i - gap; j &gt;&#x3D; 0; j -&#x3D; gap) &#123;</span><br><span class="line">				&#x2F;&#x2F; 交换元素</span><br><span class="line">				if (arr[j] &gt; arr[j + gap]) &#123;</span><br><span class="line">					int temp &#x3D; arr[j];</span><br><span class="line">					arr[j] &#x3D; arr[j + gap];</span><br><span class="line">					arr[j + gap] &#x3D; temp;</span><br><span class="line">				&#125;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	System.out.println(Arrays.toString(arr));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>那么在上面的程序段中，数组长度为15，所以在第一轮，数组被分为了15 / 2 = 7个小组，然后分别对每个小组的元素进行遍历。在第一轮中小组之间的元素间隔都为7，所以分成的小组数其实也就是元素之间的间隔。接着就可以对每个小组的元素进行比较，然后进行交换，接下来以此类推。</p>
<h1 id="快速排序"><a href="#快速排序" class="headerlink" title="快速排序"></a>快速排序</h1><p>快速排序（Quicksort）是对冒泡排序的一种改进。<br>快速排序由C. A. R. Hoare在1960年提出。它的基本思想是：通过一趟排序将要排序的数据分割成独立的两部分，其中一部分的所有数据都比另外一部分的所有数据都要小，然后再按此方法对这两部分数据分别进行快速排序，整个排序过程可以递归进行，以此达到整个数据变成有序序列。<br>演示：<br><img src="/images/kuaisu.gif" alt="alt"></p>
<p>代码如下：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">@Test</span><br><span class="line">public static void quickSort(int[] arr, int left, int right) &#123;</span><br><span class="line">		int l &#x3D; left;&#x2F;&#x2F; 左下标</span><br><span class="line">		int r &#x3D; right;&#x2F;&#x2F; 右下标</span><br><span class="line">		int pivot &#x3D; arr[(left + right) &#x2F; 2];&#x2F;&#x2F; 找到中间的值</span><br><span class="line">		&#x2F;&#x2F; 将比pivot小的值放在其左边，比pivot大的值放在其右边</span><br><span class="line">		while (l &lt; r) &#123;</span><br><span class="line">			&#x2F;&#x2F; 在pivot左边寻找，直至找到大于等于pivot的值才退出</span><br><span class="line">			while (arr[l] &lt; pivot) &#123;</span><br><span class="line">				l +&#x3D; 1;&#x2F;&#x2F; 将l右移一位</span><br><span class="line">			&#125;</span><br><span class="line">			&#x2F;&#x2F; 在pivot右边寻找，直至找到小于等于pivot的值才退出</span><br><span class="line">			while (arr[r] &gt; pivot) &#123;</span><br><span class="line">				r -&#x3D; 1;&#x2F;&#x2F; 将r左移一位</span><br><span class="line">			&#125;</span><br><span class="line">			if (l &gt;&#x3D; r) &#123;</span><br><span class="line">				&#x2F;&#x2F; 左右下标重合，寻找完毕，退出循环</span><br><span class="line">				break;</span><br><span class="line">			&#125;</span><br><span class="line">			&#x2F;&#x2F; 交换元素</span><br><span class="line">			int temp &#x3D; arr[l];</span><br><span class="line">			arr[l] &#x3D; arr[r];</span><br><span class="line">			arr[r] &#x3D; temp;</span><br><span class="line"></span><br><span class="line">			&#x2F;&#x2F;倘若发现值相等的情况，则没有比较的必要，直接移动下标即可</span><br><span class="line">		</span><br><span class="line">			&#x2F;&#x2F; 如果交换完后，发现arr[l]&#x3D;&#x3D;pivot，此时应将r左移一位</span><br><span class="line">			if (arr[l] &#x3D;&#x3D; pivot) &#123;</span><br><span class="line">				r -&#x3D; 1;</span><br><span class="line">			&#125;</span><br><span class="line">			&#x2F;&#x2F; 如果交换完后，发现arr[r]&#x3D;&#x3D;pivot，此时应将l右移一位</span><br><span class="line">			if (arr[r] &#x3D;&#x3D; pivot) &#123;</span><br><span class="line">				l +&#x3D; 1;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">		&#x2F;&#x2F; 如果l&#x3D;&#x3D;r，要把这两个下标错开，否则会出现无限递归，导致栈溢出的情况</span><br><span class="line">		if (l &#x3D;&#x3D; r) &#123;</span><br><span class="line">			l +&#x3D; 1;</span><br><span class="line">			r -&#x3D; 1;</span><br><span class="line">		&#125;</span><br><span class="line">		&#x2F;&#x2F; 向左递归</span><br><span class="line">		if (left &lt; r) &#123;</span><br><span class="line">			quickSort(arr, left, r);</span><br><span class="line">		&#125;</span><br><span class="line">		&#x2F;&#x2F; 向右递归</span><br><span class="line">		if (right &gt; l) &#123;</span><br><span class="line">			quickSort(arr, l, right);</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>
<p>快速排序的实现原理很简单，就是将原数组分成两部分，然后以中间值为标准，比它小的就放其左边，比它大的就放其右边，然后在左右两边又以相同的方式继续排序。<br>所以在代码实现过程中，首先要创建两个移动的变量，一个从最左边开始往右移动，一个从最右边开始往左移动，通过这两个变量来遍历左右两部分的元素。当发现左边有大于中间数的元素，右边有小于中间数的元素，此时就进行交换。当两个变量重合也就是相等的时候遍历结束，然后左右两部分作递归处理。</p>
<h1 id="归并排序"><a href="#归并排序" class="headerlink" title="归并排序"></a>归并排序</h1><p>归并排序（MERGE-SORT）是建立在归并操作上的一种有效的排序算法,该算法是采用分治法（Divide and Conquer）的一个非常典型的应用。将已有序的子序列合并，得到完全有序的序列；即先使每个子序列有序，再使子序列段间有序。若将两个有序表合并成一个有序表，称为二路归并。<br>演示：<br><img src="/images/guibing.gif" alt="alt"></p>
<p>归并排序使用了一种分治思想，分治思想的意思就是’分而治之”，就是把一个复杂的问题分成两个或更多的相同或相似的子问题，再把子问题分成更小的子问题……直到最后子问题可以简单地直接求解。<br>通过这个动图来看的话，相信很多人都一脸懵，没关系，我们通过静态图来分析一下：<br><img src="/images/guibing.png" alt="alt"><br>假设现在有一个待排序的序列，[5，2，4，7，1，3，2，2]，那么我们就需要将该序列进行分治，先将其分成两份：[5，2，4，7]和[1，3，2，2]，再将这两份分别分成两份：[5，2]和[4，7]；[1，3]和[2，2]，最后将这四部分再次分别分为两份，最后就将整个序列分为了八份。需要注意的是，在分的过程中，不需要遵循任何规则，关键在于归并，归并的过程中便实现了元素的排序。<br>代码如下：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">@Test</span><br><span class="line">	public static void mergeSort(int[] arr, int left, int right, int[] temp) &#123;</span><br><span class="line">		&#x2F;&#x2F; 分解</span><br><span class="line">		if (left &lt; right) &#123;</span><br><span class="line">			int mid &#x3D; (left + right) &#x2F; 2;&#x2F;&#x2F; 中间索引</span><br><span class="line">			&#x2F;&#x2F; 向左递归进行分解</span><br><span class="line">			mergeSort(arr, left, mid, temp);</span><br><span class="line">			&#x2F;&#x2F; 向右递归进行分解</span><br><span class="line">			mergeSort(arr, mid + 1, right, temp);&#x2F;&#x2F; mid + 1，中间位置的后一个位置才是右边序列的开始位置</span><br><span class="line">			&#x2F;&#x2F; 每分解一轮便合并一轮</span><br><span class="line">			merge(arr, left, right, mid, temp);</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	&#x2F;**</span><br><span class="line">	 * 合并的方法</span><br><span class="line">	 * </span><br><span class="line">	 * @param arr   待排序的数组</span><br><span class="line">	 * @param left  左边有序序列的初始索引</span><br><span class="line">	 * @param right 中间索引</span><br><span class="line">	 * @param mid   右边有序序列的初始索引</span><br><span class="line">	 * @param temp  做中转的数组</span><br><span class="line">	 *&#x2F;</span><br><span class="line">	public static void merge(int[] arr, int left, int right, int mid, int[] temp) &#123;</span><br><span class="line">		int i &#x3D; left; &#x2F;&#x2F; 初始化i，左边有序序列的初始索引</span><br><span class="line">		int j &#x3D; mid + 1;&#x2F;&#x2F; 初始化j，右边有序序列的初始索引(右边有序序列的初始位置即为中间位置的后一个位置)</span><br><span class="line">		int t &#x3D; 0;&#x2F;&#x2F; 指向temp数组的当前索引，初始为0</span><br><span class="line">		&#x2F;&#x2F; 先把左右两边的数据(已经有序)按规则填充到temp数组</span><br><span class="line">		&#x2F;&#x2F; 直到左右两边的有序序列，有一边处理完成为止</span><br><span class="line">		while (i &lt;&#x3D; mid &amp;&amp; j &lt;&#x3D; right) &#123;</span><br><span class="line">			&#x2F;&#x2F; 如果左边有序序列的当前元素小于或等于右边有序序列的当前元素，就将左边的元素填充到temp数组中</span><br><span class="line">			if (arr[i] &lt;&#x3D; arr[j]) &#123;</span><br><span class="line">				temp[t] &#x3D; arr[i];</span><br><span class="line">				t++;&#x2F;&#x2F; 索引后移</span><br><span class="line">				i++;&#x2F;&#x2F; i后移</span><br><span class="line">			&#125; else &#123;</span><br><span class="line">				&#x2F;&#x2F; 反之，将右边有序序列的当前元素填充到temp数组中</span><br><span class="line">				temp[t] &#x3D; arr[j];</span><br><span class="line">				t++;&#x2F;&#x2F; 索引后移</span><br><span class="line">				j++;&#x2F;&#x2F; j后移</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">		&#x2F;&#x2F; 把有剩余数据的一边的元素填充到temp中</span><br><span class="line">		while (i &lt;&#x3D; mid) &#123;</span><br><span class="line">			&#x2F;&#x2F; 此时说明左边序列还有剩余元素</span><br><span class="line">			&#x2F;&#x2F; 全部填充到temp数组</span><br><span class="line">			temp[t] &#x3D; arr[i];</span><br><span class="line">			t++;</span><br><span class="line">			i++;</span><br><span class="line">		&#125;</span><br><span class="line">		while (j &lt;&#x3D; right) &#123;</span><br><span class="line">			&#x2F;&#x2F; 此时说明左边序列还有剩余元素</span><br><span class="line">			&#x2F;&#x2F; 全部填充到temp数组</span><br><span class="line">			temp[t] &#x3D; arr[j];</span><br><span class="line">			t++;</span><br><span class="line">			j++;</span><br><span class="line">		&#125;</span><br><span class="line">		&#x2F;&#x2F; 将temp数组的元素复制到原数组</span><br><span class="line">		t &#x3D; 0;</span><br><span class="line">		int tempLeft &#x3D; left;</span><br><span class="line">		while (tempLeft &lt;&#x3D; right) &#123;</span><br><span class="line">			arr[tempLeft] &#x3D; temp[t];</span><br><span class="line">			t++;</span><br><span class="line">			tempLeft++;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>
<p>来分析一下吧，对于该排序算法，有两个部分组成，分解和合并。首先讲讲分解，在前面也说到了，我们需要将待排序的序列不停地进行分解，通过两个索引变量控制，一个初始索引，一个结尾索引。只有当两索引重合才结束分解。此时序列被分解成了十五个小份，这样分解工作就完成了。接下来是合并，合并操作也是最麻烦的，也是通过两个索引变量i，j。开始i在左边序列的第一个位置，j在右边序列的第一个位置，然后就是寻找左右两个序列中的最小值，放到新序列中，这时可能会出现一边的元素都放置完毕了，而另外一边还存在元素，此时只需将剩余的元素按顺序放进新序列即可，因为这时左右两边的序列已经是有序的了，最后将新序列复制到旧序列。这里也特别需要注意，因为合并的过程是分步的，而并非一次合并完成，所以数组的索引是在不断变化的。</p>
]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>算法</tag>
      </tags>
  </entry>
  <entry>
    <title>排序算法归总-1</title>
    <url>/Java/%E6%8E%92%E5%BA%8F%E7%AE%97%E6%B3%95%E5%BD%92%E6%80%BB-1/</url>
    <content><![CDATA[<h1 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h1><p>不多BB,整理一下程序员必备技能——排序~</p>
<a id="more"></a>
<h1 id="注"><a href="#注" class="headerlink" title="注"></a>注</h1><p>下面的演示图源于网络，个人觉得很清晰就一起收藏了</p>
<h1 id="可爱的冒泡排序"><a href="#可爱的冒泡排序" class="headerlink" title="可爱的冒泡排序"></a>可爱的冒泡排序</h1><p>冒泡排序（Bubble Sort），是一种较简单的排序算法。<br>它重复地走访过要排序的元素列，依次比较两个相邻的元素，如果他们的顺序（如从大到小、首字母从A到Z）错误就把他们交换过来。走访元素的工作是重复地进行直到没有相邻元素需要交换，也就是说该元素列已经排序完成。<br>这个算法的名字由来是因为越大的元素会经由交换慢慢“浮”到数列的顶端（升序或降序排列），就如同碳酸饮料中二氧化碳的气泡最终会上浮到顶端一样，故名“冒泡排序”。<br>演示：<br><img src="/images/maopao.gif" alt="alt"></p>
<p>代码如下：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">@Test</span><br><span class="line">public void bubbleSort() &#123;</span><br><span class="line">	int[] arr &#x3D; &#123; 3, 44, 38, 5, 47, 15, 36, 26, 27, 2, 46, 4, 19, 50, 48 &#125;;</span><br><span class="line">	&#x2F;&#x2F; 统计比较次数</span><br><span class="line">	int count &#x3D; 0;</span><br><span class="line">	&#x2F;&#x2F; 第一轮比较</span><br><span class="line">	for (int i &#x3D; 0; i &lt; arr.length - 1; i++) &#123;</span><br><span class="line">		&#x2F;&#x2F; 第二轮比较</span><br><span class="line">		for (int j &#x3D; 0; j &lt; arr.length - 1 - i; j++) &#123;</span><br><span class="line">			if (arr[j] &gt; arr[j + 1]) &#123;</span><br><span class="line">				&#x2F;&#x2F; 交换位置</span><br><span class="line">				int temp &#x3D; arr[j];</span><br><span class="line">				arr[j] &#x3D; arr[j + 1];</span><br><span class="line">				arr[j + 1] &#x3D; temp;</span><br><span class="line">			&#125;</span><br><span class="line">			count++;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	System.out.println(Arrays.toString(arr));</span><br><span class="line">	System.out.println(&quot;一共比较了:&quot; + count + &quot;次&quot;);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>我们首先在开始循环时定义了一个boolean变量为true，然后如果元素之间进行了交换，就将值置为false。所以，我们就可以通过这个boolean变量来判断是否有元素进行了交换。如果boolean变量为true，则证明没有元素进行交换，那么久说明此时的数组元素已经完成排序，那么跳出外层循环即可，否则就继续排序。</p>
<h1 id="选择排序"><a href="#选择排序" class="headerlink" title="选择排序"></a>选择排序</h1><p>选择排序（Selection sort）是一种简单直观的排序算法。它的工作原理是：第一次从待排序的数据元素中选出最小（或最大）的一个元素，存放在序列的起始位置，然后再从剩余的未排序元素中寻找到最小（大）元素，然后放到已排序的序列的末尾。以此类推，直到全部待排序的数据元素的个数为零。选择排序是不稳定的排序方法。<br>演示：<br><img src="/images/xuanze.gif" alt="alt"></p>
<p>代码如下：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">@Test</span><br><span class="line">public void SelectionSort() &#123;</span><br><span class="line">	int[] arr &#x3D; &#123; 3, 44, 38, 5, 47, 15, 36, 26, 27, 2, 46, 4, 19, 50, 48 &#125;;</span><br><span class="line">	for (int i &#x3D; 0; i &lt; arr.length - 1; i++) &#123;</span><br><span class="line">		int index &#x3D; i;</span><br><span class="line">		for (int j &#x3D; 1 + i; j &lt; arr.length; j++) &#123;</span><br><span class="line">			if (arr[j] &lt; arr[index]) &#123;</span><br><span class="line">				index &#x3D; j;&#x2F;&#x2F; 保存最小元素的下标</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">		&#x2F;&#x2F; 此时已经找到最小元素的下标</span><br><span class="line">		&#x2F;&#x2F; 将最小元素与前面的元素交换</span><br><span class="line">		int temp &#x3D; arr[index];</span><br><span class="line">		arr[index] &#x3D; arr[i];</span><br><span class="line">		arr[i] &#x3D; temp;</span><br><span class="line">	&#125;</span><br><span class="line">	System.out.println(Arrays.toString(arr));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>实现也非常的简单，首先在外循环里定义了一个index变量存储i的值，这是为了避免重复地比较，因为在每一轮的比较结束后，前i个元素是已经排好序的，所以无需再次比较，只需从i开始即可。后面的比较都是基于index位置的元素进行比较，倘若比较完后index位置的元素是最小值，那就无需交换，不动即可。而如果找到了比index位置的元素更小的元素，那就将该元素的索引赋值给index，然后继续比较，直到比较完成，比较完成之后得到的index即为数组中的最小值，那此时只需要将index位置的元素和i位置的元素交换即可。</p>
<h1 id="插入排序"><a href="#插入排序" class="headerlink" title="插入排序"></a>插入排序</h1><p>插入排序（Insertion sort）是一种简单直观且稳定的排序算法。如果有一个已经有序的数据序列，要求在这个已经排好的数据序列中插入一个数，但要求插入后此数据序列仍然有序，这个时候就要用到一种新的排序方法——插入排序法,插入排序的基本操作就是将一个数据插入到已经排好序的有序数据中，从而得到一个新的、个数加一的有序数据，算法适用于少量数据的排序，时间复杂度为O(n^2)。是稳定的排序方法。插入算法把要排序的数组分成两部分：第一部分包含了这个数组的所有元素，但将最后一个元素除外（让数组多一个空间才有插入的位置），而第二部分就只包含这一个元素（即待插入元素）。在第一部分排序完成后，再将这个最后元素插入到已排好序的第一部分中。<br>插入排序的基本思想是：每步将一个待排序的记录，按其关键码值的大小插入到前面已经排序的数组中的适当位置上，直到全部插入完为止。<br>演示：<br><img src="/images/charu.gif" alt="alt"></p>
<p>代码如下：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">@Test</span><br><span class="line">public void InsertionSort() &#123;</span><br><span class="line">	int[] arr &#x3D; &#123; 3, 44, 38, 5, 47, 15, 36, 26, 27, 2, 46, 4, 19, 50, 48 &#125;;</span><br><span class="line">	for (int i &#x3D; 1; i &lt; arr.length; i++) &#123;</span><br><span class="line">		&#x2F;&#x2F; 定义待插入的数</span><br><span class="line">		int insertValue &#x3D; arr[i];</span><br><span class="line">		&#x2F;&#x2F; 找到待插入数的前一个数的下标</span><br><span class="line">		int insertIndex &#x3D; i - 1;</span><br><span class="line">		while (insertIndex &gt;&#x3D; 0 &amp;&amp; insertValue &lt; arr[insertIndex]) &#123;</span><br><span class="line">			arr[insertIndex + 1] &#x3D; arr[insertIndex];</span><br><span class="line">			insertIndex--;</span><br><span class="line">		&#125;</span><br><span class="line">		arr[insertIndex + 1] &#x3D; insertValue;</span><br><span class="line">	&#125;</span><br><span class="line">	System.out.println(Arrays.toString(arr));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>那么在这里，因为数组元素我们并不确定，所以只能将数组的第一个元素看成是一个有序的序列，所以从数组的第二个元素开始才是我们需要去寻找插入位置的元素。所以外层循环从1开始，然后将arr[i]，也就是当前的第二个元素先保存起来，然后找到待插入元素的前一个元素下标，也就是i-1，此时通过一个while循环去比较。<br>当insertIndex小于0时应该退出循环，因为此时已经与前面的所有元素比较完毕。在比较的过程中，如果待插入元素小于前一个元素，就将前一个元素后移，也就是将前一个元素的值直接赋值给待插入元素位置。因为在最开始已经将待插入元素进行了保存，所以只需将待插入元素的值赋值给它的前一个元素即可。因为在while循环中insertIndex执行了自减操作，所以它的前一个元素下标应为insertIndex + 1。而如果待插入的元素值大于前一个元素，那么就不会进入while循环，这样insertIndex + 1之后的位置仍然是自己所在的位置，所以赋值后值不改变，后面的操作以此类推。</p>
]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>算法</tag>
      </tags>
  </entry>
  <entry>
    <title>jwt单点登录详细整理</title>
    <url>/%E9%80%9A%E4%BF%A1/jwt%E5%8D%95%E7%82%B9%E7%99%BB%E5%BD%95%E8%AF%A6%E7%BB%86%E6%95%B4%E7%90%86/</url>
    <content><![CDATA[<h1 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h1><p>工作以来接触过两次sso单点登录，一次基于spring security，最近的一次基于jwt。感觉最大的问题就是有时候编程思路不太清晰就开始动手coding，总是蒙蒙的。参与了两次关于登录流程的设计，实在是不应该对这里再糊里糊涂了。痛定思痛，详细整理一下普通的会话管理和基于jwt的单点登录。(由于涉及到工作内容，以下就不用之前的源码展示了)</p>
<p>补充：下文会话管理部分图片为手绘，jwt相关图片来源于网络。</p>
<a id="more"></a>
<h1 id="会话管理"><a href="#会话管理" class="headerlink" title="会话管理"></a>会话管理</h1><p>在说jwt之前先说一下常用的会话管理  </p>
<ul>
<li><p>基于server-session的管理方式</p>
</li>
<li><p>cookie-based的管理方式</p>
</li>
<li><p>token-based的管理方式</p>
<ul>
<li><p><strong>基于server-session的管理</strong></p>
</li>
<li><p>(1).服务端session是用户第一次访问应用时，服务器就会创建的对象，代表用户的一次会话过程，服务器为每一个session都分配一个唯一的sessionid，以保证每个用户都有一个不同的session对象。</p>
</li>
<li><p>(2).服务器在创建完session后，会把sessionid通过cookie返回给用户所在的浏览器，这样当用户第二次及以后向服务器发送请求的时候，就会通过cookie把sessionid传回给服务器，以便服务器能够根据sessionid找到与该用户对应的session对象。</p>
</li>
<li><p>(3).session通常有失效时间的设定，比如2个小时。当失效时间到，服务器会销毁之前的session，并创建新的session返回给用户。但是只要用户在失效时间内，有发送新的请求给服务器，通常服务器都会把他对应的session的失效时间根据当前的请求时间再延长2个小时。</p>
</li>
<li><p>(4).session在一开始并不具备会话管理的作用。它只有在用户登录认证成功之后，并且往session对象里面放入了用户登录成功的凭证，才能用来管理会话。管理会话的逻辑也很简单，只要拿到用户的session对象，看它里面有没有登录成功的凭证，就能判断这个用户是否已经登录。当用户主动退出的时候，会把它的session对象里的登录凭证清掉。所以在用户登录前或退出后或者session对象失效时，肯定都是拿不到需要的登录凭证的。</p>
<p>以上过程可简单使用流程图描述如下：<br><img src="/images/server-session.png" alt="alt"></p>
<p>该种会话管理方式的优点:个人理解主要就是安全性比较高，因为在浏览器端与服务器端保持会话状态的媒介始终只是一个sessionid串，只要这个串够随机，攻击者就不能轻易冒充他人的sessionid进行操作；除非通过CSRF或http劫持的方式，才有可能冒充别人进行操作；即使冒充成功，也必须被冒充的用户session里面包含有效的登录凭证才行。<br>当然结合这两次的相关开发经验，此种方式也暴露了相当一部分缺点：</p>
<ul>
<li>(1).这种方式将会话信息存储在web服务器里面，所以在用户同时在线量比较多时，这些会话信息会占据比较多的内存。</li>
<li>(2).当应用采用集群部署的时候，会遇到多台web服务器之间如何做session共享的问题。因为session是由单个服务器创建的，但是处理用户请求的服务器不一定是那个创建session的服务器，这样他就拿不到之前已经放入到session中的登录凭证之类的信息了</li>
<li>(3).多个应用要共享session时，除了以上问题，还会遇到跨域问题，因为不同的应用可能部署的主机不一样，需要在各个应用做好cookie跨域的处理。</li>
</ul>
<p>针对问题1和问题2，可以采用redis这种中间服务器来管理session的增删改查，一来减轻web服务器的负担，二来解决不同web服务器共享session的问题。针对问题3，我个人所在的项目是通过生成token来实现的，在跳转系统时将token作为参数传给其它系统(同样需要跨域！！！但无需增加处理逻辑)，然后解析，借此实现多个应用共享同一用户不同系统登陆状态。至于其他方面我这里并不知道什么太好的处理…也欢迎各位同行指点探讨。</p>
</li>
<li><p><strong>cookie-based的管理方式</strong></p>
<p>当用户登录成功之后，把登录凭证写到cookie里面，并给cookie设置有效期，后续请求直接验证存有登录凭证的cookie是否存在以及凭证是否有效，即可判断用户的登录状态。使用该种方式相比于第一种方式减轻了服务器的处理逻辑和复杂程度。使用它来实现会话管理的整体流程如下：</p>
</li>
<li><p>(1).用户发起登录请求，服务端根据传入的用户密码之类的身份信息，验证用户是否满足登录条件，如果满足，就根据用户信息创建一个登录凭证，这个登录凭证简单来说就是一个对象，最简单的形式可以只包含用户id，凭证创建时间和过期时间三个值。</p>
</li>
<li><p>(2).服务端把上一步创建好的登录凭证，先对它做数字签名，然后再用对称加密算法做加密处理，将签名、加密后的字串，写入cookie。cookie的名字必须固定（如ticket），因为后面再获取的时候，还得根据这个名字来获取cookie值。这一步添加数字签名的目的是防止登录凭证里的信息被篡改，因为一旦信息被篡改，那么下一步做签名验证的时候肯定会失败。做加密的目的，是防止cookie被别人截取的时候，无法轻易读到其中的用户信息。</p>
</li>
<li><p>(3).用户登录后发起后续请求，服务端根据上一步存登录凭证的cookie名字，获取到相关的cookie值。然后先做解密处理，再做数字签名的认证，如果这两步都失败，说明这个登录凭证非法；如果这两步成功，接着就可以拿到原始存入的登录凭证了。然后用这个凭证的过期时间和当前时间做对比，判断凭证是否过期，如果过期，就需要用户再重新登录；如果未过期，则允许请求继续。</p>
<p>以上过程可简单使用流程图描述如下：<br><img src="/images/cookie-based.png" alt="alt"></p>
<p>这种方式最大的优点就是实现了服务端的无状态化，彻底移除了服务端对会话的管理的逻辑，服务端只需要负责创建和验证登录cookie即可，无需保持用户的状态信息。对于第一种方式的第二个问题，用户会话信息共享的问题，它也能很好解决：因为如果只是同一个应用做集群部署，由于验证登录凭证的代码都是一样的，所以不管是哪个服务器处理用户请求，总能拿到cookie中的登录凭证来进行验证；如果是不同的应用，只要每个应用都包含相同的登录逻辑，那么他们也是能轻易实现会话共享的，不过这种情况下，登录逻辑里面数字签名以及加密解密要用到的密钥文件或者密钥串，需要在不同的应用里面共享，总而言之，就是需要算法完全保持一致。</p>
<p>这种方式由于把登录凭证直接存放客户端，并且需要cookie传来传去，所以它的缺点也比较明显：</p>
<p>cookie有大小限制，存储不了太多数据，所以要是登录凭证存的消息过多，导致加密签名后的串太长，就会引发别的问题，比- (1).如其它业务场景需要cookie的时候，就有可能没那么多空间可用了；所以用的时候得谨慎，得观察实际的登录cookie的大小；比如太长，就要考虑是非是数字签名的算法太严格，导致签名后的串太长，那就适当调整签名逻辑；比如如果一开始用4096位的RSA算法做数字签名，可以考虑换成1024、2048位；</p>
</li>
<li><p>(2).每次传送cookie，增加了请求的数量，对访问性能也有影响；</p>
</li>
<li><p>(3).也有跨域问题，毕竟还是要用cookie。</p>
</li>
<li><p><strong>token-based的管理方式</strong></p>
<p>这种方式从流程和实现上来说，跟cookie-based的方式没有太多区别，只不过cookie-based里面写到cookie里面的ticket在这种方式下称为token，这个token在返回给客户端之后，后续请求都必须通过url参数或者是http header的形式，主动带上token，这样服务端接收到请求之后就能直接从http header或者url里面取到token进行验证：</p>
<p>流程如下：<br><img src="/images/token-based.png" alt="alt"></p>
<p>token可以存于localStorage或者sessionStorage里面，然后每发ajax请求的时候，都把token拿出来放到ajax请求的header里即可。不过如果是非接口的请求，比如直接通过点击链接请求一个页面这种，是无法自动带上token的。所以这种方式也仅限于走纯接口的web应用。</p>
<p>这种方式用在web应用里也有跨域的问题，比如应用如果部署在a.com，api服务部署在b.com，从a.com里面发出ajax请求到b.com，默认情况下是会报跨域错误的，这种问题可以用CORS（跨域资源共享）的方式来快速解决。这里就是我之前提到的第一种方式缺点三的解决。</p>
<p>这种方式跟cookie-based的方式同样都还有的一个问题就是ticket或者token刷新的问题。有的产品里面，你肯定不希望用户登录后，操作了半个小时，结果ticket或者token到了过期时间，然后用户又得去重新登录的情况出现。这个时候就得考虑ticket或token的自动刷新的问题，简单来说，可以在验证ticket或token有效之后，自动把ticket或token的失效时间延长，然后把它再返回给客户端；客户端如果检测到服务器有返回新的ticket或token，就替换原来的ticket或token。</p>
</li>
</ul>
</li>
</ul>
<p><strong>三种方式小结</strong>  </p>
<p>在web应用里面，会话管理的安全性始终是最重要的安全问题，这个对用户的影响极大。</p>
<p>首先从会话管理凭证来说，第一种方式的会话凭证仅仅是一个sessionid，所以只要这个sessionid足够随机，而不是一个自增的数字id值，那么其它人就不可能轻易地冒充别人的sessionid进行操作；第二种方式的凭证ticket以及第三种方式的凭证token都是一个在服务端做了数字签名，和加密处理的串，所以只要密钥不泄露，别人也无法轻易地拿到这个串中的有效信息并对它进行篡改。总之，这三种会话管理方式的凭证本身是比较安全的。</p>
<p>然后从客户端和服务端的http过程来说，当别人截获到客户端请求中的会话凭证，就能拿这个凭证冒充原用户，做一些非法操作，而服务器也认不出来。这种安全问题，可以简单采用https来解决，虽然可能还有http劫持这种更高程度的威胁存在，但是从代码能做的防范，确实也就是这个层次了。</p>
<h1 id="JWT"><a href="#JWT" class="headerlink" title="JWT"></a>JWT</h1><h2 id="JWT介绍"><a href="#JWT介绍" class="headerlink" title="JWT介绍"></a>JWT介绍</h2><p>JSON Web Token（JWT）是一个开放标准（RFC 7519），它定义了一种紧凑和自包含的方式，用于在各方之间作为JSON对象安全地传输信息。作为标准，它没有提供技术实现，但是大部分的语言平台都有按照它规定的内容提供了自己的技术实现，所以实际在用的时候，只要根据自己当前项目的技术平台，到官网上选用合适的实现库即可。</p>
<p>使用JWT来传输数据，实际上传输的是一个字符串，这个字符串就是所谓的json web token字符串。所以广义上，JWT是一个标准的名称；狭义上，JWT指的就是用来传递的那个token字符串。这个串有两个特点： </p>
<ol>
<li>紧凑：指的是这个串很小，能通过url 参数，http请求提交的数据以及http header的方式来传递； </li>
<li>自包含：这个串可以包含很多信息，比如用户的id、角色等，别人拿到这个串，就能拿到这些关键的业务信息，从而避免再通过数据库查询等方式才能得到它们。</li>
</ol>
<p>通常一个JWT是长这个样子的：<br><img src="/images/jwt1.png" alt="alt"></p>
<h2 id="JWT的数据结构"><a href="#JWT的数据结构" class="headerlink" title="JWT的数据结构"></a>JWT的数据结构</h2><p>一个JWT实际上是由三个部分组成：header（头部）、payload（载荷）和signature（签名）。这三个部分在JWT里面分别对应英文句号分隔出来的三个串：<br><img src="/images/jwt2.png" alt="alt"></p>
<p>先来看header部分的结构以及它的生成方法。header部分是由下面格式的json结构生成出来：<br><img src="/images/jwt3.png" alt="alt"></p>
<p>这个json中的typ属性，用来标识整个token字符串是一个JWT字符串；它的alg属性，用来说明这个JWT签发的时候所使用的签名和摘要算法。typ跟alg属性的全称其实是type跟algorithm，分别是类型跟算法的意思。之所以都用三个字母来表示，也是基于JWT最终字串大小的考虑，同时也是跟JWT这个名称保持一致，这样就都是三个字符了…typ跟alg是JWT中标准中规定的属性名称，虽然在签发JWT的时候，也可以把这两个名称换掉，但是如果随意更换了这个名称，就有可能在JWT验证的时候碰到问题，因为拿到JWT的人，默认会根据typ和alg去拿JWT中的header信息，当你改了名称之后，显然别人是拿不到header信息的，他又不知道你把这两个名字换成了什么。JWT作为标准的意义在于统一各方对同一个事情的处理方式，各个使用方都按它约定好的格式和方法来签发和验证token，这样即使运行的平台不一样，也能够保证token进行正确的传递。</p>
<p>一般签发JWT的时候，header对应的json结构只需要typ和alg属性就够了。JWT的header部分是把前面的json结构，经过Base64Url编码之后生成出来的：<br><img src="/images/jwt5.png" alt="alt"></p>
<p>再来看payload部分的结构和生成过程。payload部分是由下面类似格式的json结构生成出来：<br><img src="/images/jwt6.png" alt="alt"></p>
<p>payload的json结构并不像header那么简单，payload用来承载要传递的数据，它的json结构实际上是对JWT要传递的数据的一组声明，这些声明被JWT标准称为claims，它的一个“属性值对”其实就是一个claim，每一个claim的都代表特定的含义和作用。比如上面结构中的sub代表这个token的所有人，存储的是所有人的ID；name表示这个所有人的名字；admin表示所有人是否管理员的角色。当后面对JWT进行验证的时候，这些claim都能发挥特定的作用。</p>
<p>根据JWT的标准，这些claims可以分为以下三种类型： </p>
<p>1.Reserved claims（保留），它的含义就像是编程语言的保留字一样，属于JWT标准里面规定的一些claim。JWT标准里面定好的claim有：</p>
<p>iss(Issuser)：代表这个JWT的签发主体；<br>sub(Subject)：代表这个JWT的主体，即它的所有人；<br>aud(Audience)：代表这个JWT的接收对象；<br>exp(Expiration time)：是一个时间戳，代表这个JWT的过期时间；<br>nbf(Not Before)：是一个时间戳，代表这个JWT生效的开始时间，意味着在这个时间之前验证JWT是会失败的；<br>iat(Issued at)：是一个时间戳，代表这个JWT的签发时间；<br>jti(JWT ID)：是JWT的唯一标识。</p>
<p>2.Public claims(这部分不常用，网上也没有特别详细的介绍)</p>
<p>3.Private claims，这个指的就是自定义的claim。比如前面那个结构举例中的admin和name都属于自定的claim。这些claim跟JWT标准规定的claim区别在于：JWT规定的claim，JWT的接收方在拿到JWT之后，都知道怎么对这些标准的claim进行验证；而private claims不会验证，除非明确告诉接收方要对这些claim进行验证以及规则才行。</p>
<p>按照JWT标准的说明：保留的claims都是可选的，在生成payload不强制用上面的那些claim，你可以完全按照自己的想法来定义payload的结构，不过这样搞根本没必要：第一是，如果把JWT用于认证， 那么JWT标准内规定的几个claim就足够用了，甚至只需要其中一两个就可以了，假如想往JWT里多存一些用户业务信息，比如角色和用户名等，这倒是用自定义的claim来添加；第二是，JWT标准里面针对它自己规定的claim都提供了有详细的验证规则描述，每个实现库都会参照这个描述来提供JWT的验证实现，所以如果是自定义的claim名称，那么你用到的实现库就不会主动去验证这些claim。</p>
<p>最后也是把这个json结构做base64url编码之后，就能生成payload部分的串：<br><img src="/images/jwt7.png" alt="alt"></p>
<p>最后看signature部分的生成过程。签名是把header和payload对应的json结构进行base64url编码之后得到的两个串用英文句点号拼接起来，然后根据header里面alg指定的签名算法生成出来的。算法不同，签名结果不同，但是不同的算法最终要解决的问题是一样的。以alg: HS256为例来说明前面的签名如何来得到。按照前面alg可用值的说明，HS256其实包含的是两种算法：HMAC算法和SHA256算法，前者用于生成摘要，后者用于对摘要进行数字签名。</p>
<p>以上就是一个JWT包含的全部内容以及它的签发过程。</p>
<h2 id="JWT的验证过程"><a href="#JWT的验证过程" class="headerlink" title="JWT的验证过程"></a>JWT的验证过程</h2><p>这个部分介绍JWT的验证规则，主要包括签名验证和payload里面各个标准claim的验证逻辑介绍。只有验证成功的JWT，才能当做有效的凭证来使用。</p>
<p>先说签名验证。当接收方接收到一个JWT的时候，首先要对这个JWT的完整性进行验证，这个就是签名认证。它验证的方法其实很简单，只要把header做base64url解码，就能知道JWT用的什么算法做的签名，然后用这个算法，再次用同样的逻辑对header和payload做一次签名，并比较这个签名是否与JWT本身包含的第三个部分的串是否完全相同，只要不同，就可以认为这个JWT是一个被篡改过的串，自然就属于验证失败了。接收方生成签名的时候必须使用跟JWT发送方相同的密钥，意味着要做好密钥的安全传递或共享。</p>
<p>再来看payload的claim验证，拿前面标准的claim来一一说明：</p>
<p>iss(Issuser)：如果签发的时候这个claim的值是“a.com”，验证的时候如果这个claim的值不是“a.com”就属于验证失败；<br>sub(Subject)：如果签发的时候这个claim的值是“liuyunzhuge”，验证的时候如果这个claim的值不是“liuyunzhuge”就属于验证失败；<br>(Audience)：如果签发的时候这个claim的值是“[‘b.com’,’c.com’]”，验证的时候这个claim的值至少要包含b.com，c.com的其中一个才能验证通过；<br>exp(Expiration time)：如果验证的时候超过了这个claim指定的时间，就属于验证失败；<br>nbf(Not Before)：如果验证的时候小于这个claim指定的时间，就属于验证失败；<br>iat(Issued at)：它可以用来做一些maxAge之类的验证，假如验证时间与这个claim指定的时间相差的时间大于通过maxAge指定的一个值，就属于验证失败；<br>jti(JWT ID)：如果签发的时候这个claim的值是“1”，验证的时候如果这个claim的值不是“1”就属于验证失败；<br>需要注意的是，在验证一个JWT的时候，签名认证是每个实现库都会自动做的，但是payload的认证是由使用者来决定的。因为JWT里面可能不会包含任何一个标准的claim，所以它不会自动去验证这些claim。</p>
<p>以登录认证来说，在签发JWT的时候，完全可以只用sub跟exp两个claim，用sub存储用户的id，用exp存储它本次登录之后的过期时间，然后在验证的时候仅验证exp这个claim，以实现会话的有效期管理。</p>
<h2 id="JWT-amp-SSO"><a href="#JWT-amp-SSO" class="headerlink" title="JWT&amp;SSO"></a>JWT&amp;SSO</h2><p>场景一：用户发起对业务系统的第一次访问，假设他第一次访问的是系统A的some/page这个页面，它最终成功访问到这个页面的过程是：<br><img src="/images/sso1.png" alt="alt"></p>
<p>在这个过程里面，我认为理解的关键点在于：</p>
<ul>
<li><p>它用到了两个cookie(jwt和sid)和三次重定向来完成会话的创建和会话的传递；</p>
</li>
<li><p>jwt的cookie是写在systemA.com这个域下的，所以每次重定向到systemA.com的时候，jwt这个cookie只要有就会带过去；</p>
</li>
<li><p>sid的cookie是写在cas.com这个域下的，所以每次重定向到cas.com的时候，sid这个cookie只要有就会带过去；</p>
</li>
<li><p>在验证jwt的时候，如何知道当前用户已经创建了sso的会话？<br>因为jwt的payload里面存储了之前创建的sso会话的sessionid，所以当cas拿到jwt，就相当于拿到了sessionid，然后用这个sessionid去判断有没有的对应的session对象即可。</p>
</li>
</ul>
<p>场景二：用户登录之后，继续访问系统A的其它页面，如some/page2，它的处理过程是：<br><img src="/images/sso2.png" alt="alt"></p>
<p>从这一步可以看出，即使登录之后，也要每次跟CAS校验jwt的有效性以及会话的有效性，其实jwt的有效性也可以放在业务系统里面处理的，但是会话的有效性就必须到CAS那边才能完成了。当CAS拿到jwt里面的sessionid之后，就能到session缓存服务器里面去验证该sessionid对应的session对象是否存在，不存在，就说明会话已经销毁了（退出）。</p>
<p>场景三：用户登录了系统A之后，再去访问其他系统如系统B的资源，比如系统B的some/page，它最终能访问到系统B的some/page的流程是：<br><img src="/images/sso3.png" alt="alt"></p>
<p>这个过程的关键在于第一次重定向的时候，它会把sid这个cookie带回给CAS服务器，所以CAS服务器能够判断出会话是否已经建立，如果已经建立就跳过登录页的逻辑。</p>
<p>场景四：用户继续访问系统B的其它资源，如系统B的some/page2：<br><img src="/images/sso4.png" alt="alt"></p>
<p>这个场景的逻辑跟场景二完全一致。</p>
<p>场景五：退出登录，假如它从系统B发起退出，最终的流程是：<br><img src="/images/sso5.png" alt="alt"></p>
<p>最重要的是要清除sid的cookie，jwt的cookie可能业务系统都有创建，所以不可能在退出的时候还挨个去清除那些系统的cookie，只要sid一清除，那么即使那些jwt的cookie在下次访问的时候还会被传递到业务系统的服务端，由于jwt里面的sid已经无效，所以最后还是会被重定向到CAS登录页进行处理。</p>
<p>方案总结<br>以上方案两个关键的前提：</p>
<p>整个会话管理其实还是基于服务端的session来做的，只不过这个session只存在于CAS服务里面；<br>CAS之所以信任业务系统的jwt，是因为这个jwt是CAS签发的，理论上只要认证通过，就可以认为这个jwt是合法的。<br>jwt本身是不可伪造，不可篡改的，但是不代表非法用户冒充正常用法发起请求，所以常规的几个安全策略在实际项目中都应该使用：</p>
<ul>
<li>使用https</li>
<li>使用http-only的cookie，针对sid和jwt</li>
<li>管理好密钥</li>
<li>防范CSRF攻击。<br>尤其是CSRF攻击形式，很多都是钻代码的漏洞发生的，所以一旦出现CSRF漏洞，并且被人利用，那么别人就能用获得的jwt，冒充正常用户访问所有业务系统，这个安全问题的后果还是很严重的。考虑到这一点，为了在即使有漏洞的情况将损害减至最小，可以在jwt里面加入一个系统标识，添加一个验证，只有传过来的jwt内的系统标识与发起jwt验证请求的服务一致的情况下，才允许验证通过。这样的话，一个非法用户拿到某个系统的jwt，就不能用来访问其它业务系统了。</li>
</ul>
<p>在业务系统跟CAS发起attach/validate请求的时候，也可以在CAS端做些处理，因为这个请求，在一次SSO过程中，一个系统只应该发一次，所以只要之前已经给这个系统签发过jwt了，那么后续 同一系统的attach/validate请求都可以忽略掉。</p>
<p>总的来说，这个方案的好处有：</p>
<ul>
<li><p>完全分布式，跨平台，CAS以及业务系统均可采用不同的语言来开发；</p>
</li>
<li><p>业务系统如系统A和系统B，可实现服务端无状态</p>
</li>
<li><p>假如是自己来实现，那么可以轻易的在CAS里面集成用户注册服务以及第三方登录服务，如微信登录等。<br>它的缺陷是：</p>
</li>
<li><p>第一次登录某个系统，需要三次重定向；</p>
</li>
<li><p>登录后的后续请求，每次都需要跟CAS进行会话验证，所以CAS的性能负载会比较大</p>
</li>
<li><p>登陆后的后续请求，每次都跟CAS交互，也会增加请求响应时间，影响用户体验。</p>
</li>
</ul>
<h1 id="特别声明"><a href="#特别声明" class="headerlink" title="特别声明"></a>特别声明</h1><p><strong>JWT数据结构</strong>参考自<a href="https://blog.csdn.net/weixin_42139757/article/details/80689234" target="_blank" rel="noopener">https://blog.csdn.net/weixin_42139757/article/details/80689234</a><br><strong>JWT&amp;SSO</strong>参考自<a href="https://blog.csdn.net/weixin_41282397/article/details/85049850" target="_blank" rel="noopener">https://blog.csdn.net/weixin_41282397/article/details/85049850</a></p>
<h1 id="后记"><a href="#后记" class="headerlink" title="后记"></a>后记</h1><p>自己还差得多，文章若有不足还希望看到的问题多多指教！</p>
]]></content>
      <categories>
        <category>通信</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>框架</tag>
        <tag>网络</tag>
      </tags>
  </entry>
  <entry>
    <title>深度优先搜索与广度优先搜索</title>
    <url>/Java/%E6%B7%B1%E5%BA%A6%E4%BC%98%E5%85%88%E6%90%9C%E7%B4%A2%E4%B8%8E%E5%B9%BF%E5%BA%A6%E4%BC%98%E5%85%88%E6%90%9C%E7%B4%A2/</url>
    <content><![CDATA[<h1 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h1><p>（1）图的表示方法：邻接矩阵（二维数组）、邻接表（链表数组【链表的链表】）。</p>
<p>（2）图的搜索方法：深度优先搜索（DFS）和广度优先搜索（BFS）。</p>
<a id="more"></a>
<h1 id="图的深度优先搜索"><a href="#图的深度优先搜索" class="headerlink" title="图的深度优先搜索"></a>图的深度优先搜索</h1><p>（1）用栈记录下一步的走向。访问一个顶点的过程中要做三件事：</p>
<pre><code>       ①访问顶点

       ②顶点入栈，以便记住它

       ③标记顶点，以便不会再访问它

（2）访问规则：

        a.如果可能，访问一个邻接的未访问顶点，标记它，并入栈。

        b.当不能执行a时（没有邻接的未访问顶点），如果栈不为空，就从栈中弹出一个顶点。

        c.如果不能执行规则a和b，就完成了整个搜索过程。

（3）实现：基于以上规则，循环执行，直到栈为空。每次循环各种，它做四件事：

       ①用peek()方法检查栈顶的顶点。

       ②试图找到这个顶点还未访问的邻接点。

       ③如果没有找到，出栈。

       ④如果找到这样的顶点，访问并入栈。</code></pre><p><img src="/images/dfs.jpg" alt="alt"></p>
<p>比如，采用dfs遍历上图，从A点出发，遍历结果（入栈顺序）为：ABCDE。</p>
<h1 id="图的广度优先搜索"><a href="#图的广度优先搜索" class="headerlink" title="图的广度优先搜索"></a>图的广度优先搜索</h1><p> （1）用队列记录下一步的走向。深度优先搜素表现的好像是尽快远离起点似的，相反，广度优先搜索中，算法好像要尽可能靠近起始点。</p>
<pre><code>（2）访问规则：

        a.访问下一个未访问的邻接点（如果存在），这个顶点必须是当前顶点的邻接点，标记它，并入队列。

        b.如果因为已经没有未访问顶点而不能执行规则a，那么从队列头取一个顶点（如果存在），并使其成为当前顶点。

        c.如果因为队列为空而不能执行规则b，则完成了整个搜索过程。

（3）实现：基于以上规则，对下图做bfs遍历，其队列的变化如下表所示：</code></pre><p><img src="/images/bfs.jpg" alt="alt"></p>
<p><img src="/images/bfs1.jpg" alt="alt"><br>   从而，遍历的结果（入队顺序）为ABCDEFGHI。如果采用bfs遍历3中的图，结果为：ABDCE。</p>
<pre><code>（4）特性：广度优先搜索首先找到与起始点相距一条边的所有顶点，然后是与起始点相距两条边的顶点，以此类推。如果要寻找起始顶点到指定顶点的最短距离，那么这个属性非常有用。首先执行BFS，当找到指定顶点时，就可以说这条路径是到这个顶点的最短路径。如果有更短的路径，BFS算法就应该已经找到它了。</code></pre><h1 id="Java实现"><a href="#Java实现" class="headerlink" title="Java实现"></a>Java实现</h1><p>DFS、BFS的代码实现如下：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">public class Graph &#123;</span><br><span class="line">	private final int MAX_VERTS &#x3D; 20;</span><br><span class="line">	private Vertex vertexList[];&#x2F;&#x2F; 顶点数组</span><br><span class="line">	private int adjMat[][];&#x2F;&#x2F; 邻接矩阵</span><br><span class="line">	private int nVerts;&#x2F;&#x2F; 当前顶点总数</span><br><span class="line">	private StackX theStack;</span><br><span class="line">	private Queue theQueue;</span><br><span class="line"> </span><br><span class="line">	public static void main(String[] args) &#123;</span><br><span class="line">		Graph theGraph &#x3D; new Graph();</span><br><span class="line">		theGraph.addVertex(&#39;A&#39;);</span><br><span class="line">		theGraph.addVertex(&#39;B&#39;);</span><br><span class="line">		theGraph.addVertex(&#39;C&#39;);</span><br><span class="line">		theGraph.addVertex(&#39;D&#39;);</span><br><span class="line">		theGraph.addVertex(&#39;E&#39;);</span><br><span class="line"> </span><br><span class="line">		theGraph.addEdge(0, 1);</span><br><span class="line">		theGraph.addEdge(1, 2);</span><br><span class="line">		theGraph.addEdge(0, 3);</span><br><span class="line">		theGraph.addEdge(3, 4);</span><br><span class="line"> </span><br><span class="line">		System.out.print(&quot;visits:&quot;);</span><br><span class="line">		&#x2F;&#x2F; theGraph.dfs();</span><br><span class="line">		theGraph.bfs();</span><br><span class="line">		System.out.println();</span><br><span class="line">	&#125;</span><br><span class="line"> </span><br><span class="line">	public Graph() &#123;&#x2F;&#x2F; 构造图</span><br><span class="line">		vertexList &#x3D; new Vertex[MAX_VERTS];</span><br><span class="line"> </span><br><span class="line">		adjMat &#x3D; new int[MAX_VERTS][MAX_VERTS];</span><br><span class="line">		nVerts &#x3D; 0;</span><br><span class="line">		for (int i &#x3D; 0; i &lt; MAX_VERTS; i++) &#123;</span><br><span class="line">			for (int j &#x3D; 0; j &lt; MAX_VERTS; j++) &#123;</span><br><span class="line">				adjMat[i][j] &#x3D; 0;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">		theStack &#x3D; new StackX();</span><br><span class="line">		theQueue &#x3D; new Queue();</span><br><span class="line">	&#125;</span><br><span class="line"> </span><br><span class="line">	public void addVertex(char lab) &#123;&#x2F;&#x2F; 添加顶点</span><br><span class="line">		vertexList[nVerts++] &#x3D; new Vertex(lab);</span><br><span class="line">	&#125;</span><br><span class="line"> </span><br><span class="line">	public void addEdge(int start, int end) &#123;&#x2F;&#x2F; 添加边</span><br><span class="line">		adjMat[start][end] &#x3D; 1;</span><br><span class="line">		adjMat[end][start] &#x3D; 1;</span><br><span class="line">	&#125;</span><br><span class="line"> </span><br><span class="line">	public void displayVertex(int v) &#123;&#x2F;&#x2F; 打印数组中v位置下的顶点名</span><br><span class="line">		System.out.print(vertexList[v].lable);</span><br><span class="line">	&#125;</span><br><span class="line"> </span><br><span class="line">	public int getAdjUnvisitedVertex(int v) &#123;&#x2F;&#x2F; 获取和v邻接的未访问的顶点</span><br><span class="line">		for (int i &#x3D; 0; i &lt; nVerts; i++) &#123;</span><br><span class="line">			if (adjMat[v][i] &#x3D;&#x3D; 1 &amp;&amp; vertexList[i].wasVisited &#x3D;&#x3D; false) &#123;</span><br><span class="line">				return i;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">		return -1;</span><br><span class="line">	&#125;</span><br><span class="line"> </span><br><span class="line">	public void dfs() &#123;&#x2F;&#x2F; 深度优先搜索</span><br><span class="line">		vertexList[0].wasVisited &#x3D; true;</span><br><span class="line">		displayVertex(0);</span><br><span class="line">		theStack.push(0);</span><br><span class="line"> </span><br><span class="line">		while (!theStack.isEmpty()) &#123;</span><br><span class="line">			int v &#x3D; getAdjUnvisitedVertex(theStack.peek());</span><br><span class="line">			if (v &#x3D;&#x3D; -1) &#123;</span><br><span class="line">				theStack.pop();</span><br><span class="line">			&#125; else &#123;</span><br><span class="line">				vertexList[v].wasVisited &#x3D; true;</span><br><span class="line">				displayVertex(v);</span><br><span class="line">				theStack.push(v);</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line"> </span><br><span class="line">		for (int i &#x3D; 0; i &lt; nVerts; i++) &#123;</span><br><span class="line">			vertexList[i].wasVisited &#x3D; false;&#x2F;&#x2F; 重置，防止后边再次使用dfs</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line"> </span><br><span class="line">	public void bfs() &#123;&#x2F;&#x2F; 广度优先搜索</span><br><span class="line">		vertexList[0].wasVisited &#x3D; true;</span><br><span class="line">		displayVertex(0);</span><br><span class="line">		theQueue.insert(0);</span><br><span class="line">		int v2;</span><br><span class="line"> </span><br><span class="line">		while (!theQueue.isEmpty()) &#123;</span><br><span class="line">			int v1 &#x3D; theQueue.remove();</span><br><span class="line"> </span><br><span class="line">			while ((v2 &#x3D; getAdjUnvisitedVertex(v1)) !&#x3D; -1) &#123;</span><br><span class="line">				vertexList[v2].wasVisited &#x3D; true;</span><br><span class="line">				displayVertex(v2);</span><br><span class="line">				theQueue.insert(v2);</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line"> </span><br><span class="line">		for (int j &#x3D; 0; j &lt; nVerts; j++) &#123;</span><br><span class="line">			vertexList[j].wasVisited &#x3D; false;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line">class StackX &#123;&#x2F;&#x2F; 自定义栈</span><br><span class="line">	private final int SIZE &#x3D; 20;</span><br><span class="line">	private int[] st;</span><br><span class="line">	private int top;</span><br><span class="line"> </span><br><span class="line">	public StackX() &#123;</span><br><span class="line">		st &#x3D; new int[SIZE];</span><br><span class="line">		top &#x3D; -1;</span><br><span class="line">	&#125;</span><br><span class="line"> </span><br><span class="line">	public void push(int j) &#123;</span><br><span class="line">		st[++top] &#x3D; j;</span><br><span class="line">	&#125;</span><br><span class="line"> </span><br><span class="line">	public int pop() &#123;</span><br><span class="line">		if (top &#x3D;&#x3D; 0) &#123;</span><br><span class="line">			return -1;</span><br><span class="line">		&#125;</span><br><span class="line">		return st[--top];</span><br><span class="line">	&#125;</span><br><span class="line"> </span><br><span class="line">	public int peek() &#123;</span><br><span class="line">		return st[top];</span><br><span class="line">	&#125;</span><br><span class="line"> </span><br><span class="line">	public boolean isEmpty() &#123;</span><br><span class="line">		return (top &#x3D;&#x3D; -1);</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line">class Queue &#123;</span><br><span class="line">	private final int SIZE &#x3D; 20;</span><br><span class="line">	private int[] queArray;</span><br><span class="line">	private int front;</span><br><span class="line">	private int rear;</span><br><span class="line"> </span><br><span class="line">	public Queue() &#123;</span><br><span class="line">		queArray &#x3D; new int[SIZE];</span><br><span class="line">		front &#x3D; 0;</span><br><span class="line">		rear &#x3D; -1;</span><br><span class="line">	&#125;</span><br><span class="line"> </span><br><span class="line">	public void insert(int j) &#123;&#x2F;&#x2F; 入队</span><br><span class="line">		if (rear &#x3D;&#x3D; SIZE - 1) &#123;</span><br><span class="line">			rear &#x3D; -1;</span><br><span class="line">		&#125;</span><br><span class="line">		queArray[++rear] &#x3D; j;</span><br><span class="line">	&#125;</span><br><span class="line"> </span><br><span class="line">	public int remove() &#123;&#x2F;&#x2F; 出队</span><br><span class="line">		if (!isEmpty()) &#123;</span><br><span class="line">			return queArray[front++];</span><br><span class="line">		&#125; else &#123;</span><br><span class="line">			return -1;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line"> </span><br><span class="line">	public boolean isEmpty() &#123;</span><br><span class="line">		return (rear + 1 &#x3D;&#x3D; front);</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line">class Vertex &#123;</span><br><span class="line">	public char lable;&#x2F;&#x2F; 名字</span><br><span class="line">	public boolean wasVisited;</span><br><span class="line"> </span><br><span class="line">	public Vertex(char lab) &#123;</span><br><span class="line">		lable &#x3D; lab;</span><br><span class="line">		wasVisited &#x3D; false;</span><br><span class="line">	&#125;</span><br><span class="line"> &#125;</span><br></pre></td></tr></table></figure>
<p>输出：</p>
<p>visits:ABDCE</p>
]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>算法</tag>
      </tags>
  </entry>
  <entry>
    <title>java集合之HashMap源码分析</title>
    <url>/Java/java%E9%9B%86%E5%90%88%E4%B9%8BHashMap/</url>
    <content><![CDATA[<h1 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h1><p>HashMap 作为一种非常非常常用的数据结构，几乎每天都要见面，今天就安排他一下!</p>
<a id="more"></a>
<h1 id="HashMap存储的实现"><a href="#HashMap存储的实现" class="headerlink" title="HashMap存储的实现"></a>HashMap存储的实现</h1><p>当我们新new一个HashMap，并将多个key-value放入HashMap</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">HashMap&lt;String , String&gt; map &#x3D; new HashMap&lt;String , String&gt;();</span><br><span class="line">map.put(&quot;王思聪&quot; , &quot;有钱&quot;);</span><br><span class="line">map.put(&quot;姚明&quot; , &quot;个子高&quot;);   </span><br><span class="line">map.put(&quot;SZ&quot; , &quot;很帅&quot;);</span><br></pre></td></tr></table></figure>
<p>HashMap 采用一种所谓的“Hash 算法”来决定每个元素的存储位置。</p>
<p>当程序执行 map.put(“王思聪” , “有钱”); 时，系统将调用”王思聪”的 hashCode() 方法得到其 hashCode 值——每个 Java 对象都有 hashCode() 方法，都可通过该方法获得它的 hashCode 值。得到这个对象的 hashCode 值之后，系统会根据该 hashCode 值来决定该元素的存储位置。</p>
<p>我们可以看 HashMap 类的 put(K key , V value) 方法的源代码(在 JDK 安装目录下可以找到一个 src.zip 压缩文件，该文件里包含了 Java 基础类库的所有源文件。注释为我手动标注…)：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">public V put(K key, V value)   </span><br><span class="line">&#123;   </span><br><span class="line"> &#x2F;&#x2F; 如果 key 为 null，调用 putForNullKey 方法进行处理  </span><br><span class="line"> if (key &#x3D;&#x3D; null)   </span><br><span class="line">     return putForNullKey(value);   </span><br><span class="line"> &#x2F;&#x2F; 根据 key 的 keyCode 计算 Hash 值  </span><br><span class="line"> int hash &#x3D; hash(key.hashCode());   </span><br><span class="line"> &#x2F;&#x2F; 搜索指定 hash 值在对应 table 中的索引  </span><br><span class="line">     int i &#x3D; indexFor(hash, table.length);  </span><br><span class="line"> &#x2F;&#x2F; 如果 i 索引处的 Entry 不为 null，通过循环不断遍历 e 元素的下一个元素  </span><br><span class="line"> for (Entry&lt;K,V&gt; e &#x3D; table[i]; e !&#x3D; null; e &#x3D; e.next)   </span><br><span class="line"> &#123;   </span><br><span class="line">     Object k;   </span><br><span class="line">     &#x2F;&#x2F; 找到指定 key 与需要放入的 key 相等（hash 值相同  </span><br><span class="line">     &#x2F;&#x2F; 通过 equals 比较放回 true）  </span><br><span class="line">     if (e.hash &#x3D;&#x3D; hash &amp;&amp; ((k &#x3D; e.key) &#x3D;&#x3D; key   </span><br><span class="line">         || key.equals(k)))   </span><br><span class="line">     &#123;   </span><br><span class="line">         V oldValue &#x3D; e.value;   </span><br><span class="line">         e.value &#x3D; value;   </span><br><span class="line">         e.recordAccess(this);   </span><br><span class="line">         return oldValue;   </span><br><span class="line">     &#125;   </span><br><span class="line"> &#125;   </span><br><span class="line"> &#x2F;&#x2F; 如果 i 索引处的 Entry 为 null，表明此处还没有 Entry   </span><br><span class="line"> modCount++;   </span><br><span class="line"> &#x2F;&#x2F; 将 key、value 添加到 i 索引处  </span><br><span class="line"> addEntry(hash, key, value, i);   </span><br><span class="line"> return null;   </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>上面程序中用到了一个重要的内部接口：Map.Entry，每个 Map.Entry 其实就是一个 key-value 对。从上面程序中可以看出：当系统决定存储 HashMap 中的 key-value 对时，仅仅只是根据 key 来计算并决定每个 Entry 的存储位置。我们完全可以把 Map 集合中的 value 当成 key 的附属，当系统决定了 key 的存储位置之后，value 随之保存在那里即可。</p>
<h1 id="Hash算法"><a href="#Hash算法" class="headerlink" title="Hash算法"></a>Hash算法</h1><p>在上面的源代码中提供了一个根据hashcode()返回值计算hash码的方法，继续查看其源码如下</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">static int hash(int h)   </span><br><span class="line">&#123;   </span><br><span class="line">    h ^&#x3D; (h &gt;&gt;&gt; 20) ^ (h &gt;&gt;&gt; 12);   </span><br><span class="line">    return h ^ (h &gt;&gt;&gt; 7) ^ (h &gt;&gt;&gt; 4);   </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>可以看到hashcode()方法为一个仅与参数自身有关的方法，对于任意给定的对象，只要它的 hashCode() 返回值相同，那么程序调用 hash(int h) 方法所计算得到的 Hash 码值总是相同的。接下来程序会调用 indexFor(int h, int length) 方法来计算该对象应该保存在 table 数组的哪个索引处。indexFor(int h, int length) 方法的代码如下：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">static int indexFor(int h, int length)   </span><br><span class="line">&#123;   </span><br><span class="line">    return h &amp; (length-1);   </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这个方法非常巧妙，它总是通过 h &amp;(table.length -1) 来得到该对象的保存位置——而 HashMap 底层数组的长度总是 2 的 n 次方，这一点可参看后面关于 HashMap 构造器的介绍。</p>
<p>当 length 总是 2 的倍数时，h &amp; (length-1) 将是一个非常巧妙的设计：假设 h=5,length=16, 那么 h &amp; length - 1 将得到 5；如果 h=6,length=16, 那么 h &amp; length - 1 将得到 6 ……如果 h=15,length=16, 那么 h &amp; length - 1 将得到 15；但是当 h=16 时 , length=16 时，那么 h &amp; length - 1 将得到 0 了；当 h=17 时 , length=16 时，那么 h &amp; length - 1 将得到 1 了……这样保证计算得到的索引值总是位于 table 数组的索引之内。</p>
<p>根据上面 put 方法的源代码可以看出，当程序试图将一个 key-value 对放入 HashMap 中时，程序首先根据该 key 的 hashCode() 返回值决定该 Entry 的存储位置：如果两个 Entry 的 key 的 hashCode() 返回值相同，那它们的存储位置相同。如果这两个 Entry 的 key 通过 equals 比较返回 true，新添加 Entry 的 value 将覆盖集合中原有 Entry 的 value，但 key 不会覆盖。如果这两个 Entry 的 key 通过 equals 比较返回 false，新添加的 Entry 将与集合中原有 Entry 形成 Entry 链，而且新添加的 Entry 位于 Entry 链的头部——具体说明继续看 addEntry() 方法的说明。</p>
<p>当向 HashMap 中添加 key-value 对，由其 key 的 hashCode() 返回值决定该 key-value 对（就是 Entry 对象）的存储位置。当两个 Entry 对象的 key 的 hashCode() 返回值相同时，将由 key 通过 eqauls() 比较值决定是采用覆盖行为（返回 true），还是产生 Entry 链（返回 false）。</p>
<p>上面程序中还调用了 addEntry(hash, key, value, i); 代码，其中 addEntry 是 HashMap 提供的一个包访问权限的方法，该方法仅用于添加一个 key-value 对。下面是该方法的代码：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">void addEntry(int hash, K key, V value, int bucketIndex)   </span><br><span class="line">&#123;   </span><br><span class="line">    &#x2F;&#x2F; 获取指定 bucketIndex 索引处的 Entry   </span><br><span class="line">    Entry&lt;K,V&gt; e &#x3D; table[bucketIndex];     &#x2F;&#x2F; </span><br><span class="line">    &#x2F;&#x2F; 将新创建的 Entry 放入 bucketIndex 索引处，并让新的 Entry 指向原来的 Entry   </span><br><span class="line">    table[bucketIndex] &#x3D; new Entry&lt;K,V&gt;(hash, key, value, e);   </span><br><span class="line">    &#x2F;&#x2F; 如果 Map 中的 key-value 对的数量超过了极限  </span><br><span class="line">    if (size++ &gt;&#x3D; threshold)   </span><br><span class="line">        &#x2F;&#x2F; 把 table 对象的长度扩充到 2 倍。  </span><br><span class="line">        resize(2 * table.length);    &#x2F;&#x2F; </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>上面方法的代码很简单，但其中包含了一个非常优雅的设计：系统总是将新添加的 Entry 对象放入 table 数组的 bucketIndex 索引处——如果 bucketIndex 索引处已经有了一个 Entry 对象，那新添加的 Entry 对象指向原有的 Entry 对象（产生一个 Entry 链），如果 bucketIndex 索引处没有 Entry 对象，也就是上面程序①号代码的 e 变量是 null，也就是新放入的 Entry 对象指向 null，也就是没有产生 Entry 链。</p>
<p>上面程序中还有这样两个变量：</p>
<pre><code>* size：该变量保存了该 HashMap 中所包含的 key-value 对的数量。
* threshold：该变量包含了 HashMap 能容纳的 key-value 对的极限，它的值等于 HashMap 的容量乘以负载因子（load factor）。</code></pre><p>可以看出，当 size++ &gt;= threshold 时，HashMap 会自动调用 resize 方法扩充 HashMap 的容量。每扩充一次，HashMap 的容量就增大一倍。</p>
<h1 id="HashMap中的构造器"><a href="#HashMap中的构造器" class="headerlink" title="HashMap中的构造器"></a>HashMap中的构造器</h1><p>之前程序中的table实际上就是一个普通数组，，每个数组都有一个固定的长度，这个数组的长度就是 HashMap 的容量。HashMap 包含如下几个构造器：</p>
<pre><code>HashMap()：构建一个初始容量为 16，负载因子为 0.75 的 HashMap。
HashMap(int initialCapacity)：构建一个初始容量为 initialCapacity，负载因子为 0.75 的 HashMap。
HashMap(int initialCapacity, float loadFactor)：以指定初始容量、指定的负载因子创建一个 HashMap。</code></pre><p>当创建一个 HashMap 时，系统会自动创建一个 table 数组来保存 HashMap 中的 Entry，下面是 HashMap 中一个构造器的代码：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&#x2F;&#x2F; 以指定初始化容量、负载因子创建 HashMap   </span><br><span class="line"> public HashMap(int initialCapacity, float loadFactor)   </span><br><span class="line"> &#123;   </span><br><span class="line">     &#x2F;&#x2F; 初始容量不能为负数  </span><br><span class="line">     if (initialCapacity &lt; 0)   </span><br><span class="line">         throw new IllegalArgumentException(   </span><br><span class="line">        &quot;Illegal initial capacity: &quot; +   </span><br><span class="line">             initialCapacity);   </span><br><span class="line">     &#x2F;&#x2F; 如果初始容量大于最大容量，让出示容量  </span><br><span class="line">     if (initialCapacity &gt; MAXIMUM_CAPACITY)   </span><br><span class="line">         initialCapacity &#x3D; MAXIMUM_CAPACITY;   </span><br><span class="line">     &#x2F;&#x2F; 负载因子必须大于 0 的数值  </span><br><span class="line">     if (loadFactor &lt;&#x3D; 0 || Float.isNaN(loadFactor)) &#x2F;&#x2F;标记一 </span><br><span class="line">         throw new IllegalArgumentException(   </span><br><span class="line">         loadFactor);   </span><br><span class="line">     &#x2F;&#x2F; 计算出大于 initialCapacity 的最小的 2 的 n 次方值。  </span><br><span class="line">     int capacity &#x3D; 1;   </span><br><span class="line">     while (capacity &lt; initialCapacity)   </span><br><span class="line">         capacity &lt;&lt;&#x3D; 1;   </span><br><span class="line">     this.loadFactor &#x3D; loadFactor;   </span><br><span class="line">     &#x2F;&#x2F; 设置容量极限等于容量 * 负载因子  </span><br><span class="line">     threshold &#x3D; (int)(capacity * loadFactor);   </span><br><span class="line">     &#x2F;&#x2F; 初始化 table 数组  </span><br><span class="line">     table &#x3D; new Entry[capacity];            &#x2F;&#x2F;  </span><br><span class="line">     init();   </span><br><span class="line"> &#125;</span><br></pre></td></tr></table></figure>
<p>上面代码中标记一的代码包含了一个简洁的代码实现：找出大于 initialCapacity 的、最小的 2 的 n 次方值，并将其作为 HashMap 的实际容量（由 capacity 变量保存）。例如给定 initialCapacity 为 10，那么该 HashMap 的实际容量就是 16。<br>程序①号代码处可以看到：table 的实质就是一个数组，一个长度为 capacity 的数组。</p>
<p>对于 HashMap 及其子类而言，它们采用 Hash 算法来决定集合中元素的存储位置。当系统开始初始化 HashMap 时，系统会创建一个长度为 capacity 的 Entry 数组，这个数组里可以存储元素的位置被称为“桶（bucket）”，每个 bucket 都有其指定索引，系统可以根据其索引快速访问该 bucket 里存储的元素。</p>
<p>无论何时，HashMap 的每个“桶”只存储一个元素（也就是一个 Entry），由于 Entry 对象可以包含一个引用变量（就是 Entry 构造器的的最后一个参数）用于指向下一个 Entry，因此可能出现的情况是：HashMap 的 bucket 中只有一个 Entry，但这个 Entry 指向另一个 Entry ——这就形成了一个 Entry 链。如图所示(图片来源于网络)：</p>
<p><img src="/images/haspmap.jpg" alt="alt"></p>
<p>当 HashMap 的每个 bucket 里存储的 Entry 只是单个 Entry ——也就是没有通过指针产生 Entry 链时，此时的 HashMap 具有最好的性能：当程序通过 key 取出对应 value 时，系统只要先计算出该 key 的 hashCode() 返回值，在根据该 hashCode 返回值找出该 key 在 table 数组中的索引，然后取出该索引处的 Entry，最后返回该 key 对应的 value 即可。看 HashMap 类的 get(K key) 方法代码：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&#x2F;&#x2F; 以指定初始化容量、负载因子创建 HashMap   </span><br><span class="line">public V get(Object key)   </span><br><span class="line">&#123;   </span><br><span class="line"> &#x2F;&#x2F; 如果 key 是 null，调用 getForNullKey 取出对应的 value   </span><br><span class="line"> if (key &#x3D;&#x3D; null)   </span><br><span class="line">     return getForNullKey();   </span><br><span class="line"> &#x2F;&#x2F; 根据该 key 的 hashCode 值计算它的 hash 码  </span><br><span class="line"> int hash &#x3D; hash(key.hashCode());   </span><br><span class="line"> &#x2F;&#x2F; 直接取出 table 数组中指定索引处的值，  </span><br><span class="line"> for (Entry&lt;K,V&gt; e &#x3D; table[indexFor(hash, table.length)];   </span><br><span class="line">     e !&#x3D; null;   </span><br><span class="line">     &#x2F;&#x2F; 搜索该 Entry 链的下一个 Entr   </span><br><span class="line">     e &#x3D; e.next)         &#x2F;&#x2F; ①  </span><br><span class="line"> &#123;   </span><br><span class="line">     Object k;   </span><br><span class="line">     &#x2F;&#x2F; 如果该 Entry 的 key 与被搜索 key 相同  </span><br><span class="line">     if (e.hash &#x3D;&#x3D; hash &amp;&amp; ((k &#x3D; e.key) &#x3D;&#x3D; key   </span><br><span class="line">         || key.equals(k)))   </span><br><span class="line">         return e.value;   </span><br><span class="line"> &#125;   </span><br><span class="line"> return null;   </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>从上面代码中可以看出，如果 HashMap 的每个 bucket 里只有一个 Entry 时，HashMap 可以根据索引、快速地取出该 bucket 里的 Entry；在发生“Hash 冲突”的情况下，单个 bucket 里存储的不是一个 Entry，而是一个 Entry 链，系统只能必须按顺序遍历每个 Entry，直到找到想搜索的 Entry 为止——如果恰好要搜索的 Entry 位于该 Entry 链的最末端（该 Entry 是最早放入该 bucket 中），那系统必须循环到最后才能找到该元素。</p>
<p>归纳起来简单地说，HashMap 在底层将 key-value 当成一个整体进行处理，这个整体就是一个 Entry 对象。HashMap 底层采用一个 Entry[] 数组来保存所有的 key-value 对，当需要存储一个 Entry 对象时，会根据 Hash 算法来决定其存储位置；当需要取出一个 Entry 时，也会根据 Hash 算法找到其存储位置，直接取出该 Entry。由此可见：HashMap 之所以能快速存、取它所包含的 Entry，完全类似于现实生活中母亲从小教我们的：不同的东西要放在不同的位置，需要时才能快速找到它。</p>
<p>当创建 HashMap 时，有一个默认的负载因子（load factor），其默认值为 0.75，这是时间和空间成本上一种折衷：增大负载因子可以减少 Hash 表（就是那个 Entry 数组）所占用的内存空间，但会增加查询数据的时间开销，而查询是最频繁的的操作（HashMap 的 get() 与 put() 方法都要用到查询）；减小负载因子会提高数据查询的性能，但会增加 Hash 表所占用的内存空间。</p>
<p>掌握了上面知识之后，我们可以在创建 HashMap 时根据实际需要适当地调整 load factor 的值；如果程序比较关心空间开销、内存比较紧张，可以适当地增加负载因子；如果程序比较关心时间开销，内存比较宽裕则可以适当的减少负载因子。通常情况下，程序员无需改变负载因子的值。</p>
<p>如果开始就知道 HashMap 会保存多个 key-value 对，可以在创建时就使用较大的初始化容量，如果 HashMap 中 Entry 的数量一直不会超过极限容量（capacity * load factor），HashMap 就无需调用 resize() 方法重新分配 table 数组，从而保证较好的性能。当然，开始就将初始容量设置太高可能会浪费空间（系统需要创建一个长度为 capacity 的 Entry 数组），因此创建 HashMap 时初始化容量设置也需要重视起来。</p>
<h1 id="后记"><a href="#后记" class="headerlink" title="后记"></a>后记</h1><p>在jdk1.8中对HashMap进行了优化，之后会结合红黑树在一起说，留个待续…</p>
]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>集合</tag>
      </tags>
  </entry>
  <entry>
    <title>如何优雅实现属性的动态注入</title>
    <url>/Java/%E5%A6%82%E4%BD%95%E4%BC%98%E9%9B%85%E5%AE%9E%E7%8E%B0%E5%B1%9E%E6%80%A7%E7%9A%84%E5%8A%A8%E6%80%81%E6%B3%A8%E5%85%A5/</url>
    <content><![CDATA[<h1 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h1><p>这是在实际开发中遇到的一个问题。从数据库查询返回的 List&lt; Map&lt; String, Object&gt;&gt; 的集合。并且返回的列名是中文的，项目也没有使用mybatis 直接使用的jdbcTemplate. 并且字段还超级多，这样将数据转换的时候如果一个一个的注入就会让代码臭长臭长的，所以才有了动态注入。<br>我这里我整个思路都贴出来。</p>
<a id="more"></a>
<h1 id="实例类Entry"><a href="#实例类Entry" class="headerlink" title="实例类Entry"></a>实例类Entry</h1><p>我们先建一个entry类。用于对象存储。<br>我这里 创建一个BaseDateBean 的类</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">@Setter</span><br><span class="line">@Getter</span><br><span class="line">public class BaseDateBean &#123;</span><br><span class="line"></span><br><span class="line">    private String  attribute1;</span><br><span class="line">    private String  attribute2;</span><br><span class="line">    private String  attribute3;</span><br><span class="line">    private String  attribute4;</span><br><span class="line">    private String  attribute5;</span><br><span class="line">    private String  attribute6;</span><br><span class="line">    private String  attribute7;</span><br><span class="line">    private String  attribute8;</span><br><span class="line">    private String  attribute9;</span><br><span class="line">    private String  attribute10;</span><br><span class="line">    private String  attribute11;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>举例就不列很多了，比如有100个，所以如果一个一个的set注入就很low了。</p>
<h1 id="创建map映射"><a href="#创建map映射" class="headerlink" title="创建map映射"></a>创建map映射</h1><p>在创建好实体类后，还得创建一个静态的map 集合，将数据库的列名和我们实体类的属性名做一个一一对应。这里创建的这个map 集合是我个人愚见。没有想到更好的办法就先这样处理的。<br>我们创建一个BaseDataMap类</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">public  class BaseDataMap&#123;</span><br><span class="line">    private BaseDataMap()&#123;&#125;</span><br><span class="line">    public static final Map&lt;String,String&gt; cnEnMap&#x3D;new HashMap&lt;&gt;();</span><br><span class="line">    static&#123;</span><br><span class="line">        cnEnMap.put(&quot;属性1&quot;,&quot;attribute1&quot;);</span><br><span class="line">        cnEnMap.put(&quot;属性2&quot;,&quot;attribute2&quot;);</span><br><span class="line">        cnEnMap.put(&quot;属性3&quot;,&quot;attribute3&quot;);</span><br><span class="line">        cnEnMap.put(&quot;属性4&quot;,&quot;attribute4&quot;);</span><br><span class="line">        cnEnMap.put(&quot;属性5&quot;,&quot;attribute5&quot;);</span><br><span class="line">        cnEnMap.put(&quot;属性6&quot;,&quot;attribute6&quot;);</span><br><span class="line">        cnEnMap.put(&quot;属性7&quot;,&quot;attribute7&quot;);</span><br><span class="line">        cnEnMap.put(&quot;属性8&quot;,&quot;attribute8&quot;);</span><br><span class="line">        cnEnMap.put(&quot;属性9&quot;,&quot;attribute9&quot;);</span><br><span class="line">        cnEnMap.put(&quot;属性10&quot;,&quot;attribute10&quot;);</span><br><span class="line">        cnEnMap.put(&quot;属性11&quot;,&quot;attribute11&quot;);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>可以看到就是一个动态的map。</p>
<h1 id="映射类"><a href="#映射类" class="headerlink" title="映射类"></a>映射类</h1><p>接下来就是核心代码了。我们创建一个ReflectHelper类</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">@Slf4j</span><br><span class="line">public class ReflectHelper &#123;</span><br><span class="line"></span><br><span class="line">    private Class cls;</span><br><span class="line">    &#x2F;**</span><br><span class="line">     * 传过来的对象</span><br><span class="line">     *&#x2F;</span><br><span class="line">    private Object obj;</span><br><span class="line">    private Hashtable&lt;String, Method&gt; getMethods &#x3D; null;</span><br><span class="line">    private Hashtable&lt;String, Method&gt; setMethods &#x3D; null;</span><br><span class="line">    public ReflectHelper(Object o) &#123;</span><br><span class="line">        obj &#x3D; o;</span><br><span class="line">        initMethods();</span><br><span class="line">    &#125;</span><br><span class="line">    public void initMethods() &#123;</span><br><span class="line">        getMethods &#x3D; new Hashtable&lt;&gt;();</span><br><span class="line">        setMethods &#x3D; new Hashtable&lt;&gt;();</span><br><span class="line">        cls &#x3D; obj.getClass();</span><br><span class="line">        Method[] methods &#x3D; cls.getMethods();</span><br><span class="line">        &#x2F;&#x2F; 定义正则表达式，从方法中过滤出getter &#x2F; setter 函数.</span><br><span class="line">        String gs &#x3D; &quot;get(\\w )&quot;;</span><br><span class="line">        Pattern getM &#x3D; Pattern.compile(gs);</span><br><span class="line">        String ss &#x3D; &quot;set(\\w )&quot;;</span><br><span class="line">        Pattern setM &#x3D; Pattern.compile(ss);</span><br><span class="line">        &#x2F;&#x2F; 把方法中的&quot;set&quot; 或者 &quot;get&quot; 去掉,$1匹配第一个</span><br><span class="line">        String rapl &#x3D; &quot;$1&quot;;</span><br><span class="line">        String param;</span><br><span class="line">        for (int i &#x3D; 0; i &lt; methods.length;   i) &#123;</span><br><span class="line">            Method m &#x3D; methods[i];</span><br><span class="line">            String methodName &#x3D; m.getName();</span><br><span class="line">            if (Pattern.matches(gs, methodName)) &#123;</span><br><span class="line">                param &#x3D; getM.matcher(methodName).replaceAll(rapl).toLowerCase();</span><br><span class="line">                getMethods.put(param, m);</span><br><span class="line">            &#125; else if (Pattern.matches(ss, methodName)) &#123;</span><br><span class="line">                param &#x3D; setM.matcher(methodName).replaceAll(rapl).toLowerCase();</span><br><span class="line">                setMethods.put(param, m);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    public boolean setMethodValue(String property,Object object) &#123;</span><br><span class="line">        Method m &#x3D; setMethods.get(property.toLowerCase());</span><br><span class="line">        if (m !&#x3D; null) &#123;</span><br><span class="line">            try &#123;</span><br><span class="line">                &#x2F;&#x2F; 调用目标类的setter函数</span><br><span class="line">                m.invoke(obj, object);</span><br><span class="line">                return true;</span><br><span class="line">            &#125; catch (Exception ex) &#123;</span><br><span class="line">                ex.printStackTrace();</span><br><span class="line">                return false;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        return false;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>上面代码可以看到其实也就两个方法setMethodValue 和initMethods 。<br>initMethods 方法是在实例化 ReflectHelper 这个类的时候执行的，主要的工作就是找到我们需要动态注入实例类的get 和set 方法。而setMethodValue 方法就是给这个属性赋值的。</p>
<h1 id="实现方法"><a href="#实现方法" class="headerlink" title="实现方法"></a>实现方法</h1><p> 现在准备工作做好了，怎么使用呢?<br> <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">private List&lt;BaseDateBean&gt; getBaseDateBean(List&lt;Map&lt;String, Object&gt;&gt; mapList)&#123;</span><br><span class="line">       List&lt;BaseDateBean&gt; list&#x3D;new ArrayList&lt;&gt;();</span><br><span class="line">       if(mapList&#x3D;&#x3D;null||mapList.isEmpty())&#123;</span><br><span class="line">           return list;</span><br><span class="line">       &#125;</span><br><span class="line">       BaseDateBean baseDateBean;</span><br><span class="line">       for(Map&lt;String, Object&gt; map:mapList)&#123;</span><br><span class="line">           baseDateBean&#x3D;new BaseDateBean();</span><br><span class="line">           for(Map.Entry&lt;String, Object&gt; entry : map.entrySet())&#123;</span><br><span class="line">               String mapKey &#x3D; entry.getKey();</span><br><span class="line">               log.info(mapKey);</span><br><span class="line">               ReflectHelper reflectHelper &#x3D; new ReflectHelper(baseDateBean);</span><br><span class="line">               log.info(BaseDataMap.cnEnMap.get(mapKey));</span><br><span class="line">               String value&#x3D;entry.getValue()&#x3D;&#x3D;null?ConstantPool.SEPARATORNULL:entry.getValue().toString();</span><br><span class="line">               log.info(value);</span><br><span class="line">               if(entry.getValue()!&#x3D;null)&#123;</span><br><span class="line">                   reflectHelper.setMethodValue(BaseDataMap.cnEnMap.get(mapKey),String.valueOf(entry.getValue()));</span><br><span class="line">               &#125;</span><br><span class="line">           &#125;</span><br><span class="line">           list.add(baseDateBean);</span><br><span class="line">       &#125;</span><br><span class="line">       return list;</span><br><span class="line">   &#125;</span><br></pre></td></tr></table></figure><br> 遍历list 集合中的map，动态的将属性值注入到实体类中。</p>
]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>动态注入</tag>
        <tag>映射</tag>
      </tags>
  </entry>
  <entry>
    <title>Spring Boot 优雅的实现CORS跨域</title>
    <url>/%E6%A1%86%E6%9E%B6/Spring%20Boot%20%E4%BC%98%E9%9B%85%E7%9A%84%E5%AE%9E%E7%8E%B0CORS%E8%B7%A8%E5%9F%9F/</url>
    <content><![CDATA[<h1 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h1><p>  一般的情况下，项目都是前后端分离的，所以这里就涉及到一个很严重的问题，当协议，端口，IP三者有其一不同就会产生跨域，所以需要做跨域支持。</p>
<a id="more"></a>
<h1 id="测试跨域的文件"><a href="#测试跨域的文件" class="headerlink" title="测试跨域的文件"></a>测试跨域的文件</h1><p>在这之前，我们先写一个测试接口是否跨域的html ,这样下面的测试比较方便。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;!DOCTYPE html&gt;</span><br><span class="line">&lt;html lang&#x3D;&quot;en&quot;&gt;</span><br><span class="line">&lt;head&gt;</span><br><span class="line">    &lt;meta charset&#x3D;&quot;UTF-8&quot;&gt;</span><br><span class="line">    &lt;title&gt;Title&lt;&#x2F;title&gt;</span><br><span class="line">&lt;&#x2F;head&gt;</span><br><span class="line">&lt;link type&#x3D;&quot;test&#x2F;css&quot; href&#x3D;&quot;css&#x2F;style.css&quot; rel&#x3D;&quot;stylesheet&quot;&gt;</span><br><span class="line"></span><br><span class="line">&lt;body&gt;</span><br><span class="line"></span><br><span class="line">	&lt;input type&#x3D;&quot;text&quot; style&#x3D;&quot;width:600px;height:30px;font-size:14px;&quot; id&#x3D;&quot;urlText&quot; value&#x3D;&quot;&quot; &#x2F;&gt;</span><br><span class="line">	&lt;br&gt;</span><br><span class="line">	&lt;input type&#x3D;&quot;button&quot; style&#x3D;&quot;margin: 10px&quot;;  id&#x3D;&quot;cors&quot; value&#x3D;&quot;判断是否可访问&quot;&#x2F;&gt;</span><br><span class="line"></span><br><span class="line">&lt;p&gt;http:&#x2F;&#x2F;localhost:9090&#x2F;test&#x2F;&lt;&#x2F;p&gt;</span><br><span class="line">&lt;script type&#x3D;&quot;text&#x2F;javascript&quot; src&#x3D;&quot;https:&#x2F;&#x2F;code.jquery.com&#x2F;jquery-3.2.1.min.js&quot;&gt;&lt;&#x2F;script&gt;  </span><br><span class="line">&lt;script type&#x3D;&quot;text&#x2F;javascript&quot;&gt;</span><br><span class="line">	$(function()&#123;</span><br><span class="line">	$(&quot;#cors&quot;).click(</span><br><span class="line">		function()&#123;</span><br><span class="line">			var url2 &#x3D; $(&quot;#urlText&quot;).val();</span><br><span class="line">			$.post(&#123;</span><br><span class="line">				contentType:&#39;application&#x2F;x-www-form-urlencoded;charset&#x3D;UTF-8&#39;,</span><br><span class="line">				url:url2,</span><br><span class="line">				data: &quot;&#x2F;rAIeKeSBG1LV XoIq82&#x2F;O&quot;,</span><br><span class="line">				success:function(data)&#123;</span><br><span class="line">					alert(&quot;success&quot;);</span><br><span class="line">				&#125;</span><br><span class="line">			&#125;)</span><br><span class="line">		&#125;);</span><br><span class="line">	&#125;);</span><br><span class="line">&lt;&#x2F;script&gt;</span><br><span class="line">&lt;&#x2F;body&gt;</span><br><span class="line">&lt;&#x2F;html&gt;</span><br></pre></td></tr></table></figure>

<p>接下来我们来学习下在springboot 项目中怎么实现支持跨域。</p>
<h1 id="CrossOrigin-注解"><a href="#CrossOrigin-注解" class="headerlink" title="@CrossOrigin 注解"></a>@CrossOrigin 注解</h1><p>这种方法是springboot 自带的，使用比较简单，在需要支持的跨域的接口上加上这个注解就可以了。<br>比如在我们项目的demo 接口加上注解.就表示这个接口支持跨域，其中origins = “*”<br>表示所有的地址都可以访问这个接口，也可以写具体的地址，表示只有这个地址访问才能访问到接口。<br> <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">@RestController</span><br><span class="line">@CrossOrigin(origin &#x3D;&quot;*&quot;)</span><br><span class="line">public class Demo &#123;</span><br><span class="line">	@RequestMapping(&quot;&#x2F;&quot;)</span><br><span class="line">	public String Demo() &#123;</span><br><span class="line">		return &quot;Hello Word&quot;;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<h2 id="测试"><a href="#测试" class="headerlink" title="测试"></a>测试</h2><p>我们也来测试一下，启动项目后，在浏览器上运行我们的测试的html文件。<br>发现localhost:9090/test/ 是可以访问的。<br>说明跨域是支持的。大伙可以先将注解去掉测试一下，然后加上注解测试一下进行对比。</p>
<p>这种方式虽然很简单，但是缺点也不小，需要跨域的接口都需要加上这个注解，这对前后端分离的项目是不友好的，所以这种方式基本上用的很少。</p>
<h1 id="重写WebMvcConfigurer的addCorsMappings-方法。"><a href="#重写WebMvcConfigurer的addCorsMappings-方法。" class="headerlink" title="重写WebMvcConfigurer的addCorsMappings 方法。"></a>重写WebMvcConfigurer的addCorsMappings 方法。</h1><p>这种方法在实际项目中也用的比较多，是一种全局支持跨域的方法。<br>我们创建一个CorsConfig 类。内容如下：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">@Configuration</span><br><span class="line">public class CorsConfig implements WebMvcConfigurer &#123;</span><br><span class="line">    @Override</span><br><span class="line">    public void addCorsMappings(CorsRegistry registry) &#123;</span><br><span class="line">        registry.addMapping(&quot;&#x2F;**&quot;)&#x2F;&#x2F;项目中的所有接口都支持跨域</span><br><span class="line">                .allowedOrigins(&quot;*&quot;)&#x2F;&#x2F;所有地址都可以访问，也可以配置具体地址</span><br><span class="line">                .allowCredentials(true)</span><br><span class="line">                .allowedMethods(&quot;*&quot;)&#x2F;&#x2F;&quot;GET&quot;, &quot;HEAD&quot;, &quot;POST&quot;, &quot;PUT&quot;, &quot;DELETE&quot;, &quot;OPTIONS&quot;</span><br><span class="line">                .maxAge(3600);&#x2F;&#x2F; 跨域允许时间</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>加上@Configuration 表示是配置类，在项目启动的时候会加载。实现WebMvcConfigurer 接口并重写addCorsMappings 方法。代码比较简单，也有注释。</p>
<p>测试的话，大家可以自行测试，我测试都是通过的和上面一样测试就可以，这里就不占篇幅了。</p>
<h1 id="Filter"><a href="#Filter" class="headerlink" title="Filter"></a>Filter</h1><p>除了上面方法外，也可以使用过滤器。我们创建一个CorsFilter 类，内容如下：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">@Slf4j</span><br><span class="line">@Component</span><br><span class="line">public class CorsFilter implements Filter &#123;</span><br><span class="line">    @Override</span><br><span class="line">    public void doFilter(ServletRequest servletRequest, ServletResponse servletResponse, FilterChain filterChain) throws IOException, ServletException &#123;</span><br><span class="line">        HttpServletResponse response &#x3D; (HttpServletResponse)servletResponse;</span><br><span class="line">        response.setHeader(&quot;Access-Control-Allow-Origin&quot;, &quot;*&quot;);</span><br><span class="line">        response.setHeader(&quot;Access-Control-Allow-Methods&quot;, &quot;POST, PUT, GET, OPTIONS, DELETE&quot;);</span><br><span class="line">        response.setHeader(&quot;Access-Control-Max-Age&quot;, &quot;3600&quot;);</span><br><span class="line">        response.setHeader(&quot;Access-Control-Allow-Headers&quot;, &quot;Origin, X-Requested-With, Content-Type, Accept, client_id, uuid, Authorization&quot;);</span><br><span class="line">        response.setHeader(&quot;Cache-Control&quot;, &quot;no-cache, no-store, must-revalidate&quot;);</span><br><span class="line">        response.setHeader(&quot;Pragma&quot;, &quot;no-cache&quot;);</span><br><span class="line">        filterChain.doFilter(servletRequest,response);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>上面代码中设置response.setHeader(“Access-Control-Allow-Origin”, “*”);表示所有的地址都可以访问项目接口。</p>
<h1 id="番外"><a href="#番外" class="headerlink" title="番外"></a>番外</h1><p>接下来我们再介绍一个常用的功能，前后端分离，在访问接口的时候，有的 公司往往会增加一下专属的后缀名才能访问。实际上没有什么太大的作用，能稍微增加一下系统的安全性。这里我就简单是实现一下。真个都非常简单。<br>一样的是实现WebMvcConfigurer 接口，重写configurePathMatch你方法和增加一个dispatcherServlet。</p>
<p>代码如下：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">@Override</span><br><span class="line">    public void configurePathMatch(PathMatchConfigurer configurer) &#123;</span><br><span class="line">        configurer.setUseRegisteredSuffixPatternMatch(true);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    @Bean</span><br><span class="line">    public ServletRegistrationBean servletRegistrationBean(DispatcherServlet dispatcherServlet) &#123;</span><br><span class="line">        ServletRegistrationBean bean &#x3D; new ServletRegistrationBean(dispatcherServlet);</span><br><span class="line">        bean.addUrlMappings(&quot;*.zlf&quot;);</span><br><span class="line">        return bean;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>
<p>这个功能实现，就只用这个多代码，configurePathMatch方法中设置的configurer.setUseRegisteredSuffixPatternMatch(true); 主要是将index 和index.* 都指向我们controller 中配置的@RequestMapping(“/index”)。</p>
<p>下面的servletRegistrationBean 方法主要是增加自定义拦截器，只有后缀为“.zlf”的接口才放行。</p>
<p>这样两步就简单的实现了接口增加自定义的后缀名了。</p>
<p>到此为止，springboot 支持跨域的方式就差不多了，当然还有其他的实现方式没有研究。这些希望对大家有帮助。</p>
]]></content>
      <categories>
        <category>框架</category>
      </categories>
      <tags>
        <tag>springboot</tag>
        <tag>java</tag>
        <tag>框架</tag>
      </tags>
  </entry>
  <entry>
    <title>Hello World</title>
    <url>/%E7%A8%8B%E5%BA%8F%E4%BA%BA%E7%94%9F/hello-world/</url>
    <content><![CDATA[<p>谈谈建站之路…</p>
<a id="more"></a>
<h2 id="心路历程"><a href="#心路历程" class="headerlink" title="心路历程"></a>心路历程</h2><h3 id="坐井观天"><a href="#坐井观天" class="headerlink" title="坐井观天"></a>坐井观天</h3><p>&ensp;&ensp;转眼毕业已经半年有余，主要的工作状态就好像每天都很忙又感觉不知道自己在忙什么。眨眼一晃半年多就这么过来了，这段时间认识了很多很多人，经历了很多很多事，就像做梦一样。刚好这次赶上疫情(看到的你记得戴口罩，保护好自己)，在家的时间比较多，总结了一下:emm,生活真的好难，好像还是代码简单，那我想尽力把简单的事做好,不然连做自己都做不到，更没什么资格去承担什么责任。</p>
<h3 id="关于本站"><a href="#关于本站" class="headerlink" title="关于本站"></a>关于本站</h3><p>&ensp;&ensp;主要写一些自己在工作以及在平时学习上的总结，这段儿会陆续整理之前想总结一直没下手的,今天先简单整理了两个…不定时更新</p>
<h3 id="总得有个标题吧"><a href="#总得有个标题吧" class="headerlink" title="总得有个标题吧"></a>总得有个标题吧</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">hello word , hello world！</span><br></pre></td></tr></table></figure>

<p><img src="/images/hello-word.jpg" alt="alt"></p>
]]></content>
      <categories>
        <category>程序人生</category>
      </categories>
      <tags>
        <tag>编程</tag>
        <tag>感悟</tag>
      </tags>
  </entry>
</search>
